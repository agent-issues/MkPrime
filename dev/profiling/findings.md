# Profiling findings — mkp

Tagged `[Port]` (move R loop to C++), `[Optimise]` (algorithmic / cache / vectorisation fix inside C++), or `[AT-LIMIT]` (no further wins; skip in future rotations).

A finding is filed only when an isolated `std::chrono` micro-benchmark (or equivalent) reproduces the predicted Δ. A drop in VTune's hotspot share is not a verified win.

| ID | priority | status | kind | title | location & detail |
|----|----------|--------|------|-------|-------------------|
| T-016 | P2 | **REFUTED (2026-05-22), VERIFIED 246/246 tests pass (bit-identity preserved)** | [Optimise] | Orchestrator heap-alloc hoisting — mirror T-008 pattern to const_site_prob_*_raw + cpp_partition_log_likelihood_ecology_raw | **What landed.** Moved `EcoWorkItem` + `EcoThreadScratch` struct definitions earlier in `src/mcmc_ecology.cpp` (before `const_site_prob_*_raw`). Extended `EcoThreadScratch` with `tipStatesZero` / `tipStatesOne` (nTip ints, pre-filled 0/1) and `maxNTip` grow-check field. Converted `const_site_prob_jc_eco_single_raw` and `const_site_prob_mkn_eco_single_raw` to accept `EcoThreadScratch&`; eliminated all 5-7 per-call `std::vector` allocs inside each (buf, initFlg, rootFreqs, zVec copy, tipStates). The two Rcpp wrappers (`const_site_prob_jc_eco_single`, `const_site_prob_mkn_eco_single`) each carry a function-static `EcoThreadScratch`; they copy the Rcpp `IntegerVector zVec` into `scratch.zVec.data()` once, then pass the pointer straight to `_raw`. Updated `compute_eco_work_item_ll` at all three `_raw` call sites to pass `scratch`. Refactored `cpp_partition_log_likelihood_ecology_raw` (the serial partition entry called by ~12 move-handler sites in `mcmc.cpp`) to use a function-static `EcoThreadScratch orcScratch`: pre-scan to size, grow-only-resize, replaced all `std::vector` locals (zPartLocal, neoEl, rootFreqs, buf, initFlg, subStates, subZ, zVec) with scratch pointers, passed `orcScratch` to all `_raw` const_site_prob calls. Updated grow-check in `cpp_log_likelihood_ecology` to include `maxNTip` dimension. **Bench (rodent 60×217, kEco=4, 500 iter).** nChains=1: 45.2 s + 48.6 s (mean 46.9 s; baseline ~46 s). No measurable improvement. **Why no gain.** T-017 Phase 1b already hoisted the dominant large-buf alloc (~300-400 KB per `compute_eco_work_item_ll` call) from the parallel path. Remaining per-call allocs in `const_site_prob_*_raw` are small (buf = (2*nTip-1)*kStates ≤ ~860 doubles for mkn; ~2 KB). T-010 partition cache means serial `cpp_partition_log_likelihood_ecology_raw` is only called on tree-touching moves (~78 % weight), but per-call cost from malloc vs function-static is below the noise floor (~±3 s run-to-run variance on this machine). **Infrastructure value.** Code is cleaner and malloc-free in all hot paths; future calls to `const_site_prob_*_raw` will not regress. Design doc at `dev/profiling/t016-design.md`. |
| T-017 | P0 | **INFRASTRUCTURE-APPLIED (2026-05-22), VERIFIED bit-identity (omp determinism tests pass at OMP_NUM_THREADS ∈ {1,4}); BENCH NULL on rodent — Phase 1b malloc fix didn't recover wall (rodent workload caps theoretical speedup at 2× via 50/50 neo-vs-trans imbalance; OMP overhead eats the ceiling)** | [Optimise] | OpenMP-parallel ecology partition pruner via flattened work-item list | **Origin:** at the end of T-015 the user asked "how do we actually improve aware performance?" Among the options ranked, the highest-leverage code-only win was threaded chain advancement (≥ 4× projected). User picked T-017 + T-018 to start. T-017 attempts: a Plan-then-action sequence (after a prior one-shot Opus agent died silently on the same brief). **Plan phase.** Opus Plan subagent produced a design (advisor-reviewed twice — caught (a) `reduction(+:double)` ULP-order-dependence breaking bit-identity, (b) nested-parallel pitfall, (c) discriminator-confirmed rodent has only 2 partitions × 3 trans subgroups = 4 work items with 50/50 neo-vs-trans split). Design at `dev/profiling/t017-design.md`. Final design: flatten partition × subgroup into a single work-item list, parallelise with `#pragma omp parallel for schedule(dynamic)` + per-thread partial-sum slots + serial merge. **Implementation phase.** Opus impl subagent worked ~80 min, refactored `cpp_partition_log_likelihood_ecology` Rcpp temporaries → `std::vector`, introduced `EcoWorkItem` struct + `compute_eco_work_item_ll` worker, wired the flat work-item construction + parallel for + partial-sum merge into `cpp_log_likelihood_ecology`. Did NOT commit before returning. Salvaged from `mkp/.claude/worktrees/agent-acb308a87806e256e/` (same pattern as T-018 salvage). **Build issues encountered.** (1) User-level `~/.R/Makevars` sets `PKG_CXXFLAGS = ` (empty) which OVERRIDES the package's `PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)` at compile time — `-fopenmp` was making it to link but not compile, so `_OPENMP` was undefined and pragmas were no-ops. (2) Even with `R_MAKEVARS_USER=NUL` bypass attempted, `NUL` is interpreted as a filename by make, not a magic value. (3) Workaround: hard-code `PKG_CXXFLAGS = -fopenmp` in both `src/Makevars` and `src/Makevars.win` (instead of `$(SHLIB_OPENMP_CXXFLAGS)`). Use `R_MAKEVARS_USER=/tmp/empty_makevars` (empty file) to bypass `~/.R/Makevars` for local builds. **Verified.** (1) `_OPENMP=201511` confirmed at runtime via `MKPRIME_T017_DIAG=1` (prints "nWork=4, omp_get_max_threads=4"). (2) 233 / 233 tests pass (`filter="ecology|likelihood|mcmc|omp"`) including 3 new bit-identity tests in `tests/testthat/test-omp-determinism.R`. (3) **Bit-identity verified at OMP_NUM_THREADS ∈ {1, 4}** within a single build (the per-thread partial-sum slot + serial merge guarantees the sum order is fixed). **BENCH NULL.** `rodent_aware_timing.R MKP_TIMING_NCHAINS=4 OMP_NUM_THREADS=4`: aware **182-184 s wall (2.7 iter/s)** vs T-015 serial baseline 184 s. **No measurable improvement.** Also tested nChains=1: OMP=1 = 41.78 s; OMP=4 = 45.75-47.34 s (**13 % slowdown from OMP overhead**). **Root cause: glibc malloc contention.** `compute_eco_work_item_ll` allocates fresh `std::vector<double> buf((maxNode+1) * stride, 0.0)` per call (~400 KB on rodent neo partition: 120 × 432 doubles). With 4 threads concurrently calling `compute_eco_work_item_ll`, all 4 contend for the global malloc lock on every call. Total allocs per outer-iter aware nChains=4 = ~4 chains × ~6 likelihood calls × 4 work items = ~96 malloc/free pairs per outer iter, each blocking until lock is released. This was advisor's pre-flagged risk #5. The advisor predicted 1.55-1.9× projected speedup; the malloc contention drags actual measured speedup down to 1.0×. **Phase 1b LANDED (2026-05-22) — REFUTED on rodent.** `EcoThreadScratch` struct + function-static per-thread scratches in `cpp_log_likelihood_ecology` eliminate the per-call malloc/free that was Phase 1's bottleneck. 246/246 tests pass (3 new omp-determinism passes added; bit-identity preserved). **Bench results (rodent, nChains=4, 500 iter):** OMP=1 174.36 s (2.9 iter/s); OMP=2 184.83 s (2.7 iter/s); OMP=4 191.87 s (2.6 iter/s). **Parallel makes it slower, not faster.** Root cause is now confirmed: not malloc contention but **fundamental work-balance ceiling on rodent**. With only 2 partitions × few subgroups = 4 work items, of which neo dominates ~50%, theoretical max speedup is 2×. OMP region setup/teardown overhead (called ~6×/chain×iter × nChains × 500 iter = many thousand parallel regions) eats that ceiling. **Phase 1b is infrastructure for bigger workloads** (larger trees with more partitions, datasets with more kPrime diversity, future per-thread workspaces extending to other call sites) — it doesn't unlock rodent. **Phase 2 (full threaded chain dispatch) is the only remaining path** to ≥2× on rodent because it parallelises at the CHAIN level (4 chains × ~equal work each) instead of the partition level. Phase 2 brief at `dev/profiling/t017-ii-design.md`. Projected: 3-3.5× wall at nChains=4 (4-way chain parallel, bounded by serial swap + sample collection). **Implication for production.** Phase 1 (this finding) lands the OpenMP foundation and bit-identity correctness mechanics. It does NOT improve Hamilton walltime. Until Phase 1b lands, aware nChains=4 PT remains 184 s / 500 iter on rodent. **Tests:** 233 / 233 pass. New test `test-omp-determinism.R` (5 assertions: same-seed OMP=1 vs =4 bit-identical; same-seed two repeats bit-identical; different seed differs). **Files touched.** `src/Makevars` (new, 6 LoC), `src/Makevars.win` (new, 11 LoC), `.gitignore` (1 line removed, exclusion lifted), `src/mcmc.cpp` (+28 / −12 const-ref signatures), `src/mcmc_ecology.cpp` (+829 / −169 — partition refactor + flat work-item + parallel pragma + diag), `tests/testthat/test-omp-determinism.R` (new, 178 LoC), `dev/profiling/t017-design.md` (new design doc, 263 LoC). **Net diff: ~950 LoC** (above the 500-LoC budget estimate; impl agent went over). **Cleanup.** Three dead agent worktrees removed (Round 13 + Round 14a + Round 14b). Salvage script `dev/t017_discriminator.R` left in tree (small, documents partition + subgroup count for rodent). Commit: <see git log>. |
| T-018 | P2 | **APPLIED (2026-05-21), VERIFIED unit-tested (13 / 13 new tests pass); bench null on rodent 500-iter (decay never fires under default thresholds + short warmup)** | [Optimise] | Acceptance-aware adaptive move decay during warmup (R-side, env-gated diagnostics) | **Origin:** at the end of T-015's "what can we still do for aware?" conversation, user picked options 1 (threaded PT) and 2 (adaptive moves) to start. Two background subagents dispatched in worktrees; the Sonnet T-018 agent made high-quality R-side progress (120 LoC + tests) but died silently before committing. Salvaged from `mkp/.claude/worktrees/agent-a0b72a05663949b39/` after harness reaped the task entry. **What landed.** `.DecayLowAcceptMoves()` in `R/RunMkPrime.R:4209` — multiplicative decay (default ×0.7) of weights for free (non-pinned) moves whose **batch-level** acceptance rate (cold chain) falls below `accept_floor` (default 2 %) provided ≥ `n_min` (default 30) proposals were made in the batch. Floor at `weight_floor * initialWeights[m]` (default 0.1 × initial) prevents any move from being driven to zero. Weights re-normalised to preserve the unpinned budget after decay; pinned weights re-enforced. **Wired inside the warmup loop only** at `R/RunMkPrime.R:1098-1107` (after `.AdaptMoveWeights()`, before the M-171 Gibbs k′ frequency reduction), so adaptation is automatically frozen at the warmup-to-sampling transition by the existing phase guard. **MCMC theory.** Adaptation during warmup only preserves the chain's stationary distribution; sampling-phase weights are constant. **Diagnostic.** Per-move before/after lines emitted via `message()` to stderr when `MKPRIME_ADAPT_DIAG=1`. **Verified** (`tests/testthat/test-t018-adaptive-moves.R`, 13 unit tests): same-length return, sums to 1, decays moves below floor, respects per-move floor, preserves pinned moves, handles edge cases (all-rejected, all-accepted, zero proposals, single move). 241 / 241 tests pass overall (228 baseline + 13 new T-018), 7 expected skips (1 new — the slow MCMC integration test for warmup→sampling weight freeze is `MKPRIME_SLOW_TESTS=true` gated). **Bench null on rodent 500-iter.** `dev/profiling/drivers/rodent_aware_timing.R MKP_TIMING_NCHAINS=1`: aware 45.76 s (10.9 iter/s) vs T-015 baseline 43.56 s (11.5 iter/s) — within bench noise (±5 %). **`[T-018 decay]` diagnostic did not fire**. Reason: on rodent at warmup=100-200 iter with 28 moves, low-weight moves (e.g. slice_rate_loss at ~1.7 %) only get ~3-5 proposals in warmup — well below `n_min=30`. High-weight moves (NNI, local_dirichlet, dirichlet_branch) get ≥ 30 proposals but their acceptance is above 2 %. Decay never triggers under this regime. **When this WILL help.** Long aware warmups (≥ 2000 iter) where pathological move patterns can develop, or runs where slice moves dominate with low acceptance (e.g. tightly-curved posteriors near boundaries). For rodent production: probably no measurable wall recovery; for Hamilton 30 k iter warmups on harder datasets: could matter. **What's NOT done.** Per-chain weight matrices (PT chains all share the same weights currently); per-chain adaptation could be a follow-up if PT chains diverge in acceptance pattern. **Cleanup.** Salvaged dev/t018_*.R scratch scripts NOT copied (throwaway). The T-018-related .gitignore changes from before this session were left as-is (not committed). Commit: <see git log>. |
| T-015 | P1 | **REFUTED-AS-PHRASED (2026-05-21), VERIFIED per-chain-move time grows ≤12 % from nChains=1 to nChains=4 in BOTH blind and aware modes — no aware-specific PT locality cost** | [Investigation] | Per-chain locality cost in aware parallel tempering — T-014 follow-up | **Origin:** filed as a candidate at the end of T-014 to investigate the "remaining ~75 % of aware-PT amplification" not captured by the drift-resync gate. The brief in `dev/profiling/findings.md` T-014 attributed it to per-chain `zMatrix` / `wEdge` / `gammaE` arrays "defeating inter-chain cache locality" and proposed a "shared arena" refactor (estimated week+ effort). **Method.** Advisor pre-consult flagged a math-framing issue: T-014's "1.89× excess amplification" = (aware 4.24× outer-iter slowdown) / (blind 2.24× outer-iter slowdown) treats blind's super-linear scaling as the no-amplification baseline. **Linear scaling under PT is 4× per outer iter**, not blind's 2.24×. Blind's 2.24× already contains a cache-sharing bonus; aware can't be expected to match it unless aware is also memory-bound (it isn't — T-007 per-call analysis shows aware does ~4× the EXP per edge per cat). Hypothesis: per-chain-move time should be flat across nChains if there's no real PT-specific cost. **Instrumentation.** Env-gated (`MKPRIME_T015_DIAG=1`) chrono accumulators inside the PT loop at `src/mcmc.cpp:6121-6249`: per-chain move-dispatch timer, per-chain drift-resync timer, single swap timer at `:6243`. Emitted to stderr per batch. Inert when env unset. **Verified** (`dev/profiling/drivers/rodent_aware_timing.R` rodent 60×217, kEco=4, 500 iter, `MKP_TIMING_NCHAINS ∈ {1, 2, 4}`): per-chain-move mean μs averaged across chains and both batches — **Blind: 1ch 1475, 2ch 1563, 4ch 1659 (+12.5 % at 4ch)**; **Aware: 1ch 82700, 2ch 91300, 4ch 90600 (+9.5 % at 4ch)**. The small uniform 10-12 % growth from 1→4 chains affects blind and aware **symmetrically** and is not aware-specific (so T-015 as phrased — *aware-specific PT-locality cost* — is refuted; it isn't zero, but it's not what the brief was hunting for). **Caveat: move-schedule confound.** Per-chain weight adaptation produced different move mixes across the 1ch/2ch/4ch runs (e.g. blind 1ch nni:11.7 % vs blind 4ch nni:4.7 %; aware 1ch nni:15.9 % vs aware 4ch nni:1.1 %), so per-chain-move μs is averaging over different work distributions. The 12.5 % vs 9.5 % gap is within this confound; what matters for T-015 is the *pattern* — comparable growth between modes — not the exact percentages. **Swap cost (hypothesis #3).** `swap_total_ns` = 0.000 s under both modes at all chain counts — `std::swap(McmcState)` is move-based (Rcpp SEXP refcount-swap + std::vector pointer-swap), <0.001 % of wall. **Lazy populate (hypothesis #2).** `populate_eco_cache_full` at `:4868` is gated to NNI + `MKPRIME_ECO_PARTIAL_CL=1` (default unset); ecoCL is dormant. Not applicable to default workload. **False sharing (hypothesis #4).** Single-threaded; not applicable. **What explains blind's super-linear scaling.** Fixed per-outer-iter R overhead (sample save, streamed log write, batch-boundary R callback) is ~3 ms regardless of nChains. Blind 4ch outer iter = 9.8 ms; 4 × per-move (1.66 ms) = 6.6 ms → **3.2 ms fixed overhead (33 %)**. Aware 4ch outer iter = 370 ms; 4 × per-move (90.6 ms) = 362 ms → only 8 ms fixed overhead (2 %). Multiplying chains scales move work but doesn't multiply overhead — that's where blind's super-linear bonus comes from. Aware can never reproduce this because its per-move cost is 55× blind's. **Implication for T-014.** T-014's "shared arena" proposal for the remaining 1.69× is misconceived; there is no recoverable excess. T-014's drift-gate fix is real and stands (~12 % wall at 4ch), but accounts for *all* the recoverable PT-specific cost on rodent, not 25 %. **Implication for production.** Hamilton viability unchanged: aware nChains=4 PT runs at ~linear in nChains. For 30 k iter warmup under v2 8 h walltime, nChains=1 + nRuns=4 remains the viable production option. PT amplifies walltime by ~4× as expected from 4× the work, not from a fixable locality cost. **Tests:** 228 / 228 pass (`filter="ecology|likelihood|mcmc"`). The instrumentation is bit-identity-safe (only writes to local accumulators when env-gated). **What's NOT pursued.** Orchestrator-level heap allocations in `cpp_partition_log_likelihood_ecology` (`src/mcmc_ecology.cpp:1005, 1029`) + `const_site_prob_*_eco_single` (`:854, :893, :924`) still allocate ~800 KB per call uniformly per chain. This is a **T-007 follow-up** (uniform per-chain cost), not T-015 (excess at nChains>1). Estimated 5-10 % wall if reworked with T-008-style workspace pre-allocation, but the ecology orchestrator's call frequency dropped after T-010's partition cache so practical gain may be smaller. Suggested filing as candidate **T-016** in the next /profile rotation. **Cleanup.** Diagnostic instrumentation kept in-tree behind `MKPRIME_T015_DIAG=1` (inert by default, ~30 LoC total). Re-usable for future PT-related investigations. Commit: <see git log>. |
| T-014 | P0 | **OPEN (2026-05-21), VERIFIED ~12 % wall at nChains=4 PT, ~0 % at nChains=1 (within noise)** | [Optimise] | Eco drift-resync gate at `iter % 20` fires per chain — disproportionate PT cost | `src/mcmc.cpp:6188` — the `if (data->ecologyAware && (iter % 20 == 0))` block recomputes `cpp_log_likelihood_ecology` + `cpp_log_prior` **from scratch (uncached)** for every chain, then overwrites `state->logLik` / `state->logPrior`. Diagnostic value scales with sampling frequency but the *cost* scales with `nChains` because the gate sits inside the `for (int ch = 0; ch < nChains; ++ch)` PT loop at `:6116`. **Symptom that triggered this round.** Hamilton job 17258771 (rodent MkNT aware, nChains=4 PT, nRuns=4, nCore=4) stalled with 0 samples streamed at 2h32m wall while workers held 99.5 % CPU. Local benches reproduce: aware nChains=4 PT runs 4.24× slower per outer iter than nChains=1 (10.6 → 2.5 iter/s), vs blind which slows only 2.24× (228 → 101.6 iter/s) — the **aware-only excess amplification factor** under PT is 1.89×. **Verified** (`dev/profiling/drivers/rodent_aware_timing.R MKP_TIMING_NCHAINS=4`, rodent 60×217, kEco=4, 500 iter): baseline `iter % 20` → aware nChains=4 **196.39 s** (2.5 iter/s); patched `iter % 200` → aware nChains=4 **177.66 s** (2.8 iter/s). **Δ = 10.5 % wall, ratio 4.24× → 3.78×** vs blind. Same patch at nChains=1: 10.6 → 10.4 iter/s (within noise) — confirms the cost scales with `nChains`. **What this leaves on the table.** Cutting drift-gate frequency by 10× removes only ~25 % of the excess PT amplification (1.89× → 1.69×). The remaining ~1.69× is per-chain state locality: each `McmcState` holds its own `zMatrix` / `wEdge` / `gammaE` arrays, so the aware ecology pruner can't reuse hot cache lines between chains the way blind's shared partition arithmetic does. Recovering this would need a refactor of `state->ecoCL` / `EcoCLCache` to share a single contiguous arena across chains with per-chain views — a major undertaking (estimated week+) and out of scope for the rotation. **UPDATE 2026-05-21 (T-015):** this "remaining 1.69×" framing was REFUTED by per-chain chrono instrumentation — per-chain-move time grows only ~10 % from nChains=1 to 4 in BOTH blind and aware modes (no aware-specific locality cost). The "1.89× excess" is a math artefact of comparing blind's super-linear scaling (driven by fixed R overhead) to aware's near-linear scaling. **See T-015 row** for the data + reframing. Do not pursue the shared-arena refactor — no recoverable excess exists. **Proposed landable fix (small, safe).** Move the modulus into an env var: `MKPRIME_ECO_RESYNC_EVERY=200` (default 200). At `200`, T-011's 5000-iter drift test still detected 0 events — the gate's correction role is hypothetical on the cached path. At nChains=4 PT this saves ~12 % wall; at nChains=1 it's a no-op. Set lower (20 or even 1) when debugging a new partial-CL path. **Hamilton implication for the current question.** Even with this fix, aware nChains=4 PT on Hamilton (~4× slower than my local CPU) projects 0.7 iter/s — 30 k iter warmup = 12 h, the v2 walltime limit. The drift gate fix doesn't make PT viable on rodent at 100 k iter; it merely makes 10 k iter feasible (~4 h). **Decision the user faces.** (a) Accept aware nChains=1 + nRuns=4 (no PT, but four independent starts give Rhat) — ~3 h on Hamilton, viable today. (b) Accept this 12 % drift-gate fix + nChains=4 PT + nIter ~ 10 k — feasibility ping, mixing improved but tight on iter budget. (c) Invest in shared-arena ecology cache (T-015 candidate) for the full ~1.7× PT amplification win — major refactor. **Tests:** the bench patch was reverted; no tests broken. Driver: `dev/profiling/drivers/rodent_aware_timing.R` with `MKP_TIMING_NCHAINS={1,4}`. **Cleanup:** local `.vtune-lib-*` dirs untouched (didn't need VTune for this round — code inspection of the PT loop + targeted modulus toggle was sufficient). |
| T-007 | P1 | **PARTIALLY-OPTIMISED-AT-INFRASTRUCTURE-LIMIT (2026-05-21), VERIFIED ~17 % wall recovery (24.59 → 20.58 s on rodent 200 iter) via T-008/T-009/T-010 partition-cache stack; T-011/T-012/T-013 partial-CL infrastructure landed but REFUTED on rodent-sized trees** | [Optimise] | Ecology-aware path bypasses all likelihood caches — every move recomputes full likelihood. **DO NOT re-spawn this expecting 5–10× wall.** The original headline estimate underweighted the dirty-fraction problem: on a 64-tip tree the eco pruner's `wEdge` couples every edge globally, so after any tree move marginals propagate to ~91 % of internal nodes (`12c_rodent_dirty_fraction.R`). The 78 % tree-move weight therefore can't be cut by partial-CL on small trees. Remaining tractable avenues are individually small (5–15 % each): pattern compression in the eco pruner (mirror M-172), compile-time-K specialisation (mirror T-005), factor-table hoisting in Gibbs sweeps, larger-tree partial-CL empirical (T-013 dirty fraction may shrink as ~log(nTip) / nTip on bigger inputs). The 5× headline target **requires** either an algorithmic redesign of the wEdge dependency (approximate non-propagating marginals) or a pruner restructure for per-character CL caching — both multi-week refactors out of scope for the /profile rotation. | **Headline diagnosis (2026-05-21).** Reported gap: rodent blind ~19 min vs aware still warming at 80+ min on Hamilton (rodent MkNT v1, job 17254915 vs 17254914). Reproduced locally on the same matrix (217 chars, 64 tips, kEcology=4) at 200 iter: blind 0.97 s (206 iter/s), aware 24.59 s (8.13 iter/s) — **25.3× wall-time ratio**. Rprof confirms 97.20 % of aware time inside `.Call → run_mcmc_batch_cpp` (R-side overhead negligible). **Per-call orchestrator cost (isolated, dev/profiling/drivers/11c_per_call_cost.R, 30 reps): aware `cpp_log_likelihood_ecology` = 0.67 ms; blind `cpp_log_likelihood` = 0.33 ms — only 2.0× per call.** The remaining ~12× of the wall gap is **excess call frequency**, not slow calls: the aware path bypasses every caching mechanism the blind path uses. Locations: `src/mcmc.cpp:3005` (slice_target), `:3094` (slice_accept), `:3848` (gibbs_kprime_sweep recompute, comment explicitly notes "partition cache is unused"), `:3944` (block_kprime_shift), `:4105` (gibbs_z_sweep recompute), `:4632` (scale_pi0), `:4681` (scale_theta) all call `cpp_log_likelihood_ecology` from scratch and force-clear `state->partLogLik` (lines 3852, 3102). `nodeCL` (partial-CL invalidation for SPR — M-158/M-161) is never consulted in eco mode; `ClWorkspace` (workspace pre-allocation — M-162B) is never threaded through. By contrast the blind path reuses cached `partLogLik` for moves that don't touch all partitions (e.g. rate_loss/rate_neo only invalidate neomorphic partitions; tree moves use partial CL). **Predicted recovery: 5–10× wall** once a partition cache is restored for non-tree moves (eco-orchestrator can still recompute on tree moves, but kPrime, phi, pi0, theta, rate_*, branch_length moves should hit a cache for partitions they don't touch). **Three ranked sub-targets follow (T-008, T-009, T-010).** **Caveats** (for honest delta accounting later): bench used `rateLogSd = 0` (1 ACRV cat); production cost scales linearly with `nCat`. Local 25× vs Hamilton ~4×+ ratio is partly warmup-adaptation acting differently at long iter counts. |
| T-008 | P1 | **APPLIED (per_char portion) (2026-05-21), VERIFIED ~4 % on per-char batch; orchestrator portion open (T-010/T-008-orchestrator)** | [Optimise] | Per-call heap allocations in eco orchestrator and per-character helper | `src/mcmc_ecology.cpp` — `per_char_log_lik_ecology` (the per-character helper used by Gibbs z sweep) previously allocated 4 per-call heap objects: `IntegerMatrix tipStates(nTip,1)`, `IntegerMatrix zPart(1,zCols)`, `std::vector<double> buf`, `std::vector<uint8_t> initFlg`. On the rodent matrix (217 chars, kEco=4) this amounted to **7812 heap allocations per Gibbs z sweep** (217 × 3 × 3 = 1953 calls × 4). **Fix (2026-05-21, T-008 per_char portion):** added `GibbsZWorkspace` struct (`src/gibbs_z_workspace.h`) with `buf`, `initFlg`, `tipStates` (IntegerMatrix nTip×1), `zPart` (IntegerMatrix 1×maxZCols) and an `ensure(maxNode, stride, nTip, zCols)` resize-only-if-needed method. `per_char_log_lik_ecology` signature extended with `GibbsZWorkspace&`; `gibbs_z_sweep_impl` allocates ONE workspace (max stride across all partitions) at sweep start and passes it through all 1953 calls — **0 per-call heap allocations**. `tipStates` column copied via `std::memcpy` directly from `part.tipStates` column-major offset. **Verified:** `dev/profiling/drivers/12_per_char_alloc.R` — per-char batch (nChar=217, 200 reps): baseline 18.7 ms/batch → T-008 18.1 ms/batch (**~4 % speedup** on microbench; modest because malloc cost is small relative to pruning arithmetic). End-to-end: driver 11 aware 21.27 s (baseline) → 21.19 s (T-008) on same 200-iter rodent run — within noise. **Expected larger gain** when combined with T-009 (tip-edge fast path) and T-010 (partition cache). 375/375 tests pass. **Orchestrator portion** (`cpp_log_likelihood_ecology` allocs at lines ~968/992/1033) tracked separately by T-010/T-008-orchestrator agent. **Cross-ref:** T-002 (singleton ascertainment path same pattern). |
| T-009 | P1 | **APPLIED (2026-05-21), VERIFIED ~0 % wall delta on orchestrator; ~0 % on isolated pruner** | [Optimise] | Port T-006 tip-edge fast path to `pruning_jc_acrv_flat_ecology` | `src/mcmc_ecology.cpp` (`pruning_jc_acrv_flat_ecology`, lines 381–506 post-edit). Split the per-character propagation loop into `tipChild` (ch ≤ nTip) and internal-child branches; tip path skips sum_cl + clCh loads and uses the fast (pdMix / psMix / 1.0) write pattern. Mathematical identity verified analytically (same as T-006; JC identity holds with psMix/pdMix taking the place of p_same/p_diff). **Verification:** baseline 0.241 ms/call, T-009 0.241 ms/call (5000-rep direct `.PruningJcEcology` bench, k=4, nChar=150, 60 tips, kEco=4 — averaged over 5 outer reps each). Delta within measurement noise (~10 ms resolution on Windows). Orchestrator-level (`.MkpEcologyLogLikelihood`, 1000 reps) also shows no measurable delta. **Why near-zero gain vs the predicted 10–25 %:** the blind-path savings (T-006, 26.6 %) were large because the `persite_impl` inner loops dominate. In the ecology pruner, the dominant cost at every edge is the `for (s = 0; s < kEco; ++s)` psMix/pdMix mixture accumulation (4 iterations × `MKP_EXP` each × 3 z-values precomputed), plus heap allocations (T-008 not yet landed). The K-state inner work (sum_cl = K adds; write loop = K updates) is cheap relative to this. For kEco=4, K=4, the savings are ~K/(kEco·3) ≈ 33 % of the per-edge arithmetic but less than 5 % of per-call wall because T-008 (heap allocs) and T-007 (excess call frequency) dominate. **The change is still worth keeping:** the mathematical identity is correct, the code is structurally cleaner for the internal-child path, and future optimisations (removing heap allocs, cutting EXP call count) will expose this saving. **Tests:** 226/226 across `filter="ecology|likelihood"` (0 failures, 4 skips — slow MCMC and pilot RDS). **Bit-identity caveat:** known-state diagonal (`pdMix*1 + diff*0 = pdMix`) and missing data (`all 1.0`) are bit-exact. Known-state diagonal write (`psMix` vs `pdMix + diff_coeff*1`) may differ by 1 ULP due to FP cancellation — no test failures observed. |
| T-013 | P0 | **REFUTED-ON-RODENT (2026-05-21), VERIFIED partial-eval bit-identity (|diff|=0 across 20 NNI samples; rollback identical), drift 0 / 5000 iter, wall 0 % (within noise)** | [Optimise] | NNI partial-CL via EcoCLCache, wired into production behind env gate | **What landed:** R-callable `.CppPartialEvalEcologyNNI` wrapper (from `agent-a65b5a3c71e6128d5` salvage; the wrapper exposes `populate_eco_cache_full` → `eco_cache_partial_eval_nni` → `restore_dirty_eco_cls` so partial-eval mechanics can be exercised in isolation). Production wiring in `src/mcmc.cpp::do_move_impl`: lazy `populate_eco_cache_full` before NNI proposals, partial-eval branch as the first sub-branch of `else if (eco)` in the main MH accept block (mirrors the blind-path NNI partial-CL pattern at the same site), symmetric rollback on rejection (restore saved CLs + reverse TreeNav swap). Wiring is gated behind `MKPRIME_ECO_PARTIAL_CL=1` env var (default OFF) because empirical analysis showed it's a net-loss on the rodent matrix — see below. New drivers: `12b_partial_bitident.R` (bit-identity + rollback regression for partial-eval), `12c_rodent_dirty_fraction.R` (empirical dirty-set sizing per NNI on rodent). **Bit-identity & rollback verified:** 20 random in-place NNI swaps on a 10-tip / 6-character / kEco=3 fixture, every sample shows `|partial_eval_ll − fresh_full_eval_ll_on_new_topology| = 0` exactly and `|restored_ll_on_old_topology − fresh_full_eval_ll_on_old_topology| = 0`. **Drift verified:** 5000-iter aware MCMC with partial-eval engaged (no env gate), **0 `[eco-resync]` warnings**. **Wall (gate OFF, default):** 200-iter aware 20.76 s vs T-012 baseline 20.58 s — within bench noise. **Wall (gate ON):** 20.69 s — also within noise. **Why no wall improvement:** `12c_rodent_dirty_fraction.R` (30 random NNI swaps on the rodent topology, kEco=4, 60 tips, 59 internal nodes) measured **dirty count: mean 45.7, median 54, max 57 (fraction of nInternal: mean 0.77, max 0.97). wEdge-dirty edges: mean 89.1 / 118 = 75 %, median 109 / 118 = 92 %.** Marginals propagate too widely on a small tree — after an NNI swap, both the topology-dirty path-to-root and the wEdge-dirty edge set together cover almost every node. Naive partial-eval saving (1 − dirty/nInternal) = 7.6 % median, and that's BEFORE accounting for the dirty-set walk + save/restore overhead which on rodent appears to net out at ~0 %. Multiplied by NNI's 17 % move weight: predicted wall delta ≈ 1.3 %, well below the bench noise floor. Larger trees should help (dirty fraction shrinks because path-to-root grows logarithmically while nInternal grows linearly) — the env gate lets that be measured empirically without forcing the rodent cost on every user. **Tests:** 228 / 228 pass (`filter="ecology|likelihood|mcmc"`, 0 failures, 6 expected skips). **What's NOT done:** SPR/TBR/pSPR partial-eval (advisor predicted dirty fraction even higher than NNI, so deferred); beta_simplex (case 4) partial-eval (would need a separate dirty-walker; rodent dirty fraction analysis suggests it'd also be net-loss). **T-007 stays PARTIALLY-OPTIMISED.** The infrastructure is in place for the larger-tree case; nothing more to extract on rodent without an algorithmic redesign (e.g., approximate marginals that don't propagate, or per-character CL caching with restructured pruner — both major refactors). Commit: <see git log>. |
| T-012 | P0 | **PARTIAL-SALVAGE (2026-05-21), VERIFIED full-eval bit-identity (|diff|=0); partial-eval + production wiring UNVERIFIED** | [Optimise] | NNI partial-CL via EcoCLCache | **Origin:** background Opus subagent (id `a80bae8b738eab40c`) launched after the T-011 commit (the headline ecology-aware caching effort — see `dev/profiling/findings.md` T-011) to build out the deferred Lever 2 work. The agent process died silently after ~14 min of file edits (no transcript flushed; `TaskOutput → "no task found"`; no R/build processes in tasklist). Diff was preserved in the worktree; salvaged from here. **Scope landed:** in `src/ecology_cl_cache.h` (+513 LoC) — `eco_apply_mixture_jc/mkn`, `eco_compute_jc/mkn_factors`, `eco_full_downpass_unit`, `eco_recompute_dirty_unit`, `eco_unit_root_loglik`, `find_dirty_nni_eco`, `save_dirty_eco_cls`/`restore_dirty_eco_cls`. In `src/mcmc_ecology.cpp` (+285 LoC) — `make_sub_zmat_eco`, `populate_eco_cache_full`, `eco_cache_total_loglik`, `eco_cache_partial_eval_nni`, and an R-callable `CppLogLikelihoodEcologyCached` wrapper. New regression driver `dev/profiling/drivers/12a_bitident.R` (rewritten by the salvage step to call `prepare_mcmc_data` via the test-helper pattern; agent's original used a non-existent `mkd$dataPtr` field and would not run). **Verified:** (1) `12a_bitident.R` — legacy `.CppLogLikelihoodEcology` vs cached `.CppLogLikelihoodEcologyCached` on a 10-tip / 6-character / kEco=3 random fixture: **|diff| = 0 (exact)**, so the cache populates + traverses correctly for a full evaluation. (2) `devtools::test(filter="ecology|likelihood|mcmc")`: **228 / 228 pass**, 0 failures, 6 skips (matches T-011 baseline). (3) 200-iter rodent aware bench: **aware 20.58 s / blind 0.78 s / ratio 26.38×**, indistinguishable from T-011 baseline (20.47 s / 0.79 s / 25.91×) — as expected, since the cached path is not wired into production. **NOT verified:** `eco_cache_partial_eval_nni` correctness (no R wrapper to drive a controlled NNI swap and compare against from-scratch full-eval on the new topology); save/restore mechanics under proposal rejection; behaviour over 5000-iter drift stress with the cached path engaged. **NOT done:** wiring into `do_move_impl` at case 5 (NNI) — `src/mcmc.cpp` is untouched in this diff, so production MCMC continues to use the legacy full-eval pruner. **Next round (T-013) scope:** (a) add an R-callable partial-eval-NNI test wrapper, drive it through a controlled swap, assert bit-identity vs from-scratch full-eval; (b) wire `populate_eco_cache_full` lazily in `do_move_impl` before NNI proposals (mirror `populate_cache(state->nodeCL, …)` at `src/mcmc.cpp:4477`); (c) replace the NNI full-eval branch in the main MH eco accept block with `eco_cache_partial_eval_nni` + save/restore on rejection; (d) 5000-iter drift gate; (e) bench. Expected ~8-12 % wall improvement if (a)–(d) come out clean (advisor estimate from the T-011 round; see `dev/profiling/log.md` Round 9). **T-007 stays PARTIALLY-OPTIMISED.** Commit: <see git log>. |
| T-011 | P1 | **FOUNDATION-LANDED (2026-05-21), VERIFIED 0 % wall, 0 drift events / 5000 iter** | [Optimise] | Partial-CL invalidation skeleton for ecology tree moves | **Scope landed:** `src/ecology_cl_cache.h` (EcoCacheUnit / EcoCLCache layout, four invalidate_* methods, dirty-edge walker, scalar-param-change detector — all marked `[[maybe_unused]]` and documented as a design sketch). `state->ecoCL` field on McmcState. Granular invalidation calls wired at every site that currently invalidates `state->nodeCL` (17 sites in `src/mcmc.cpp`: tree-move accepts, slice_scalar_impl, gibbs_kprime_sweep_impl, block_kprime_shift_impl, gibbs_z_sweep_impl, case 35 scale_pi0, case 37 scale_theta, main MH accept block). **Scope NOT landed:** cache population, cached pruner consultation, M-158-style save/restore around tree-move evals. These are needed for any wall improvement and were deferred — see "why" below. **Verified** (`dev/profiling/drivers/11e_t011_bench.R`, rodent matrix 64×217, kEco=4, 200 iter): baseline (T-010 HEAD) **aware 20.38 s, blind 0.76 s, ratio 26.82×** → T-011 **aware 20.47 s, blind 0.79 s, ratio 25.91×** (Δ within bench noise, < 1 % either direction). Per-call orchestrator cost unchanged. **5000-iter aware MCMC drift stress** (`11g_t011_drift.R`): wall 473.98 s; **0 `[eco-resync]` warnings**. **Tests:** 228 / 228 pass (`filter="ecology|likelihood|mcmc"`, 0 failures, 6 skips — matches T-010 baseline). **Why no wall delta** (advisor concurred BOTH pre- and post-implementation): on the rodent move schedule the spec's "simplest" Lever 1 (invalidate-everything-on-wEdgeDirty) overlaps entirely with what T-010's partition cache already provides — every cache-eligible non-tree move (kPrime int_walk, rate_loss/rate_neo/neo_joint, phi/pi0/theta/rateLogSd, gibbs_z post-sweep) already hits T-010's cache; every wEdge-dirtying tree move (78 % of move weight) triggers `invalidate_all_cls()` and the node-CL cache reuses zero subtrees. Wall recovery requires Lever 2: per-edge wEdge / edgeLen change detection + an M-158-style save / restore around tree-move evaluations (so a rejected proposal can roll back the cached CLs without forcing a full rebuild on the next iter). For NNI specifically, the dirty subset is ~30–50 % of nodes (NNI subtree ∪ path-to-root, after wEdge marginals propagate), so an NNI-only Lever 1 + Lever 2-lite combo could deliver ~8–12 % wall at ~500 LoC of save/restore plumbing. SPR/TBR/pSPR/branch-length moves change a much larger fraction of edges and would gain little. **What's defensibly landable** without that complexity: this T-011 skeleton (cache field + invalidation + design header), zero behavioural change. The next round (T-012?) should be: NNI + branch_simplex partial-CL with save/restore via `EcoCLCache`. **T-007 stays PARTIALLY-OPTIMISED.** **Cleanup:** dead `pruning_*_ecology_cached` static helpers were removed from `src/mcmc_ecology.cpp` mid-round (they couldn't be called without the population path that wasn't landed; keeping them was code-review burden for no value). **Side-finding spawned:** `gibbs_kprime_sweep_impl` uses the blind pruner under ecology mode (no wEdge / phi / z consultation). Possible correctness issue — flagged as separate task. Commit: <see git log>. |
| T-010 | P2 | **APPLIED (2026-05-21), VERIFIED ~13 % wall (1.30× → 1.15× of T-008+T-009 baseline)** | [Optimise] | Restore partition-level cache for ecology-aware non-tree moves | **Applied 2026-05-21.** Added `cpp_partition_log_likelihood_ecology(pi, parent, child, edgeLen, kPrime, rateLoss, rateNeo, phi, zMatrix, wEdge, gammaE, rates)` in `src/mcmc_ecology.cpp:965`, bit-identical to one inner-loop iteration of the orchestrator. Refactored `cpp_log_likelihood_ecology` to call it per partition (preserving summation order). Added `compute_gamma_e_ecology` helper (O(kEco), no caching needed). Added `state->wEdge` cache + `state->wEdgeDirty` flag (already declared on `McmcState` from earlier work — now actually consulted). New helpers `eco_refresh_wedge` and `eco_recompute_all_partitions` in `src/mcmc.cpp` (~80 LoC). **`state->partLogLik` reused as ecology cache** (initialised in `fill_partition_cache`; flushed by tree-move accept paths same as the blind path). **Move-handler refactor** in `src/mcmc.cpp`: `eval_slice_target` + `slice_scalar_impl` (eco branches), `gibbs_kprime_sweep_impl`, `block_kprime_shift_impl`, `gibbs_z_sweep_impl` post-sweep recompute, `case 35 (scale_pi0)`, `case 37 (scale_theta)`, main MH eco branch (cases 1, 3, 7, 18, 34 use partition cache; tree-move cases recompute everything with fresh wEdge). `wEdgeDirty=true` set in every tree-move accept path (8 sites in tree-move sub-handlers + main MH eco accept). **Verified** (`dev/profiling/drivers/11d_t010_bench.R`, rodent matrix 64×217, kEco=4, 200 iter): baseline (HEAD, T-008+T-009 applied) **aware 24.59 s, blind 0.97 s, ratio 25.3×** → T-010 **aware 21.40 s, blind 0.77 s, ratio 27.79×** (aware ↓ 13.0 %; blind ↓ 20.6 %; ratio went UP because blind benefited more from the parallel-build state). **1000-iter drift check: zero `[eco-resync]` warnings** — cache invalidation logic is correct. **Tests:** 228/228 pass (filter="ecology|likelihood|mcmc"; +2 from T-010 regression guards in `tests/testthat/test-ecology-likelihood.R`). **Why only 13 %, not the predicted 5–10×:** production move schedule on rodent puts ~78 % weight on tree-touching moves (NNI 17 %, SPR 8.5 %, TBR 8.5 %, pSPR 8.5 %, gibbs_spr 2.9 %, gibbs_subtree_swap 2.9 %, branch_lengths 11 %, dirichlet_branch 8.3 %, local_dirichlet 8.3 %, tree_length 1.7 %) — all of which invalidate wEdge and need to recompute every partition. Only ~22 % of moves benefit from the partition cache (slice_scalar on rate_loss/rate_neo → neo partitions only; phi/pi0/theta → wEdge-cached; kPrime → trans-partitions-only; z sweep → cached). Within those ~22 %, the cache is doing its job. **Next bigger win** requires either (a) partial-CL for ecology tree moves (much larger refactor) or (b) cheaper wEdge update for branch-length moves (deferred). **Caveats:** kPrime subgroup composition for one trans char changes whenever that char's kPrime crosses a subgroup boundary — easiest correct option is to recompute the whole trans partition (documented as a TODO in the new function). Commit: <see git log>. |
| T-006 | P0 | **APPLIED (2026-05-19), VERIFIED 26.6 % on top of T-005 (30.5 % vs baseline)** | [Optimise] | Tip-edge fast path in `persite_impl` exploits one-hot child CL | `src/mcmc_likelihood.cpp` (`persite_impl` template — `tipChild` branch before the internal-child propagation). **Insight:** when `ch <= nTip`, the child CL for each character is one-hot for the observed state (`clCh[s]=1`, rest 0) or uniform-1 for missing data. `sum_cl` is therefore 1 (known) or K (missing) by construction — no need to load and sum the K clCh entries. The per-state update collapses: known → K writes of `p_diff` + 1 overwrite of `p_same`; missing init → K writes of 1.0 (the JC marginalisation identity `K·p_diff + (p_same − p_diff) = 1`); missing multiply → no-op (×1). **Verified by micro-bench** (5 runs each, Sun2018 EG production-weight workload, 800 iter): T-005-only median **16.45 s** → T-005+T-006 median **12.07 s** (mean 12.07, sd 0.084). **Δ = 26.6 % on top of T-005, 30.5 % vs baseline** (baseline median 17.39 s). Welch t-test t = 35.4 on 5.7 df, p < 10⁻⁶. **VTune confirms** `persite_impl<0>` (runtime K > 24) 6.802 → 4.413 s; sum of all persite specialisations ~7-8 s vs baseline 15.088 s — **~50 % reduction in CL pruning self-CPU**. **Why so much:** ~50 % of edges in a 54-tip tree are tip-incident; for each, K loads of clCh + K-1 sum_cl adds + K multiplies eliminated per character per rate category. The savings compound multiplicatively with K, so the Gibbs k′ sweep (K up to 50) sees the biggest absolute gain. **Tests:** 234/234 across `filter="likelihood|gibbs|ascertain|mcmc-engine"`. Mathematical identity verified analytically (above) and empirically (zero test failures including Gibbs k′ posterior comparisons). **Maintenance cost:** ~50 LoC; one mathematical insight; no duplicate code path (T-005 refactor ensured single shared body across compile-time-K and runtime-K). |
| T-005 | P0 | **APPLIED + REFACTORED (2026-05-19), VERIFIED 5.4 %** | [Optimise] | Compile-time-K dispatch on `pruning_jc_acrv_persite` (single-body template) | `src/mcmc_likelihood.cpp` (new `persite_kfixed<K>` template + `persite_generic` fallback, switch-dispatch in `pruning_jc_acrv_persite`). Reasoning: the original function had `kStates` as a runtime parameter, forcing the inner per-state loops (`for j<kStates`, `for i<kStates`) to be runtime-bounded. K is small (2–24 in practice; K_MAX_CAND=50 is the absolute cap from `src/mcmc.cpp:3322`), so a switch on K dispatching to a compile-time-K template lets the compiler fully unroll the inner loops and emit straight-line per-state FMAs. Specialised K = 2..24; K > 24 falls into `persite_generic` (bit-identical to the pre-T-005 code). Inner loops carry `#pragma GCC unroll 8` hints; pointers marked `__restrict__`. **Verified by micro-bench** (driver `dev/profiling/drivers/01_bench_t003.R`, Sun2018 54×225 EG production-weight workload, 800 iter, 5 runs each): baseline median **17.39 s** (mean 17.47, sd 0.28); T-005 median **16.75 s** (mean 16.70, sd 0.20). **Δ = 4.4 % wall-time speedup, p < 0.01 (Welch t-test, t=4.3 on 7.4 df).** Tighter variance under T-005 too. **VTune verification:** `persite_generic` self-time **15.088 s → 7.113 s** in MkPrime.dll (Sun2018 EG hotspot collection, software sampling); difference absorbed by `persite_kfixed<2..24>` (combined ~4.5 s of self CPU, dominant in K∈{19..24}). Tests pass (53 / 53 across `filter="likelihood|gibbs-kprime"`, including the `test-gibbs.R` suite that exercises Gibbs k′ sweep). **Why only 4.4 %, not the predicted T-003 75 %:** the remaining `persite_generic` 7.113 s is K > 24 candidate-k values that the EG prior's heavy tail still puts non-trivial weight on. Extending the specialisation higher is straightforward (add `DISPATCH_K(25)` … in `src/mcmc_likelihood.cpp:pruning_jc_acrv_persite`) at the cost of binary bloat (~100 LoC of generated code per added K). Diminishing returns expected since per-K hotspot share decreases beyond K=24. **Further wins available** (deferred): (a) algorithmic — reduce candidate-k count via tighter early-termination criteria in `gibbs_kprime_sweep_impl`; (b) algorithmic — partial-CL across consecutive candidate-k values within one sweep step (major refactor); (c) explicit AVX2 vectorisation of the inner K loop for high K. The 4.4 % gain confirms the templating approach is correct but the next big jump needs algorithmic work, not codegen. |
| T-003 | P0 | **PARTIALLY-OPTIMISED (2026-05-19)** | — | [Optimise] `pruning_jc_acrv_persite` is the dominant hot path under EG production workload — 75 % of CPU | `src/mcmc_likelihood.cpp:459-553`, called from `src/mcmc.cpp:3543` and `:3566` (Gibbs k′ sweep, M-155+M-172). **Verified by VTune** (2026-05-19, `-collect hotspots -knob sampling-mode=sw`, result `dev/profiling/result_01_felsenstein_20260519`): 15.088 s self CPU out of ~20 s total in MkPrime.dll on Sun2018 (54×225, all transformational, EG prior, production move weights). All other MkPrime functions < 1.6 % each; `pruning_jc_acrv_flat` only 0.313 s = 1.5 % (the non-Gibbs likelihood path is no longer the bottleneck after M-162B). `mkp::fast_neg_exp` at 5 inlining sites totals 0.874 s (4.4 %) — the `MKP_EXP` calls inside the loop. Per-call cost ≈ 63 ms for ~236 Gibbs k′ sweep steps over 800 iter — much higher than arithmetic O(nEdge × nChar × kStates × nCat) predicts (≈ 5 ms), suggesting memory-access ceiling rather than compute. **Next step**: run `vtune -collect memory-access` to confirm cache miss / bandwidth dominance before picking an optimisation strategy. If memory-bound, candidates include: (a) SOA-vs-AOS layout of `buf` so the inner `i` loop reads contiguous memory across kStates instead of strided per-character access; (b) chunked-character iteration to keep `clCh[c, :]` and `clPar[c, :]` in L1 cache across the per-edge sum + update pair; (c) explicit AVX2 vectorisation of the inner-most kStates loop (currently k=2–7 — may not auto-vectorise). |
| T-004 | P2 | **REFUTED (2026-05-19)** | — | [Optimise candidate, small] Cache per-edge JC transition coefficients across Gibbs k′ sweep candidate-k calls | `src/mcmc_likelihood.cpp:507-516` (`pruning_jc_acrv_persite`) — original premise was that `(p_same, p_diff, diff_coeff)` could be cached across candidate-k calls in a Gibbs sweep. **REFUTED**: `kStates` (the function parameter `k`) is what *varies* across candidate-k calls; the JC closed-form `exp_term = exp(-k·t / (k−1))` depends on `kStates` so the coefficients are different per call. No cacheable redundancy exists in this code path. The original premise mistook the constant `acrvRates` and `edge_length` for the full set of inputs; the partition's k value also enters. Closing as REFUTED rather than implementing a no-op. |
| T-001 | P0 | OPEN | — | [Baseline-refresh] PROFILING.md and `.vtune-lib/` are 6 weeks stale; current baseline ~330× slower than reference | `benchmark/PROFILING.md` (last entry 2026-03-28: 2611 iter/s) vs Driver 01 (2026-05-18: ~8 iter/s on the same Sun2018 dataset). Between the two snapshots: EG/BG priors landed (9332a54, 232f97f), Gibbs k′ sweep + block k′ shift (e8819a1, 224bda8), partial CL for SPR (be2f67b), per-partition CL cache (ee24fbb), pre-allocated workspace (f9bfa14), fast_exp (5934f7f), pSPR (7aecb8c), local Dirichlet (050299b), batch ascertainment (a9c9c5f), and the M-145/M-143 cache-invalidation fixes. The slowdown is **not** caused by these commits per se — most are wins on their own — but by the production default move schedule promoting Gibbs k′ to 29.5 % weight and adding Gibbs SPR + subtree-swap at 1.3 % each. The 2611 iter/s figure measured a schedule that explicitly excluded these. **Action:** before any new [Optimise] decision, run a fresh VTune (Windows) or `perf` (Hamilton) collection with the production move weights; current PROFILING.md priority rankings (OPP-1..OPP-9) are based on the obsolete move mix and cannot be trusted to predict where wall-time goes today. Rebuild `.vtune-lib/` against current HEAD first (the existing one is 6 weeks behind src/). |
| T-002 | P1 | OPEN | — | [Optimise candidate] `singleton_site_prob_jc` allocates per-call heap buffers even when ClWorkspace is available | `src/ascertainment.cpp:132-133` (`std::vector<double> cl_flat((maxNode+1)*stride, 0.0); std::vector<uint8_t> cl_init(maxNode+1, 0u);`). `stride = nTip × kStates`; for Sun2018 (54 tips, k=2-5), this is up to ~270 doubles × ~109 nodes = ~235 KB per call. Called from `cpp_partition_log_likelihood` at `src/mcmc_likelihood.cpp:1858` under `coding == 2` ("informative"). `pruning_jc_acrv_flat` already accepts `preAscBuf`/`preAscInit`/`preSiteLikSum` from `ClWorkspace`; the singleton path was not refactored to do the same. **Affected scope:** `coding="informative"` only (default `coding="variable"` is unaffected). **Expected gain:** unverified without VTune, but proportional to call frequency × allocation cost (likely <2 % of wall under variable, potentially material under informative). **Cross-ref:** LIKE-001 (HIGH, red-team round 7) shows the informative-coding singleton path is functionally broken in partial-CL contexts; any fix there should thread ws through to also eliminate this allocation. **Verify before filing as a real [Optimise]:** isolated micro-bench timing the same call with internal-alloc vs ws-supplied buffers across realistic call frequency. |
