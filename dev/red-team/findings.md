# Red-team findings — mkp

Non-trivial bugs / perf issues filed by red-team rounds. Trivial fixes are applied inline and not recorded here.

| ID | severity | status | type | title | location & detail |
|----|----------|--------|------|-------|-------------------|
| EG-001 | HIGH | REFUTED-AS-CAUSE | Bug | Missing per-character truncation normaliser in EG prior on p | `R/MkPrimeModel.R:489–497` and `src/mcmc.cpp:269–315` — EG normalised over full support k'∈{2,3,…}, but `LogPrior` enforces k'≥kObs_i without dividing by `Z_i(p) = Σ_{k≥kObs_i} P(k\|p)`. log Z reaches −4.7 nats at kObs=8, p=0.7; tens-of-nats systematic bias in p marginal for a 50-char dataset. **Empirical test (2026-05-16, prior-validation campaign, 26 trees × rep_01):** The geo prior (no truncation bug) produces u_post mean=0.14, median=0.11, p(u_post<0.5)=81%, bias=−1.11 — i.e. u_post is *more* anchored toward 0 under geo, not less. EG arm same 26 tasks: mean=1.31, median=1.16, p(u_post<0.5)=22%, bias=+0.07. Per-task Spearman(u_post, u_true) ≈ −0.11 under BOTH priors. The truncation bug is real as a code defect, but is NOT the cause of the u_post anchor; both arms show u_post tracks the prior expectation rather than u_true. See `dev/pilots/2026-05-12-prior-validation/analysis/eg001_upost_compare.{R,rds,png}`. Reframe: u is just not identified by 50-char data — posterior follows prior shape. |
| EG-002 | MED | OPEN | Bug | EG prior on k' ignores per-character kObs evidence in the empirical component | `R/MkPrimeModel.R:348–377` — `.LogPriorEmpiricalGeometric` sums j from 2 to m for every character regardless of kObs_i. P_emp is mass-concentrated at j=2 (0.67), j=3 (0.21); most prior mass on k' comes from tiny-N_obs decompositions. Prior mode of k' is at smallest allowed k' = kObs_i for every character → u=0 mode for every character. Architectural sibling of EG-001. |
| EG-003 | HIGH | CONFIRMED | Bug | Prior shape strongly determines u_post, regardless of kObs or u_true | `R/MkPrimeModel.R:316–377` + `data/empiricalNObs.rda` — E[u\|k'≥kObs] in 1.18–2.86 across p∈{0.3,0.5}, kObs∈{2,3,5,7}; P(u=0) = 0.21–0.47 a priori. **Empirical confirmation (2026-05-16):** geo arm (26 trees × rep_01, p_post≈0.86) gives u_post mean=0.14 — matching its steep prior E[u\|truncation] ≈ p/(1−p) − E[shift] ≈ small; EG arm gives u_post mean=1.31 — matching its empirical body decay 0.67, 0.21, 0.067. Spearman(u_post, u_true) ≈ −0.1 in BOTH arms → u is unidentified by 50-char likelihoods; posterior shape is dominated by the prior in both cases. Severity upgraded MED → HIGH: this is the dominant scientific issue with current u_post estimates, not a side concern. The user-observed u-anchor is *expected behaviour* of the prior, full stop. See `dev/pilots/2026-05-12-prior-validation/analysis/eg001_upost_compare.{R,rds,png}`. |
| EG-004 | LOW | OPEN | Bug | `LogPrior` crashes with NA propagation when kPrime or kObs has NA | `R/MkPrimeModel.R:406` — `if (any(state$kPrime[transIdx] < mkd$kObs[transIdx])) return(-Inf)` throws on NA. Fix: `isTRUE(...)` or `\|\| anyNA(...)`. |
| EG-005 | LOW | OPEN | Bug | MkPrimeEmpiricalPrior allows zero-mass gap between body and tail | `R/data.R:67–115` — When `tail_start_k > length(body)+2L` and `tail_decay>0`, k values in the gap return -Inf → kills chain. Reject mismatched tail_start_k or interpolate. |
| EG-006 | LOW | OPEN | Perf | `empBodyLastK` passed R→C++ but unused in prior calc | `src/mcmc.cpp:269–315` + `src/mcmc_likelihood.cpp:2142` — Dead parameter. |
| EG-007 | LOW | OPEN | Bug | R fallback `logit_scale_p` kernel disagrees with comment (Uniform, not Normal) | `R/RunMkPrime.R:3176-3188` — Comment says "normal step on the logit scale" but uses `sigma * (runif(1) - 0.5)`. C++ uses Bactrian. Production unaffected; misleading for any R-vs-C++ check. |
| EG-008 | LOW | OPEN | Perf | Acceptance target 0.35 below 1D-RWMH optimum (~0.44) | `R/RunMkPrime.R:3713` — Slightly large step, minor per-iter ESS hit, not correctness. |
| EG-009 | LOW | OPEN | Bug | Dead `mh_p` plumbing still in `MkPrimeMCMC()` and tuning tables | `R/MkPrimeMCMC.R:504-506`, `R/RunMkPrime.R:3713,3741` — `scale_p`, target `mh_p=0.35`, `tuningKeys["mh_p"]` all still wired but no prior branch inserts move 8. User setting `mcmc=MkPrimeMCMC(moveWeights=c(mh_p=0.1))` passes validation but does nothing. |
| PAR-001 | HIGH | FIXED (1fd87f9) | Bug | `.BuildResult` crashes when parallel run ends early with unlaunched pool slots | `R/RunMkPrime.R:2096` (PR #1 / wonderful-tereshkova-08a8b5) — when `nRuns > nCore` and the polling loop breaks via `cancelled` / `max_time` before all runs are launched, `completedRuns[run]` for unlaunched slots is the initial state from `.InitRun`, which has no `flush_idx` / `saved_idx` / `tree_saved_idx` / `samples` fields. `.BuildResult` does `if (isStreaming && runs[[run]]$flush_idx > 0L)` → `if (NULL > 0L)` → `if (logical(0))` → error "missing value where TRUE/FALSE needed". Confirmed via reproducer (`maxTime=2`, `nRuns=6`, `nCore=2`). Fix: in `.RunParallelRuns` result-collection (L1803-1809), for unlaunched runs populate minimum fields (`flush_idx=0L`, `saved_idx=0L`, `tree_saved_idx=0L`, `tree_samples=list()`) or skip them entirely (shrink `logFilePaths` and the result list). The very scenario `maxTime` exists for — bounding a long parallel run — now crashes instead of returning partial results. |
| PAR-002 | MED | DOCUMENTED (1fd87f9) | Design | Interrupt during parallel run leaves checkpoint stuck at iter 0 | `R/RunMkPrime.R:336-347, 415-456` (PR #1) — `shared$actualIter` is the interrupt handler's source of truth, but parallel workers (separate `callr` processes) can't write to it. On Ctrl-C the handler sees `shared$actualIter > 0L` as FALSE and skips the checkpoint save. The pre-existing 0-iter checkpoint from L344 is all that survives. `ResumeMkPrime` then restarts from scratch. **Not a regression** (deleted `future` code had identical behaviour; comment at L351 acknowledges it). `MkPrimeRecover()` reads log files for inspection but not resume. Fix would have the orchestrator periodically persist a best-effort checkpoint derived from log row counts. |
| PAR-003 | LOW | FIXED (1fd87f9) | Design | `$wait()` after cancel can block indefinitely if a worker is mid-long-batch | `R/RunMkPrime.R:1806` (PR #1) — after cancel files are written and the polling loop breaks, the orchestrator calls `procs[[run]]$wait()` (timeout = -1 by default). Workers only check the cancel file at batch boundaries. A long-running batch hangs the parent until the next boundary; escape requires a second Ctrl-C to trip `on.exit` kill at L1646. Not a regression (`future::value()` had same semantics). Mitigation: `p$wait(timeout = 30000)` followed by `p$kill()` if still alive. |
| PAR-004 | LOW | FIXED (1fd87f9) | Test | Pool-dispatch test does not assert pool concurrency | `tests/testthat/test-parallel.R` "pool dispatch: nRuns > nCore launches in waves" (PR #1) — asserts all 4 runs produce rows but never checks that ≤ `poolSize` workers are alive at once or that runs 3/4 launched after 1/2 finished. A regression to "launch all simultaneously" would silently pass. Suggested: capture timestamps in workers (write to a side file) and assert run-3's launch time > run-1's finish time. Also: no interrupt test and no maxTime-during-pool test (the latter would have caught PAR-001). |
| PAR-005 | LOW | FIXED (1fd87f9) | Polish | Redundant `MkPrime:::` in worker body | `R/RunMkPrime.R:1658` (PR #1) — `MkPrime:::.RunMkPrimeSingleRun(...)` is unnecessary; the func's environment (via `package = TRUE` and namespace deserialisation) is the MkPrime namespace, so unqualified lookup works. Risk: `R CMD check` NOTE about own-package `:::`. Trivial fix: drop the qualifier. |
| PAR-006 | MED | FIXED (01ebca8) | Bug | `.BuildResult` crashes when all parallel runs killed | `R/RunMkPrime.R:2173` (PR #1, round 5b verification) — when PAR-003's 30s wait-timeout fires for every worker, `.RunParallelRuns` returns `runs = list()` and `.BuildResult` then hits `runs[[1]]$chain_tuning[[1]]` → `subscript out of bounds`. Reachable when a user's MCMC batch boundary exceeds 30s on a large problem. PAR-001's shrink-list fix didn't add an empty-case guard. Repro: `MkPrime:::.BuildResult(runs = list(), ...)` crashes immediately. Suggested fix: empty-list guard in `.BuildResult` returning a minimal MkPosterior + `cli_warn`, OR guard in `.RunParallelRuns` returning a sentinel value the caller intercepts. |
| PAR-007 | LOW | FIXED (01ebca8) | UX | `cli_alert_warning` ignores bullet items | `R/RunMkPrime.R:463-486, 2561-2567` (PR #1, round 5b verification) — `cli::cli_alert_warning(c(msg, "!" = ..., "i" = ...))` concatenates bullets onto the lead line instead of rendering each with its prefix. Verified via minimal repro. `cli::cli_warn` or `cli::cli_bullets` produce the intended layout. Pre-existing pattern in interrupt-handler branches; PAR-002 added a fourth broken instance. User loses critical guidance about `ResumeMkPrime`/`MkPrimeRecover` behaviour during interrupt. Best fixed in one pass across all 4 sites. |
| PAR-008 | LOW | FIXED (dae4652) | Design | Silent worker loss after PAR-003 kill | `R/RunMkPrime.R:1830` (PR #1, round 5b verification) — `tryCatch($get_result(), error = function(e) NULL)` makes a hard-killed run vanish from the result with no `cli::cli_warn`. A user who ran nRuns=4 and got back result$nRuns=2 has no way to tell whether 2 workers were killed by the 30s timeout (real concern: batches too long) or never launched (benign pool drain on early stop). At minimum emit a one-line warn naming the dropped run index. |
| PAR-009 | LOW | FIXED (dae4652) | UX | nRuns shrinkage not surfaced | `R/RunMkPrime.R:1842, R/MkPosterior.R:42` (PR #1, round 5b verification) — when `.RunParallelRuns` shrinks runs from 6 to 4, `result$nRuns` becomes 4 while `result$mcmc$nRuns` remains 6. `print.MkPosterior` shows the lower number with no note. Combined with PAR-008, masks meaningful failure modes from the user. |
| PAR-010 | LOW | FIXED (dae4652) | Test weakness | mtime wave-ordering test admits all-simultaneous regression | `tests/testthat/test-parallel.R:200-206` (PR #1, round 5b verification) — `expect_gte(min(mtimes[3:4]), max(mtimes[1:2]))` passes trivially when all 4 mtimes fall in the same NTFS second-tick, even if a regression launched all 4 simultaneously. Mtime test runs in ~2s total per test summary, so on a slightly faster machine all 4 logs share a tick. Either stretch each run past 2s of unique work, or assert a stronger relationship using callr proc start-times instead of file mtime. |
| PAR-011 | LOW | FIXED (dae4652) | Polish | Stray `MkPrime:::` in PlotDuringMCMC | `R/PlotDuringMCMC.R:653` (out of PR #1 scope, round 5b sniff) — `MkPrime:::.LogFilePaths(...)` should be a bare reference like PAR-005's fix. Same anti-pattern. |
| PAR-012 | LOW | FIXED (dae4652) | Design | 30000ms wait-timeout hardcoded | `R/RunMkPrime.R:1826` (PR #1, round 5b verification) — `$wait(timeout = 30000)` assumes batch boundary ≤30s. Users with large datasets running long warmup batches can have batches exceeding this, triggering avoidable PAR-006 crashes. Consider `max(30000L, mcmc$pollInterval * 1000L * 3L)` or a documented `mcmc$cancelGrace` option. |
| EG-010 | LOW | OPEN | Bug | σ_logit step adaptation has no upper bound | `R/RunMkPrime.R:3786-3787` — `tuning[[tk]] <- max(0.01, tuning[[tk]] * adj)`. Persistent acceptance=1 → unbounded growth. Self-correcting in practice; cap at ~10 for robustness. |
| EG-INFO-1 | INFO | — | Note | Tempering does not help p-mixing under EG | `src/mcmc.cpp:4538` — prior term not scaled by β; p doesn't enter LL, so hot chains see same case-30 acceptance as cold. Rules out heat-change (commit 82dd4fd) as a hypothesis for u_post anchor. |
| STREAM-001 | HIGH | FIXED | Bug | mk-arm tree streaming — brColStart off-by-2 + resume drops treeFile (Round 4) | (Deliberate deviation from FIXED/OPEN binary: two candidate root-cause defects in this bug's causal chain are now fixed and regression-tested, but the empirical Hamilton symptom cannot yet be reproduced locally, so I won't claim FIXED.) `R/RunMkPrime.R:277, 2369, 2389, 2406` — fixed in Round 4. Two related code defects: (a) `brColStart` calc in both `RunMkPrime` (:277) and `ResumeMkPrime` (:2369) omitted the 2 diagnostic columns (`swap_cold`, `topo_hash`) that C++ writes between `beta_scale` and the `kPrime_*` columns. Resulting tree had its last 2 edge lengths replaced by garbage (one of them `topo_hash`, a value ~1e16); written tree was still parseable but unusable for any tree-based downstream analysis. (b) `ResumeMkPrime` passed `treeFile = NULL` to both `.RunSerialRuns` (:2389) and the per-run loop (:2406), so any chain that hit a SIGKILL, was resumed, and continued sampling produced no tree output for the entire post-resume run. The Hamilton mk-arm SLURM job-17154895 was a single 8h attempt with no requeue, so the `treeFile=NULL`-on-resume path doesn't fully explain the 232 empty-tree tasks (which had 871k log rows from a single attempt). The brColStart bug **does** affect every run universally but only invalidates edge lengths, not the line count. Empty tree files on Hamilton remain partially unexplained; the local reproducer at `dev/red-team/repros/stream-001-002.R` does NOT reproduce empty tree files even pre-fix. Marked PARTIAL: two real defects fixed, but a third environmental factor on Hamilton may still be in play. |
| STREAM-002 | HIGH | FIXED | Bug | Periodic checkpoint flush — fixed in Round 4 | `R/RunMkPrime.R:1486, 1572` — `.RunSerialRuns` was passing `checkpointFile = NULL` to inner `.RunMkPrimeSingleRun` calls, suppressing every per-batch save (lines 1239/1250 sample-phase, 1304/1321/1346 maxTime/cancel/checkEvery). Only the iter=0 save at :353 and the post-Phase-1/per-epoch saves at :1513/:1591 ever fired. A SIGKILL during a long Sample-phase run thus left an unrecoverable iter=0 checkpoint. Fix: pass `mcmc$checkpointFile` through, and update the in-batch save calls to pass `shared$runs` (all runs) instead of `list(r)` (current run only) so the cross-run checkpoint stays coherent. Regression test in `tests/testthat/test-checkpoint.R::"Cross-run R-hat orchestrator saves checkpoint mid-run"`. |
| STREAM-003 | MED | FIXED (Round 6 — Round-4 fix was phantom) | Bug | brColStart off-by-2 ignores `swap_cold`/`topo_hash` (CLAIMED FIXED in Round 4 — but `git log -S "diagCols"` and inspection of worktree HEAD `ea5ad22:R/RunMkPrime.R` show the fix was never written; truly fixed in Round 6; process problem filed as TREEMOVE-001) | `R/RunMkPrime.R:277, 2369` — surfaced while investigating STREAM-001. R-side `brColStart` calculation did not account for the two diagnostic columns (`swap_cold`, `topo_hash`) that C++ inserts into every scalar sample row between `beta_scale` (or whichever last hyperparam) and the `kPrime_*` block. `relBr <- row[brColStart:(brColStart + nEdge - 1L)]` therefore read 2 columns to the left of the true `br_*` block, so the reconstructed tree's first 1–2 edge lengths were taken from the diagnostic cols (one of them `topo_hash`, an FNV hash of order 1e16) and the last 1–2 actual `br_*` were dropped. Tree was still written to disk and round-tripped through `ape::read.tree`, but with garbage edge lengths. Affected ALL streaming arms (mk and EG), unconditionally. Regression test: `tests/testthat/test-tree-thin.R::"mk arm ... writes trees with finite edge lengths"` (now also asserts non-negative edge lengths). |
| STREAM-004 | LOW | FIXED (Round 6) | Bug | brColStart off-by-1 for `kPrimePrior = "beta_geometric"` | `R/RunMkPrime.R:277, 2369` — the post-fix formula uses `pCols = if (isLogseries) 0L else 1L`, which is correct for `"geometric"`/`"empirical_geometric"` but **wrong for `"beta_geometric"`**: that prior contributes 2 hyperparam cols (`kprime_alpha`, `kprime_beta` per `.ParamNames` line 3293-3294), not 1. The reconstructed tree therefore drops the last 1 actual `br_*` and reads `kprime_beta` (or similar) as one edge length for any beta_geometric arm. Fix: branch on prior, or set `pCols <- length(grep("^kprime_alpha$|^kprime_beta$|^p$", paramNames))` from paramNames directly. Out of scope for the STREAM-001/002 work since beta_geometric is not used in the prior-validation campaign. |
| HARNESS-001 | MED | ROOT-CAUSED | Bug | R-level maxTime=7.5h does NOT trigger graceful exit on Hamilton | `data-raw/hamilton/run_one.R:174` has `maxTime = 7.5 * 3600`. Empirically tested in job 17170442 (mk arm, 260 tasks, 8h SLURM wall): all 260 tasks SIGKILL'd at 8h SLURM TIMEOUT, 0 partial `.rds` files written. Tree streaming + checkpoint flush worked correctly (260/260 tree files with substantial content; STREAM-001 confirmed FIXED in same run), but the R-level graceful stop at maxTime=7.5h did not fire. Hypotheses: (a) maxTime check only runs between batches in `.RunMkPrimeSingleRun` and Sample-phase batches may exceed 30min; (b) maxTime check is structural — fires at phase transitions but not mid-Sample; (c) some long-running C++ call doesn't yield back to R between batches. Streamed data (scalar `.log` files, `_trees.nwk`, periodic checkpoints) is fully recoverable; only the post-run summary RDS is missing. Severity upgraded LOW→MED: HARNESS-001 doesn't block analysis but does block clean resume semantics for any run that hits wall, and may mask `RunMkPrime`-side maxTime bug. **Root cause (2026-05-16, isolated subagent investigation, full report at `dev/red-team/harness-001-investigation.md`):** With `nRuns=2L` + `maxRhat=1.1`, the harness routes through `.RunSerialRuns` (`R/RunMkPrime.R:385`). Each inner `.RunMkPrimeSingleRun` resets `startTime` at line 757, so each run gets its own 7.5 h budget. The Phase-1 loop (lines 1487-1512) only breaks early on `"cancelled"`, not `"max_time"`. So Run 1 returns cleanly after 7.5 h, Run 2 starts a fresh 7.5 h budget, and SLURM SIGKILL's at 8 h before Run 2's first wall-clock check fires. Secondary: `samplingBatch = 5000L` (line 693) means the inner `maxTime` check (line 1300) only fires every 5000 iterations, adding multi-minute jitter. **Fix:** Share one `startTime` across all runs and inject the *remaining* budget into each `innerMcmc$maxTime`; also break Phase-1 loop on `stop_reason == "max_time"`. `.RunParallelRuns` (line 1693) is unaffected — already uses a single shared `startTime` and is the implementation model. Test: assert that a 2-run serial config with unreachable `maxRhat` exits within `maxTime + one_batch`, not `2 * maxTime`. |
| TREEMOVE-001 | HIGH | OPEN | Process | brColStart formulas drop diagnostic cols (3rd recurrence; permanent fix needs single source of truth) | `R/RunMkPrime.R:269, 2633` — STREAM-003 (Round 4) and STREAM-004 (Round 4) were both filed as "FIXED" but neither was ever written to disk (`git log -S "diagCols"` empty across all branches; worktree `ea5ad22:R/RunMkPrime.R` still buggy). Round 6 wrote the fix for the third time. Two structurally-different inline formulas (`RunMkPrime`: `4L + neoCols + pCols + qHet + diagCols + nTrans + 1L`; `ResumeMkPrime`: `5L + pCols + (any neomorphic) + qHet + diagCols + nTrans + 1L`) must each agree with `.ParamNames` to land the right column offset — a smell that a single source of truth would close. **Permanent fix**: replace both inline formulas with a helper `.BrColStart(paramNames)` that does `which(grepl("^br_1$", paramNames))[1]` — guaranteed to track `.ParamNames` even if more diagnostic columns are added. Regression test added in `tests/testthat/test-tree-thin.R::"streamed trees have finite, non-negative, plausible edge lengths"`. |
| TREEMOVE-002 | LOW | OPEN | Bug | `topo_hash` diagnostic is unstable across topologically-equivalent representations after in-place NNI | `src/mcmc.cpp:516-523` (`fnv_topo_hash`), case 5 NNI in-place branch at `src/mcmc.cpp:3991-4003` — comment claims "Edges must be in canonical preorder (guaranteed by all tree moves)" but in-place NNI deliberately does NOT canonicalise via `preorder_weighted_impl`; only preserves the weaker invariant that parent appears before child. After a sequence of accepted in-place NNIs without an interleaved SPR/TBR/Dirichlet (which DO canonicalise), the parent vector may hash differently for the same unrooted topology. Effect: `topo_hash` written at sample time (line 4867) takes different values for runs that visit the same topology → inflates the `length(unique(trace$topo_hash))` metric used in `data-raw/step1_hamilton_logp_decomp.R:34`. Likelihood unaffected. Fix options: (a) canonicalise a copy at hash time (cheap), (b) re-canonicalise state->parent in-place after each accepted NNI (more expensive), or (c) downgrade `topo_hash` docs to "approximate" and recommend a TreeTools-based hash in R for exact identity. |
| TREEMOVE-003 | LOW | OPEN | Test gap | No regression test asserts R `.kMoveTypes` ↔ C++ dispatch ↔ `validNames` agree | `R/RunMkPrime.R:3248-3277` (`.kMoveTypes`), `src/mcmc.cpp:3913-4263` (dispatch switch), `R/MkPrimeMCMC.R:503-522` (`validNames`) — currently `mh_p` (case 8) and several `slice_*` aliases hit live C++ cases but are silently absent from `validNames` (already filed as EG-009). A test that asserts the three surfaces agree (every key in `.kMoveTypes` has a corresponding C++ case AND appears in `validNames`) would prevent future drift if a new case is added to C++. |
| TREEMOVE-004 | LOW | OPEN | Doc | `proposals.R` documents NNI/SPR/TBR as "unrooted binary" but C++ implementation is strictly rooted | `R/proposals.R:93,102,126,138,167` and `src/proposals.cpp:26` (`const int root = nTip + 1`), `src/tree_moves.cpp:263`, `src/node_cl_cache.h:1083` — proposal impls treat tree as rooted; `eligiblePrune` excludes the 2 edges leaving root. Mk reversibility makes this scientifically correct (rooted SPR explores unrooted topology space with same posterior weight on each rooting), but the docstring claim "unrooted binary" is misleading. Either correct the docstring or change the proposal to ignore root-incident edges. |
| LIKE-001 | HIGH | OPEN | Bug | Singleton-site ascertainment correction dropped from partial-CL and Gibbs paths under `coding="informative"` | `src/node_cl_cache.h:697,703,716` (`cache_total_loglik` — neo, known, trans branches), `src/mcmc_likelihood.cpp:1577-1603` (`const_site_prob_for_k`, used by Gibbs k′ sweep at `src/mcmc.cpp:3284-3297`), `src/mcmc.cpp:878` (`gibbs_spr_impl`, explicit TODO), and analogous sites in `gibbs_spr_impl_het`, `gibbs_subtree_swap` — when `data->codingType == 2` ("informative"), all of these compute the ascertainment correction using only constant-site probability and DROP the singleton-site term. `cpp_partition_log_likelihood` (full-eval at line 4527) correctly adds singleton, so partial-CL moves produce a logLik wrong by `Σ nChar × [log(1−p_const) − log(1−p_const−p_singleton)]` (~0.3 nat/char in the test data; 1.5–1.8 nat total drift on a 10-char × 8-tip example). **Effects:** (a) MH acceptance ratios for partial-CL-eligible moves (NNI, beta_simplex, dirichlet, SPR M-158) compute wrong α under coding="informative", biasing chain; (b) `state->logLik` desyncs from true likelihood after every accepted partial-CL move (existing per-100-iter drift check at `src/mcmc.cpp:3839-3856` catches but only counts, no correction); (c) **Gibbs SPR and Gibbs k′ sweep sample using wrong weights** — these are always-accept moves so the chain does NOT target the desired posterior under informative coding. **Empirical reproduction (Round 7):** all-trans 8-tip 10-char dataset, `coding="informative"`, `gibbsSpr=FALSE,gibbsSubtreeSwap=FALSE`, 1000 iters → `diag_counters$dir_mismatch = 106/106 (100%)`, drift=1, mismatch magnitudes 0.5–1.8 nats; same setup `coding="variable"` → `0/112 (0%)`, drift=0. **Fix sketch:** in `cache_total_loglik`, after constant-site computation for each part, `if (cache.coding == 2) p_total = p_const + singleton_site_prob_jc(..., k, ...)` (and mkn variant for type 0). Same pattern at every TODO site. Per `test-het.R:614`, qHeterogeneity is incompatible with "informative", so the het-path TODO at `src/mcmc_likelihood.cpp:1483` does not need fixing for production. **Severity HIGH:** default `coding="variable"` unaffected, but users selecting `coding="informative"` (documented + tested at `test-ascertainment.R:240-263`) get a silently incorrect chain whenever cache-accelerated partial-CL moves run — the default path since `cacheBonus=5` boosts these moves. |
| LIKE-002 | LOW | OPEN | Test gap | `test-node-cl-cache.R` regression-style assertions only exercise `coding="variable"` | `tests/testthat/test-node-cl-cache.R:140-169, 182-232` — `"slice sampler invalidates CL cache (M-145 regression)"` and `"partial-CL path does not freeze tree_length"` use default `MkPrimeModel()` (coding="variable"). Adding a parameterised variant with `coding="informative"` and the same `drift == 0` / `diag_counters$dir_mismatch == 0` assertion would catch LIKE-001 the moment partial CL was introduced. Will fail until LIKE-001 fixed. |
| LIKE-003 | LOW | OPEN | Perf | `cache_total_loglik` re-scans cache.units to find type-1 partition members | `src/node_cl_cache.h:705-722` — outer loop over partitions (line 684); inside type-1 branch, scans all `cache.units` to find those with `unit.partIdx == pi`. For typical mkp data (1 transformational partition) negligible; for any future workload with many partitions, becomes O(P × nUnits/P). Suggested: precompute `std::vector<std::vector<int>> partUnits(nParts)` once after `build_cache_units`. Correctness unaffected. |
| LIKE-004 | INFO | — | Note | Diagnostic counters never surfaced to R user | `src/mcmc.cpp:3839-3856` (`diagDriftCount`), 4347-4355 (`diagNniMismatchCount`), 4416-4436 (`diagDirMismatchCount`) — counters increment in MCMC inner loop and return via `run_mcmc_batch_cpp` (line 4898+) but are NOT consumed by `RunMkPrime`. They caught LIKE-001 during the red-team test but would be invisible in production. A drift count > threshold (say, 1% of iters) should emit `cli::cli_warn`. |
| CONV-001 | MEDIUM | OPEN | Bug | False-positive `converged = TRUE` when all scalar params constant in convergence window | `R/RunMkPrime.R:2039` (`min(ess[isConvParam], na.rm=TRUE)` returns `Inf` when every selected ESS is NA), `R/RunMkPrime.R:2051-2052` (`max(rhat[...], na.rm=TRUE)` returns `-Inf` when every R-hat is NA). Convergence test `minEss >= mcmc$minEss` is then TRUE (Inf ≥ anything) and `!is.na(maxRhat) && maxRhat <= mcmc$maxRhat` is also TRUE (`is.na(-Inf)` is FALSE, `-Inf <= 1.01` TRUE). Same `Inf`-via-NA pattern S-RED16 (commit 97819de) fixed for tree ESS — missed in three sibling sites: `.CheckConvergence` (lines 2039,2051), `.CheckConvergenceFromLogs` (lines 2142,2153), `ConvergenceDiagnostics` (`R/Convergence.R:65-66,76`). Reachable when the first convergence check fires before any move has been accepted (10-sample window of identical rows), or when every keyCols column degenerates simultaneously. Fix: filter non-finite via `is.finite` before `min`/`max`, return `NA_real_` when no finite values remain. Wrap with `suppressWarnings()` to suppress the noisy "no non-missing arguments" warning. Regression test: assert `.CheckConvergence` returns `converged=FALSE` when fed a per-run sample matrix of all-equal rows. |
| CONV-002 | MEDIUM | OPEN | Bug | `.ComputeRhat` recycles unequal-length runs via `cbind`, silently corrupting R-hat | `R/Convergence.R:247-252` — builds `chainMat <- do.call(cbind, lapply(perRun, function(r) r$samples[, keyCols[j]]))` without harmonising per-run sample counts. When runs have differing `nrow(samples)` — which occurs in interrupt recovery (`R/streaming.R:251-265` builds `per_run` from individual `ReadMkLog(f)` results with no length harmonisation) — `cbind` recycles the shorter vector, emits "number of rows of result is not a multiple of vector length" warning, and produces a garbage chainMat. R-hat is then computed on misaligned data. Same root cause as M-146 which was previously fixed in `.CheckConvergenceFromLogs` (`R/RunMkPrime.R:2127-2135`, test at `tests/testthat/test-ess-rhat.R:241-279`). Fix: tail-equalise lengths the same way `.CheckConvergenceFromLogs` does (`tail(s, minRows)`) before `cbind`. Regression test asserting `ConvergenceDiagnostics` emits no warning on unequal-length per-run posteriors. |
| SWAP-001 | HIGH | OPEN | Bug | `GibbsSubtreeSwap` commits state without a Metropolis–Hastings accept step | `src/mcmc.cpp:1874` — the Gibbs subtree-swap move at line 1874 commits `state->logLik` unconditionally; the only path on which the proposal does not advance is the "self-draw → no-op" at line 1839. If the partner-set count `|partners(B)|` differs from `|partners(A)|` across the swap (which it generally does — partner counts depend on local subtree structure), a Gibbs draw without an MH correction does **not** target the stationary distribution. Surfaced by L3 (math-prover) campaign 2026-05-26, `dev/red-team/proofs/hastings-tree-moves.md` §"GibbsSubtreeSwap". Empirical confirmation needed: extend `tests/testthat/test-tbr-detailed-balance.R` with a β=0 chain that uses only `GibbsSubtreeSwap` and a flat target, and check uniformity across labelled binary topologies via chi-squared (the existing test does this for TBR/SPR). If the chi-squared rejects, this is a real bug; the fix is to add an MH accept step using the explicit partner-set log-ratio. |
| SWAP-002 | HIGH | OPEN | Bug | `WeightedSubtreeSwap` `logHR` omits the partner-set log-ratio | `src/mcmc.cpp:2980–2983` — `WeightedSubtreeSwap` does include an MH accept step, but the `logHR` it submits to the accept rule omits any `log(|partners_{T'}(B)| / |partners_T(A)|)` term. The forward proposal picks a partner uniformly from `partners_T(A)`; the reverse picks from `partners_{T'}(B)`; these candidate-set sizes can differ. Without the ratio in `logHR`, the move is detailed-balance-correct only when the two sizes coincide (e.g. small symmetric trees). Surfaced by L3 (math-prover) campaign 2026-05-26, `dev/red-team/proofs/hastings-tree-moves.md` §"WeightedSubtreeSwap". Empirical confirmation as for SWAP-001 (β=0 flat-target uniformity test on weighted variant). Fix sketch: count `|partners_{T'}(B)|` after the swap and add `log(|partners_{T'}(B)|) - log(|partners_T(A)|)` to `logHR` before passing to the accept step. |
| MOVE-INFO-1 | INFO | — | Note | NNI/SPR/TBR proofs cleanly verified; minor stylistic redundancy in TBR BFS | `src/tree_moves.cpp:306` has a redundant `isDesc[parent[i]]` clause in the TBR BFS guard — surfaced by L3 (math-prover) campaign 2026-05-26. Mathematically harmless (`isDesc[i]` already implies `isDesc[parent[i]]` by BFS ordering); flagged for stylistic cleanup, no functional impact. NNI / plain SPR / plain TBR / plain subtree-swap / pSPR / GibbsSPR / WeightedSPR Hastings & detailed balance all verified watertight (the latter two with caveats: GibbsSPR fixed-tau assumption; WeightedSPR informal topology-cancellation argument mitigated by its MH step). |
| ACRV-INFO-1 | INFO | — | Note | ACRV discretisation watertight; lane brief had wrong file path | `R/acrv.R:39`, `src/mcmc_likelihood.cpp:85`, `src/gibbs_partial_cl.h:195`, `src/node_cl_cache.h:51` — surfaced by L7 (math-prover) campaign 2026-05-26. The orchestrator brief said the discretisation lived in `src/acrv.cpp`, which does not exist (that filename holds only pruning kernels for ACRV-bearing models). Discretisation is the R-side function at `R/acrv.R:39`; midpoint-and-rescale pattern reproduced at three C++ sites. Mean-1 identity holds for any σ ≥ 0, any K ≥ 1, after the K/Σ rescale. Large-σ ceiling at K (for K=6, top rate → 6 from below) is the mechanism behind `feedback_acrv_saturation_masks_state_drift` — confirmed as designed, not a bug. |
| LIKE-001 | HIGH | PATCH-READY | Bug | Singleton-site ascertainment correction dropped from partial-CL and Gibbs paths under `coding="informative"` (UPDATE) | L5 (math-prover) campaign 2026-05-26 closed the math, see `dev/red-team/proofs/ascertainment.md`. The Mk' relabelling correction commutes exactly with both ascertainment denominators under JC (C and G are σ-invariant subsets of S^n; L_fels permutation-symmetric), so no relabel × ascertain interaction complicates the fix. Closed-form replacement is `p += singleton_site_prob_jc(...)` under `coding == 2`. A ~7-line trivial patch is at `dev/red-team/patches/L5-ascertainment.patch` (touches `const_site_prob_for_k`); the partial-CL sites (`src/node_cl_cache.h:697,703,716`) need the analogous one-line addition. Awaiting human review before apply. |
| LS-001 | MED-latent | OPEN | Bug | Logseries k'-prior also missing per-character truncation normaliser Z_i(c) | `R/MkPrimeModel.R::.LogPriorLogseries` (line range in proof) — surfaced by L6 (math-prover) campaign 2026-05-26, `dev/red-team/proofs/kprime-priors.md`. Same arithmetic family as EG-001: prior is normalised over the full k'∈{2,3,…} support, but `LogPrior` enforces k'≥kObs_i without dividing by `Z_i(c) = Σ_{k≥kObs_i} P(k\|c)`. **Currently latent** because `kprimeLogseriesC` is a fixed hyperparameter — the missing Z_i bias cancels in every MH ratio that holds c fixed. **Must be fixed before c becomes a sampled parameter** (e.g. any future config that enables a slice/MH update on `kprimeLogseriesC`). Fix is the same shape as EG-001: per-character runtime Z_i summation in `LogPrior` and `cpp_log_prior`. No patch attached — non-trivial. |
| R5-4 | — | OUT-OF-LANE | Process | R5-4 of pi0/phi/z audit is empirical, not analytic | `project_pi0_audit.md` R5-4 — surfaced by L6 (math-prover) campaign 2026-05-26. The claim is about post-relabel `phi` medians depending on `sim3-scoring.R::HasBipartSplits` output (the function fixed in `project_scoring_bug`). This cannot be discharged by a math-prover — it requires re-running the sim3 scoring pipeline against the corrected `HasBipartSplits`. R5-1, R5-2, R5-3 all re-verified and still stand (those are prior-arithmetic and scoring-independent). Action: re-run sim3 scoring; defer R5-4 to a heavy-test or empirical follow-up session. |
| FAST-EXP-001 | HIGH | PATCH-READY | Bug | `1 − exp(arg)` and F81 `1 − d` cancellation at small rt | ~24 sites in `src/likelihood.cpp` and `src/mcmc_likelihood.cpp` (full list in patch) — surfaced by N1 (numerical-auditor) campaign 2026-05-26, `dev/red-team/numerical/fast-exp-conditioning.md`. The matrix-exp primitive `fast_neg_exp` itself is stable (max ε_rel 8.6e-15 vs mpmath dps=50 reference over 50k samples). The bug is in the **algebra around it**: `p_diff = (1/k) − (1/k)·exp(arg)` and the F81 analogue `1 − d` suffer catastrophic cancellation when `rt` is small. Worst measured ε_rel = **0.39** at rt=1e-15; ε_rel = 3e-5 at the production-realistic rt=1e-12. **Reachability**: under the default `rateLogSd ~ Gamma(1, 1)` prior, σ ≳ 7 combined with short branches produces rt ≲ 1e-12 — i.e. posterior tail, not bounded out. A chain visiting this regime evaluates state-discordant sites as `log(0) = −∞`, biasing acceptance. Fix is mechanical: `dev/red-team/patches/N1-fast-exp.patch` substitutes `-std::expm1(arg)` for `1 - MKP_EXP(arg)` at the 24 affected call sites; restores ulp accuracy (≤2.4e-16) at ~1.5× exp-cost per branch. Awaiting human review. |
| N2-INFO-1 | INFO | — | Note | `log(1−p) → log1p(−p)` substitution is bit-equivalent in IEEE 754; real loss is upstream | `src/likelihood.cpp` / `src/mcmc_likelihood.cpp` ascertainment subtractions — surfaced by N2 (numerical-auditor) campaign 2026-05-26, `dev/red-team/numerical/pruning-underflow.md`. Orchestrator hypothesis was that `log(1-p) → log1p(-p)` would recover precision when p→1. Experiment 6 (driver script) measured zero difference at p = 1 − 10⁻ⁱ for i=1..16 — once p is stored as a double, `1.0 − p` rounds once and both formulations consume that rounded value. The genuine precision loss is upstream in computing `p_const` itself. A 16-site `expm1` substitution patch was captured at `dev/red-team/patches/N2-pruning-underflow.patch` for completeness but **marked cosmetic and not recommended for application**. |
| N2-INFO-2 | INFO | — | Note | Pruning kernels lack rescaling but underflow surfaces as `R_NegInf`, not silent corruption | `src/likelihood.cpp:123,249,352`; `src/mcmc_likelihood.cpp:263,444,1038,1217,1449,1641,1949` — surfaced by N2 (numerical-auditor) campaign 2026-05-26. Felsenstein pruning has no partial-likelihood rescaling, but every site_lik value is guarded by `<= 0` checks that return `R_NegInf` to R. Reachable for caterpillars of depth ≥ 64 with branch lengths ≤ 1e-12, or depth ≥ 512 with branches ≤ 0.1. Worst measured ε_rel across the 5×7 + 4×4 stress grid was 1.84e-16 (~1 ULP) vs an independent pure-R reference — defensive failure mode, not a silent bug. If production trees ever grow to depth ≳ 100 at near-zero branches, rescaling becomes worth adding. |
| CONV-002 | MEDIUM | PATCH-READY | Bug | `.ComputeRhat` cbind-recycle confirmed live (UPDATE from D3) | `R/Convergence.R:249-251` — D3 (mcmc-diagnostician) campaign 2026-05-26 confirmed CONV-002 empirically: `do.call(cbind, lapply(...))` emits the predicted "number of rows of result is not a multiple of vector length" warning on unequal-length per-run matrices. Currently latent in production (runs are length-equal) but adaptive truncation or partial run drops would expose it. 13-line trivial patch at `dev/red-team/patches/D3-ess-rhat-stress.patch` (tail-equalises per-run sample matrices to `min(nrow)` before `cbind`). Awaiting human review. |
| SWAP-001 | HIGH | OPEN (test inconclusive) | Bug | `GibbsSubtreeSwap` no MH step (UPDATE from D2) | `src/mcmc.cpp:1874` — surfaced by L3 (math-prover). D2 (mcmc-diagnostician) ran quick-mode β=0 flat-target chains: 5-tip × 50k iter × thin 25 → 2000 samples — chi² p=0.979, no detection. **Quick-mode PASS does NOT refute SWAP-001**: at n=5 with 2000 samples, partner-set asymmetries may be too small to surface. Escalation path documented in `dev/red-team/heavy-tests/tree-ess-and-swap.md`: full-scale 1M iter / 5000 samples on Hamilton; if that also passes, re-run at n=6 or n=7 to expose larger partner-set differences. Math concern from L3 stands until empirical test at meaningful scale either confirms or refutes. |
| SWAP-002 | HIGH | OPEN (test inconclusive) | Bug | `WeightedSubtreeSwap` `logHR` omits partner-set ratio (UPDATE from D2) | `src/mcmc.cpp:2980–2983` — same status as SWAP-001 after D2: quick-mode chi² p=0.749 (no detection); needs escalation to 1M iter at n=5 or smaller-N×more-samples at n=6/7 before concluding either way. |
| TREE-ESS-INFO-1 | INFO | — | Note | `src/tree_ess.cpp` is root-invariant (D2 verified empirically) | D2 (mcmc-diagnostician) campaign 2026-05-26 — `dev/red-team/heavy-tests/tree-ess-rooting.R` ran a 6-tip 200-tree NNI walk with per-tree random re-rooting; `identical(dmat_orig, dmat_rerooted) == TRUE`, `|delta ESS| = 0` for both medianPseudoESS and frechetCorrelationESS. RF root-invariance is bit-exact. Closes a long-standing concern (per project memory `project_scoring_bug` noting `ape::prop.part` root-dependence): `tree_ess.cpp` does not share `prop.part`'s problem. |
| ESS-CAP-INFO-1 | INFO | — | Note | Possible ESS cap on white-noise small N — needs follow-up | D3 (mcmc-diagnostician) campaign 2026-05-26 ran a probe row on white-noise small N that hinted at an ESS-cap behaviour worth investigating. Not yet a concrete finding — flagged here so a follow-up red-team or profile session can drill in. See `dev/red-team/heavy-tests/ess-rhat-stress.md` "ESS-cap probe" row. |
| SBC-MASS-FAIL-001 | HIGH | OPT-B INSUFFICIENT — kPrime_pooled also bimodal under option α; underlying joint mismatch not yet conclusively diagnosed | Bug | D1 SBC kPrime_pooled FAILs (AD p ≈ 1e-7 in all 4 Mk' arms) trace to a forward/inference prior mismatch on the kPrime parameter | **Diagnosis (mcmc-diagnostician agent, 2026-05-27, full report at `dev/red-team/heavy-tests/sbc-mass-fail-diagnosis.md`).** Forward `sbc.R::.drawKPrime` (lines 192-194) draws `kPrime = u + 2` with `u ~ Geo(p)`. Inference `R/MkPrimeModel.R:470` evaluates `u = kPrime - kObs[i] ~ Geo(p)` per character. Forward and inference parameterisations disagree whenever `kObs_i > 2`, which is ~47% of characters in the simulator's output (per agent analysis). Result: kPrime_pooled rank is bimodal (60% sims median rank<5, 8.5% median rank>60), AD rejects at p ≈ 1e-7. **Two earlier bugs already fixed (commit 8e4e370):** tree_length forward-prior used `sum(runif(0.05, 0.30))` (median 2.46) vs inference Gamma(2, 2/50) mean 50 → ranks pile at 0; rateLogSd forward clamped to ≤3.0 vs inference unclamped. Both fixes confirmed by partial PASS of MkNT_geometric and rate_log_sd across all arms in v2 results. **Outstanding (this finding):** kPrime parameterisation. **Fix sketch:** either (a) make forward `kPrime_i = kObs_i + Geo(p)`, but kObs emerges from forward simulation — needs rejection-sample loop until kObs ≤ kPrime, OR (b) restrict kPrime_pooled SBC to characters where kObs == 2 (drops sample size but keeps validity), OR (c) compute rank in the unconstrained `u_i = kPrime_i - 2` space and verify against `Geo(p)` directly. Option (c) is mathematically cleanest. **Run state:** 17289622 (pre-fixes, 6/6 FAIL) at `dev/red-team/heavy-tests/sbc-results-hamilton/`; 17294905 (tree-length + rateLogSd fixes, 1/6 PASS — MkNT_geometric only) at `dev/red-team/heavy-tests/sbc-results-hamilton-v2/`; 17295308 (post-likelihood-patches L5+N1+D3, same harness as 17294905, **expected to show same kPrime_pooled FAIL pattern — predicting MkNT_geometric+MkNT_logseries PASS and 4 Mk' arms FAIL on kPrime_pooled only**) submitted 2026-05-27 08:35, in flight. Severity HIGH because it blocks SBC validation of Mk' priors until fix lands, but downgrade likely once harness is corrected — the underlying L6 priors are proven watertight. |
| SBC-HARNESS-002 | LOW | OPEN | Bug | SBC per-arm tasks all write `${out}/verdict.txt` — last finishing task wins | `dev/red-team/heavy-tests/sbc.R::write_top_level_summary` writes the top-level summary as a single shared file. With array tasks running in parallel and writing to the same shared `${out}/verdict.txt`, only one arm's run-summary is preserved (whichever finished last). Per-arm verdicts under `${out}/<arm>/` are fine. Cosmetic; doesn't affect interpretation since per-arm files are intact. Fix: include `--arm` in the top-level filename or skip the top-level write when running with `--arm`. |
| SBC-HARNESS-004 | HIGH | FIXED 2026-05-27 | Bug | `.simJCchar` iterates Preorder edge list in reverse (postorder), corrupting all tip states |
| SBC-HARNESS-006 | CRITICAL | FIXED 2026-05-27 | Process | Hamilton SBC SLURM jobs silently ran the stale OUT-OF-TREE `${RT}/heavy-tests/sbc.R` instead of the source-controlled `${SRC}/dev/red-team/heavy-tests/sbc.R` | `dev/red-team/heavy-tests/submit-sbc.sh` — line invoking `Rscript "${RT}/heavy-tests/sbc.R"`. The path `${RT}/heavy-tests/` is a non-source-controlled scratch copy on Hamilton dated 2026-05-27 13:16, which predates SBC-HARNESS-003/004/005 fixes (committed 14:27–15:04 the same day). Every SBC run since that copy was last synced (v3, v4, v5, v6, v7) actually ran the *stale* harness — meaning the rev()-postorder bug was present in all of them despite the source tree showing it fixed. **Smoking-gun discriminator**: stale rev()-buggy simulator + sim 1 (seed 20261527) produces n_var=27, kObs pattern `221222122222222222222222212222`. Fresh in-tree simulator + same seed produces n_var=30, kObs pattern all 2s. v7 summary.rds sim 1 has n_char=27 and kObs all 2 (after filtering) — pattern matches the stale buggy run exactly, not the fresh fix. SBC-HARNESS-005's topology fix was ALSO unused (the stale sbc.R still uses `AdditionTree`). **Diagnosed via MLE-on-fixed-tree check**: v7 sims show `ll(tl=MLE)-ll(tl=tl_true) < 0.04 nats` (likelihood flat), so any HIGH rank pile means the *generated data* doesn't match `tl_true` — pointing at the forward simulator. Local re-run with the in-tree sbc.R produces uniform tree_length ranks (AD p=0.99) — finally confirming SBC PASS for MkNT_geometric. **Fix**: change submit-sbc.sh to invoke `"${SRC}/dev/red-team/heavy-tests/sbc.R"`. Also wipe `${RT}/heavy-tests/` or symlink it to the source tree to prevent recurrence. **All SBC runs v3–v7 must be re-interpreted as having tested the buggy harness, not the production sampler.** |
| SBC-HARNESS-005 | HIGH | FIXED 2026-05-27 | Bug | SBC harness uses `AdditionTree` as start topology with `fixTopology=TRUE`; near-saturated JC data makes parsimony useless, so topology is almost always wrong | `dev/red-team/heavy-tests/sbc.R:308–316` (start-tree block) — with `fixTopology = TRUE` the harness only samples continuous parameters conditional on the starting topology. Valid SBC requires this topology to equal the true topology; otherwise `p(θ|y, T_start)` is tested instead of `p(θ|y, T_true)`. At `tl_true ~ Gamma(2, 0.04)` (mean=50), branches average ~3.5 expected substitutions per character and JC saturation reduces data to near-random noise (`P(same) ≈ 0.50`). Parsimony-based `AdditionTree` has zero topology-recovery power: empirical check (10 sims) showed RF(AdditionTree, true_tree) ≥ 8 in 10/10, RF=10 (maximum for 8 taxa) in 8/10. MCMC conditioned on the wrong topology samples a TL posterior that is systematically lower than truth → SBC ranks pile HIGH. Evidence: (a) sbc-warmup-trace sim 1 with RF=10 start gave rank=67/67; same sim with `--true-topo` gave rank=61/67 (within random scatter under H0). (b) The `tree_length` SBC FAIL persisted in v6 (mean rank ~52 vs expected 33.5) despite the SBC-HARNESS-004 rev() fix, confirming topology is the dominant driver. **Fix:** replace `AdditionTree(pd)` start-tree block with `start_tree <- true_tree; start_tree$edge.length <- rep_len(0.1, nrow(true_tree$edge))`. The harness explicitly tests `p(θ|y, T_true)`, which is the valid SBC target for continuous parameters under fixed topology. Committed with v7 harness changes. | `dev/red-team/heavy-tests/sbc.R:139` (also `sbc-warmup-trace.R:85`, `sbc-warmup-002-instrument.R:67`) — `for (e in rev(seq_len(nrow(edges))))` gives postorder traversal on a `TreeTools::Preorder` edge list. For every non-root edge, `states[pa]` is read before the parent node has been drawn (still `0` from `integer(...)` init). Result: tips are approximately independent Bernoullis biased toward state 0 along each incident edge — phylogenetic correlation is destroyed. Inference (correct JC pruning) explains the reduced correlation with a shorter tree length → posterior on `tree_length` is biased LOW → SBC ranks pile HIGH. **Quantitative signature (sim 15, seed 20262541, 8 tips, 28 binary chars, true topology):** Brent MLE_TL = 1.20 vs tl_true = 4.13 (11.6 nats gap); 30-seed sweep shows buggy median MLE/tl_true = 0.80 vs fixed 1.51. **Fix:** `rev(seq_len(...))` → `seq_len(...)` in all three files. See diagnosis note at `dev/red-team/heavy-tests/sbc-tl-mix-001-diagnosis.md` and `sbc-tl-mix-001-diagnose.R` / `sbc-tl-mix-001-confirm.R`. All SBC submissions v1–v5 contaminated for `tree_length`; v6 requeue with fix is the first clean run. |
| SBC-HARNESS-003 | MED | FIXED (2026-05-27) — convention now matches inference at all k | Bug | `.simJCchar` uses `exp(-kTrue * t)` rate but inference JC kernels use `exp(-kStates * t / (kStates − 1))` | `dev/red-team/heavy-tests/sbc.R:142` (`pSame <- 1 / kTrue + (1 - 1 / kTrue) * exp(-kTrue * t)`) vs `src/likelihood.cpp:85` (`arg = -kStates * t / (kStates - 1.0)`). Conventions agree at k=2 but diverge for k > 2: at k=3 the forward rate is 2× the inference rate; at k=20 it is ~19×. Causes posterior `tree_length` to overshoot forward truth → AD rank piles at the upper end. **Empirical signature** in v2 verdicts (job 17294905): `tree_length` PASSes in `MkNT_geometric` (kFloor=2, clamp k≤20 still keeps many chars at k=2-3) and `Mkp_beta_geometric` (small-k bias from Exp(1) priors on α, β); FAILs in `MkNT_logseries`, `Mkp_geometric`, `Mkp_empirical_geometric`, `Mkp_logseries` — all arms where k commonly exceeds 3. **Predicted by 17295308 result**: MkNT_geometric PASS, MkNT_logseries FAIL (tree_length p=3e-6) — confirmed in tasks 0/5 of that job. Fix: one-line change in `.simJCchar` — replace `exp(-kTrue * t)` with `exp(-kTrue * t / (kTrue - 1))`. Quick mode rerun to verify. Filed during the 2026-05-27 follow-up session after the mcmc-diagnostician agent's prediction table predicted MkNT_logseries PASS that did not materialise; refined by re-grep of `src/likelihood.cpp:85`. |
| SBC-PRIOR-CONFOUND-001 | — | **WITHDRAWN 2026-05-27** — diagnosis built on misread wide-format CSV (kPrime_pooled fills 5881 padded rows while tree_length fills only 200; averaging across all rows produced spurious MkNT-high / Mk'-low asymmetry). Real per-sim means from first 200 rows show tree_length biased uniformly HIGH across every arm in both v4 (means 43–58) and v5 (means 52–60). Asymmetric-mechanism story is invalid. New lead is SBC-WARMUP-001 below. | Bug | (withdrawn) SBC forward / inference kPrime parameterisation mismatch contaminating every parameter | Escalation of SBC-MASS-FAIL-001 after Hamilton v4 (job 17296284) collected 2026-05-27. v4 applied option (b) (restrict `kPrime_pooled` to kObs==2 chars) and SBC-HARNESS-003 (rate convention fix). Predicted outcome was 5/5 controls PASS, 1 EG-001 demonstration FAIL. **Actual v4 outcome: all 6 arms FAIL, none PASS.** Per-parameter pattern in `dev/red-team/heavy-tests/sbc-results-hamilton-v4/*/verdict.txt`: `tree_length` AD p = 3e-06 (AD floor) in **every arm** including MkNT_geometric (was PASS in v3 at p=0.003); `kPrime_pooled` AD p ≈ 1.5–2.6e-07 in every Mk' arm (kObs==2 filter did not fix it); `p` FAILs in `Mkp_geometric` at 3e-06 but borderline-PASSes in `Mkp_empirical_geometric` at 0.053 (per-arm sign matters less than the universal `tree_length` FAIL); `rate_log_sd` PASSes universally. **Direction-of-bias smoking gun**: tree_length mean ranks (uniform expected ≈ 33.5/67) are biased *high* in MkNT arms (MkNT_geometric=45.12, MkNT_logseries=51.59) and biased *very low* in Mk' arms (Mkp_geometric=3.17, Mkp_beta_geometric=2.71, Mkp_logseries=2.65, Mkp_empirical_geometric=4.97). **Mechanism**: forward draws characters under k=kTrue with kTrue distributed *independently of kObs*; the realised character has kObs ≤ kTrue (hidden states possible). For MkNT inference, `MkPrimeData(pd, knownStates = kObs)` fixes k=kObs (no kPrime marginalisation) — when kTrue > kObs the forward and inference are different models and inference under-estimates tree_length → truth ranks high. For Mk' inference, kPrime is marginalised over [kObs, ∞] with prior `kPrime - kObs ~ Geo(p)`; the *prior* on kPrime depends on kObs (not on a free latent state count), whereas the *forward* prior on kTrue is independent of kObs. The two joints differ already at the prior level for any char with kObs > 2 (~47% of chars at p~0.5 per the SBC-MASS-FAIL-001 diagnosis). Posterior kPrime is pulled toward the kObs-conditioned prior → over-estimated rate per branch → over-estimated tree_length → truth ranks low. Both biases share root cause. **Two parsimony-rejected alternatives** the user's prompt flagged: (i) L6 normaliser arithmetic for the plain geometric — rejected: L6 is `proofs/kprime-priors.md` watertight, and the universal MkNT tree_length FAIL is the same mechanism in a model where `p` is not even a parameter; (ii) `mh_p` sampler bug — rejected: same reason. SBC-HARNESS-003 itself is CORRECT (k=2 case is a no-op, k≥3 case aligns with `src/likelihood.cpp:85`); it didn't break tree_length, it just stopped masking the joint mismatch (in v3, an over-fast forward rate compensated some of the hidden-state bias). **Fix space**: do NOT touch inference (L6 priors are proven and integrate with the rest of the model). Three harness-side options: (α) **restrict forward to kTrue=2** (or filter to chars where the realised kObs==kTrue) — eliminates hidden states; the cost is that Mk' kPrime>2 cases are no longer exercised by SBC, but the L6 proof already covers them analytically; (β) **rejection-sample** so the forward joint matches inference: per char, simulate with proposed kTrue, observe kObs, accept iff (kTrue - kObs) ~ Geo(p) — expensive and finicky; (γ) **two-stage harness**: separate MkNT-style arms (k=kObs fixed, no kPrime) and Mk'-style arms (binary chars with kPrime audited via posterior on `kPrime - 2 = u` against Geo(p)), each clean by construction. Recommendation: **option (α)** for the next Hamilton submission — minimal harness churn, restores SBC validity, lets v5 controls actually demonstrate the predicted 5/6 PASS pattern. EG-001's empirical demonstration (currently `p` borderline 0.053 in `Mkp_empirical_geometric`) is now sitting on a contaminated joint and does not constitute clean evidence; revisit in v5 once the joint is clean. |
| SBC-WARMUP-001 | — | **REFUTED 2026-05-27** — discriminating trace check ruled out burn-in. | Bug | (refuted) hypothesised that universal `tree_length` HIGH bias was a sampler burn-in deficit | Driver and discriminating-trace plots at `dev/red-team/heavy-tests/sbc-warmup-trace.R` + `sbc-warmup-discriminating-traces.png`; raw traces in adjacent `.rds`. **Critical sim (sim 15, seed 20262528+13 = 20262541, tl_true=4.13 — small enough that data is informative)**: posterior on `tree_length` stationarises at TL ≈ 1.4-1.5 regardless of (i) start TL ∈ {0.1×14=1.4, 0.295×14=4.13, 1.0×14=14.0}, (ii) `N_WARM` ∈ {2000, 5000, 10000} (a 20000-warmup run separately exposes SBC-WARMUP-002 below), (iii) start topology (AdditionTree RF=10 vs `true_tree` RF=0), (iv) post-warmup iterations (6000 thin 60 vs 60000 thin 600 — same stationary TL), (v) `autoTune` ∈ {FALSE, TRUE}. Every variant produces rank = L (maximum) because the entire posterior sample sits below `tl_true`. Even with `start_edge = 1.0` (start TL=14, well above truth=4.13), the chain *descends* to TL≈1.5 and stays. Burn-in would have predicted the trace climbing toward truth when start is below truth, and descending toward truth when start is above; instead the chain settles at a value below all reasonable starts. Cross-check on v5 Mkp_geometric (200 sims): observed mean rank 53.36 vs prior-quantile-prediction 34.42 (predicted under "posterior ≡ prior" null); for the 52 sims with `tl_true ∈ (0, 25]` (where data is most informative), mean rank is 52.5/67 — these should rank LOW under either correct SBC (~33.5) or posterior≡prior (~6); instead they rank HIGH, confirming the posterior is systematically *below* truth even when the likelihood should pull it toward truth. **Therefore burn-in / start-state distance is not the mechanism.** Original fix candidates (a) AdditionTree edge lengths and (b) bump N_WARM are both refuted by the evidence above. **Next campaign step**: investigate the real mechanism in SBC-TL-MIX-001 below. |
| SBC-TL-MIX-001 | HIGH | FIXED (SBC-HARNESS-006 path bug; v8 requeue) — root cause was SBC-HARNESS-006: SLURM jobs ran a stale out-of-tree sbc.R that NEVER received the rev() fix. SBC-HARNESS-004's diagnosis of the rev() bug was correct, and the in-tree fix was correct; but the SLURM submit script invoked a different file, so v6+v7 still ran the buggy simulator. SBC-HARNESS-005's topology fix was also dead code on Hamilton for the same reason. Local in-tree re-run gives tree_length AD p=0.99 — clean PASS. v8 will be the first SLURM run that actually tests the fixed harness. | Bug | Posterior on `tree_length` is biased LOW relative to truth even on the true topology with informative data | Surfaced by the SBC-WARMUP-001 discriminating check (above). Quantitative gap on sim 15 (seed 20262541, 8 tips, 28 informative binary chars, true topology, `fixTopology = TRUE`): tl_true = 4.13 implies ≈ 124 expected substitutions across the matrix (30 chars × 4.13), whereas the chain's stationary mean TL ≈ 1.4 implies ≈ 42 — a 2.7× under-estimate. The bias is stable across 6000 and 60000 sampling iterations, `autoTune` ∈ {FALSE, TRUE}, true and AdditionTree-estimated topology, and starts ranging over 1.4–14. **Diagnosis 2026-05-27** (`dev/red-team/heavy-tests/sbc-tl-mix-001-diagnosis.md`, drivers `sbc-tl-mix-001-diagnose.R` + `sbc-tl-mix-001-confirm.R`): the forward simulator `.simJCchar` at `dev/red-team/heavy-tests/sbc.R:139` iterates `rev(seq_len(nrow(edges)))` — postorder on a `TreeTools::Preorder` edge list — so for every non-root edge it reads `states[pa]` before the parent state has been drawn (still `0` from `integer(...)`). Tips end up drawn approximately as independent Bernoullis biased toward state 0, destroying phylogenetic correlation and pulling the inference MLE downward. Discriminating evidence: (a) likelihood profile on sim 15, `rel_br` fixed at truth's normalised weights, `MkpLogLikelihood(... nCat=1, coding="variable")`: Brent MLE_TL = **1.199** (logL = -97.09), logL drops 11.6 nats to tl_true = 4.13. Same profile with `relabel = FALSE` (plain Mk) also peaks at 1.20 — not an Mk' issue. (b) Instrumented MCMC (autoTune=TRUE, 20k iters): `tree_length` accept = 0.44, `branch_lengths` = 0.60, `scale_tree_length` tuning = 1.61, 70/70 unique tree_length samples — sampler is healthy and faithfully drawing from the (mis-specified-by-harness) posterior. (c) Confirmation: re-simulating sim 15 with `for (e in seq_len(nrow(edges)))` (forward iteration) gives MLE_TL = **4.59** ≈ tl_true = 4.13; 30-seed sweep gives buggy median MLE/tl_true = 0.80 vs fixed 1.51 — buggy biased LOW, matching v5's `tree_length` posterior-below-truth signature. **Mechanisms ruled out:** sampler stuck (acceptance + scale healthy), edge prior pathology (Gamma logPrior monotone pulls TL *up* toward mode 25, not down), and FAST-EXP-001 (sim 15 has mean rt ≈ 0.6, far from rt ≲ 1e-12 regime). **Recommended fix (do NOT apply here):** one-line edit at `sbc.R:139` — drop the `rev()`. **Downstream:** v1–v5 SBC submissions are contaminated for `tree_length` (and any downstream rank stats that propagate the wrong joint); EG-001's borderline `p` p=0.053 in Mkp_empirical_geometric is suspect and must be re-evaluated on a v6 clean run. SBC-WARMUP-001 / SBC-WARMUP-002 / SBC-PRIOR-CONFOUND-001 stand as already-resolved/withdrawn separately. **Do NOT submit another Hamilton SBC array until this is fixed.** |
| SBC-WARMUP-002 | MEDIUM | FIXED 2026-05-27 | Bug | Adaptive move-weight scheduler crushes scalar-target moves under long warmup | While running the SBC-WARMUP-001 discriminating check at `N_WARM = 20000` (sim 1, seed 20262527), the kept `tree_length` trace had **1 unique value across 100 samples** while `rate_log_sd` had **51 unique values**. The chain is alive — just `tree_length` is frozen. **Mechanism (confirmed by instrumentation `dev/red-team/heavy-tests/sbc-warmup-002-trace.txt`):** the bug spec hypothesised "all 60 proposals reject"; the actual mechanism is the `log(dim)` term in the softmax score in `.AdaptMoveWeights`. With kPrime having `dim = nTrans ≈ 30`, `log(30) ≈ 3.4` of additive score advantage; at the annealed temperature `T = 0.5` this is an exp(6.8) ≈ 900× unnormalised weight differential. `tree_length` (dim=1) is annealed to `wMin = 0.01` despite a healthy ~36% acceptance rate and the cheapest per-call cost of any move (14 μs). `branch_lengths` (dim=1 beta_simplex on rel_br_lengths, 86% accept) lands at exactly `wMin = 0.01` for the same reason. **Fix:** added `scalarFloorMoves` / `wMinScalar = 0.02` to `.AdaptMoveWeights`. Mirrors the existing init-time scalar floor in `.BuildMoves`: dim=1 `scale` / `int_walk` / `gibbs_p` / `scale_p` / `logit_scale_p` / `kprime_alpha` / `kprime_beta` moves and all `joint_2d` moves get a 0.02 floor instead of the global 0.01. Final adapted weights surfaced on the result as `res$moveWeights` for diagnostics. **Evidence post-fix:** at warmup end (call 40), `tree_length` weight 0.0164 → 0.0200, `rate_log_sd` 0.0167 → 0.0200; unique TL values 1 → 3 in the same reproducer. Residual freeze is **SBC-TL-MIX-001 territory** (harness `.simJCchar` postorder bug), not this finding. **Follow-up resolved (2026-05-27):** `branch_lengths` (type="beta_simplex", dim=1) also hit the global floor (w=0.0100 at call 40 in the post-fix trace). **`"beta_simplex"` has been added to `.kScalarFloorTypes` and the init-time `scalarTypes` in `.BuildMoves`** (both must stay in sync per the L844 comment). Reasoning: beta_simplex is the cheapest, highest-acceptance edge-length proposal (86% acc, 45 μs) and guaranteeing it ≥2% weight prevents starvation of the primary single-edge MH move regardless of what companion moves are active. **Correction to original "out of scope" note:** the default config has `dirichletBranch=TRUE` and `localDirichlet=TRUE`, so `dirichlet_branch` (dim=5) and `local_dirichlet` (dim=6) are present and also touching `rel_br_lengths` — making the case for the floor less urgent than "sole move" framing implied, but not removing it. Per-target floor logic (sum over all moves touching a target) was considered and deferred — cleaner in principle but invasive; type-set extension covers the practical case. `"dirichlet_simplex"` is a phantom type in this codebase (actual types are `local_dirichlet_simplex` and `dirichlet_simplex_class_w`, both dim>1 in normal use; the `moveDim == 1L` guard already filters them). Regression tests at `tests/testthat/test-m092-adaptive-scheduler.R::"SBC-WARMUP-002-BRANCH ..."` (unit) and `"SBC-WARMUP-002: scalar moves keep ≥wMinScalar ..."` (end-to-end, adds `branch_lengths ≥ 0.02` assertion). |
| SBC-HARNESS-001 | MED | OPEN | Bug | D2(b) subtree-swap n=6: NNI positive control fails max/min PASS rule despite chi² p=0.45 | Hamilton job 17294906, post-patch run 2026-05-27. `dev/red-team/heavy-tests/subtree-swap-db.R` at n=6 (105 buckets) × 1M iter × thin 200 = 5000 samples → NNI control chi² p=0.4514, max/min=2.57 → FAIL by PASS-rule (max/min < 2.0). The chi² p-value is comfortably PASS at α=0.005, but the max/min rule used in `subtree-swap-db.R` is too strict for 105 buckets with E[count] ≈ 48. Either (a) the PASS-rule should switch on n_topos (loosen max/min for n≥6) or (b) PASS-rule should defer entirely to chi² above some bucket count. As written, n=6 results are inconclusive — `WARN: NNI positive control failed`. Refine harness before drawing conclusions about SWAP-001/002 at n≥6. At n=5 the same harness PASSed for control and both swap variants (job 17289638): `gibbs_subtree_swap` chi² p=0.5862, max/min=1.24; `weighted_subtree_swap` chi² p=0.8062, max/min=1.17 — **SWAP-001 and SWAP-002 not detected at n=5 with full statistical power** but math-prover concern stands until n≥6 test passes a fixed harness. Output captured to `dev/red-team/heavy-tests/swap-db-results/swap-db-n6_17294906.out`. |
