# Red-team campaign — 2026-05-26

**Goal.** Replace nine rounds of focused-but-narrow audits with formal proofs and
heavy tests, so MkPrime's mathematical output is analytically defensible end-to-end.

**Plan.** [we-need-to-do-frolicking-sutherland.md](../../../../.claude/plans/we-need-to-do-frolicking-sutherland.md).

**Agent profiles introduced.** `~/.claude/agents/{math-prover,mcmc-diagnostician,numerical-auditor}.md`.

**Scope.** MkNT + Mk' (full) + diagnostics layer.

---

## Wave 1 — math foundations

| Lane | Agent | Status | Artefact | Patch | One-line |
|---|---|---|---|---|---|
| L1 F81-Het collapse | math-prover | **watertight w/ caveats** | `proofs/f81-collapse.md` | — | JC(k) lumpability collapse (PR #2 b99b0ae/8d4638e/a966d8b) correct vs Kemeny–Snell strong lumpability. Three new kernels match reference line-by-line; fused ascertainment correct (outConstProb = sum_eff, no caller-side `k_F` needed). F81-Het collapse explicitly deferred per commits — §6 of proof records what F81-Het would need (constant π on lumped class). No patch. |
| L2 Hastings continuous | math-prover | **watertight** | `proofs/hastings-continuous.md` | — | All 6 continuous proposal families (Bactrian M-118 + 2D M-120, logit-MH p, beta-simplex, Dirichlet-simplex random/local, bounded-int) match first-principles. Multiplicative Jacobians, 2D joint Jacobian, density-ratio cancellations all verified. M-120 symmetry confirmed numerically (ρ=0.6 → 0.599). No patch. |
| L3 Hastings tree moves | math-prover | dispatched | `proofs/hastings-tree-moves.md` | — | — |
| L4 Mk' relabelling | math-prover | **watertight w/ caveats** | `proofs/relabelling-correction.md` | — | `src/corrections.cpp:44` matches `log(k'!/(k'−kObs)!)`; numerical brute-force on 3-tip star confirms ratios `k'(k'−1)`. Added to log-likelihood only — no double count vs log-prior. Caveats: (1) F81-Het call sites apply correction unconditionally — needs Het-bin exchangeability check (folds into L1); (2) relabelling↔ascertainment commutation heuristic, not discharged — folds into L5. |
| L7 ACRV discretisation | math-prover | **watertight** | `proofs/acrv.md` | — | mean-1 holds as algebraic identity post-rescale; impl matches at `R/acrv.R:39`, `src/mcmc_likelihood.cpp:85`, `src/gibbs_partial_cl.h:195`, `src/node_cl_cache.h:51`. Spec said `src/acrv.cpp` — file doesn't exist, kernels live elsewhere; agent cited correct locations. Confirms `feedback_acrv_saturation_masks_state_drift` mechanism (top rate ceiling at K). |

*Wave 1 dispatched 2026-05-26: 5 parallel general-purpose Opus agents, each in an
isolated worktree, each briefed to act in the `math-prover` role with the role spec
inlined. Awaiting completion notifications.*

## Wave 2 — derived analyses

| Lane | Agent | Status | Artefact | Patch | One-line |
|---|---|---|---|---|---|
| L5 Ascertainment | math-prover | **watertight (mixed)** | `proofs/ascertainment.md` | `L5-ascertainment.patch` (7 lines) | JC(k) const+sing, F81 const, MkN const+sing all watertight. F81 sing watertight w/ caveats. **L4 caveat (2) closed**: Mk' falling-factorial commutes exactly with both ascertainment denominators (C and G are σ-invariant subsets; L_fels permutation-symmetric). **M-052 (F81-Het sing)**: open / production-gated, deferred. **`mcmc.cpp:1092`**: confirmed = LIKE-001; closed-form `p += singleton_site_prob_jc(...)` given. Patch produced for `const_site_prob_for_k`. |
| L6 k'-priors | math-prover | **mixed** | `proofs/kprime-priors.md` | — (fix non-trivial, deferred) | Geometric / Beta-Geometric watertight (normaliser ≡ 1). **EG-001 confirmed** (missing per-char Z_i(p) biases up to −4.5 nats at p=0.9, kObs=7). **NEW LS-001**: logseries has the same Z_i(c) bug — currently latent because c is fixed, hazard if ever sampled. **R5-4 out-of-lane**: it's an empirical claim about post-relabel phi medians, not closeable analytically — needs a sim3 rerun against corrected `HasBipartSplits`. No falling-factorial leak in `LogPrior`. |
| N1 fast_exp + eigendecomp | numerical-auditor | **stable-w-caveats; new bug** | `numerical/fast-exp-conditioning.md` + driver | `N1-fast-exp.patch` (24 sites, `expm1` substitution) | `fast_neg_exp` itself stable (8.6e-15 max ε_rel over 50k samples, vs the 1e-10 audit threshold). **NEW FAST-EXP-001**: `p_diff = (1/k) − (1/k)·E` and F81 `1 − d` suffer catastrophic cancellation at small `rt`. Worst measured ε_rel = **0.39** at rt=1e-15; ε_rel = 3e-5 at the production-realistic rt=1e-12. Reachable under default `rateLogSd ~ Gamma(1,1)` when σ ≳ 7 meets short branches — posterior tail, not bounded out. Patch replaces `1 - MKP_EXP(arg)` with `-std::expm1(arg)`. |
| N2 Felsenstein underflow | numerical-auditor | **stable-w-caveats; brief refuted** | `numerical/pruning-underflow.md` + driver | `N2-pruning-underflow.patch` (16 sites, **cosmetic**, not applied) | Pruning kernels lack rescaling but `site_lik ≤ 0` guards return `R_NegInf` — defensive, not silent corruption. Reachable for caterpillars depth ≥ 64 w/ branches ≤ 1e-12, or depth ≥ 512 w/ branches ≤ 0.1. Worst ε_rel = 1.84e-16 (~1 ULP) vs pure-R reference across 5×7+4×4 grid. **Orchestrator hypothesis disproven**: `log(1-p) → log1p(-p)` is bit-equivalent in IEEE 754 across all reachable p (Experiment 6: zero diff at p = 1 − 10⁻ⁱ, i=1..16). Real precision loss is upstream in computing `p_const`, which `log1p` cannot recover. |

*Wave 2 dispatched after Wave 1 commit (main HEAD = 34a2607). Subagent worktrees
branch from new HEAD and thus include Wave 1 proofs.*

## Wave 3 — diagnostics & calibration

| Lane | Agent | Status | Artefact | Patch | One-line |
|---|---|---|---|---|---|
| D1 SBC MkNT+Mk' | mcmc-diagnostician | **harness ready (6 arms)** | `heavy-tests/sbc.{R,md,sh}` | — | Quick mode 4.7 s (budget ≤ 60 s), 6/6 arms execute, all sims complete. Arms: `MkNT_geometric`, `Mkp_geometric`, `Mkp_beta_geometric`, `Mkp_empirical_geometric` (predicted FAIL — EG-001 regression demo), `Mkp_logseries`, `MkNT_logseries`. Pass criterion: Anderson–Darling with Bonferroni p > 0.001/K_arm. Topology rank intentionally excluded (Talts is for continuous params; bucketing would smuggle in a threshold — verified via advisor() reconcile). Full-scale 200 sims × 6000 iter × L=100 = ~50 CPU-hours on Hamilton (6 array tasks, 8h × 8GB × 1 CPU). |
| D2 TreeESS rooting **+ SWAP empirical** | mcmc-diagnostician | dispatched | `heavy-tests/{tree-ess-rooting,subtree-swap-db}.R` + joint companion | — | extended brief: also runs β=0 chi-squared for `GibbsSubtreeSwap` and `WeightedSubtreeSwap` — could upgrade SWAP-001/002 from OPEN to CONFIRMED |
| D3 ESS/R-hat stress | mcmc-diagnostician | **harness ready; CONV-002 confirmed live** | `heavy-tests/ess-rhat-stress.{R,md}` | `D3-ess-rhat-stress.patch` (13 lines, R/Convergence.R::.ComputeRhat tail-equalises before `cbind`) | Quick mode 1.6 s (budget ≤ 90 s) — 14/15 PASS. AR(1) at ρ ∈ {0.1, 0.5, 0.9, 0.99, 0.999} × N ∈ {1e3, 1e4, 1e5} vs N(1−ρ)/(1+ρ); AR(2); bimodal w/ pinned modes; near-non-stationary RW; Pólya-urn sticky; CONV-001 family regression (constant chain → NA/Inf, not falsely-low Rhat); CONV-002 regression. **CONV-002 confirms live**: `R/Convergence.R:249-251` emits the predicted recycle warning. Currently latent (production runs length-equal) but adaptive truncation or partial run drops would expose it. Probe row also hints at potential `ESS-CAP-001` (white-noise small N) — needs follow-up. |
| N3 Partial-CL cache coherence | numerical-auditor | **harness ready; LIKE-001 reproduced** | `numerical/cache-coherence.md` + driver | — (no N3-specific patch; fix owned by L5) | Quick mode (~0.4 s): `coding="variable"` 0/86 moves mismatched, max \|partial − full\| = 4.26e-14 (~2 ulp on 30-nat logL). `coding="informative"` 94/94 moves mismatched (NNI 30/30, BS 29/29, Dir 35/35), max = **2.20 nats** — matches the 1.5–1.8 nat LIKE-001 signature quantitatively. C++ already cross-checks via `src/mcmc.cpp:4608-4727` accumulating `diagMaxDiff` + per-move-type counters; harness just reads them back via `pcl_diag.txt`. Harness doubles as the LIKE-001 regression test once the L5 patch lands. |

*Wave 3 dispatched after Wave 2 commit (main HEAD = 9f759d1). Prompts include
explicit `git show $(git rev-parse main):path` instruction to defend against the
Wave-2 observation that worktree base lags orchestrator HEAD.*

---

## Orchestrator decisions log

(One entry per autonomous decision the orchestrator makes during execution —
worktree-base verifications, conflicts integrating returned artefacts, advisor()
calls, anything the human should know after the fact.)

- **2026-05-26, post-Wave 1 integration.** All 5 Wave 1 worktree bases verified = main HEAD `2bc4da7` (matches `feedback_verify_agent_base` requirement). Four worktrees retained the proof artefact; L1's worktree was auto-cleaned by the harness (no tracked changes) but the agent had written `f81-collapse.md` to the *main repo path* directly. File contents inspected and sensible — committed as 34a2607 on main. No advisor() escalation needed; verdicts (4 watertight w/ or w/o caveats + 1 with 2 OPEN findings) were clear and source-grounded.

---

## Agent profile observations (feedback for future revisions of `~/.claude/agents/`)

A running log of what the profiles got right, missed, or should encode more strongly,
gathered from agent outputs as they return. Used to revise the profile files at
campaign end.

### From Wave 1

- **Path resolution in worktrees.** L1 (F81 collapse) wrote `dev/red-team/proofs/f81-collapse.md` to the *main repo path* rather than its own worktree's path. Cause unclear (agent may have resolved the path against `$PWD` of the harness rather than its worktree root). The math-prover profile should make this explicit: "anchor relative paths against `git rev-parse --show-toplevel` of your worktree, never the parent shell's CWD". Tied to `feedback_verify_agent_base`: worktree base verification is necessary but not sufficient — write-target verification is also needed.

- **Brief errata are handled gracefully when the role spec encourages it.** L7's brief said `src/acrv.cpp` (a non-existent file); the agent located the real implementation (`R/acrv.R:39` plus three C++ sites) and cited it. This matches the "cite every implementation claim with file:line" rule in the math-prover profile. Worth elevating to an explicit instruction: "if the brief names a file that doesn't exist, locate the actual implementation, cite both the wrong brief and the real path, and proceed."

- **Caveat-flagging convention works well for orchestration.** L4 produced two caveats (F81-Het exchangeability → L1; relabelling↔ascertainment commutation → L5) tagged for downstream lanes. This is exactly the pattern the orchestrator needs to weave waves together. Worth elevating in the math-prover profile: "when a step would require a separate proof, label it explicitly as a follow-up and indicate which lane / role should pick it up."

- **The <200-word return summary is the right cadence.** All five agents stayed within budget; orchestrator could update the campaign log from the return message alone, without reading the full proof. Keep.

- **Open-question status is more useful than forced-watertight.** L3's two OPEN findings (SWAP-001 / SWAP-002) with explicit empirical follow-ups (β=0 uniformity test) are exactly the right shape. The profile already says "do not handwave; open question is better than false watertight" — confirmed working as intended.

### From Wave 2

- **Worktree base lags the orchestrator's HEAD.** L6 reported its worktree base was `2bc4da7` (pre-Wave 1) while orchestrator main HEAD had advanced to `34a2607`. The agent worked around it correctly by using `git show 34a2607:dev/red-team/proofs/relabelling-correction.md`, but the Wave 2 dispatch prompt told agents Wave 1 artefacts would be "in `dev/red-team/proofs/` in your worktree". That instruction was wrong. Profile fix: tell agents to (a) check `git merge-base HEAD main`, (b) if main is ahead, read newer artefacts via `git show <main-hash>:path`. Also confirms `feedback_verify_agent_base` is still load-bearing — worktrees branch off the original main HEAD at *agent dispatch time*, not at *agent run time*.

- **Out-of-lane verdict is a useful escape valve.** L6 was asked to re-derive R5-4, but R5-4 turned out to be an empirical claim (post-relabel phi-median distribution dependent on a downstream scoring function), not a math claim. The agent didn't force-fit a proof — it declared R5-4 out-of-lane and explained why. Worth elevating in the math-prover profile: "if the brief asks you to prove something that is fundamentally empirical, say so and identify the right type of follow-up (heavy test / simulation rerun / etc.)."

- **Identifying latent-but-not-active bugs.** L6 surfaced LS-001 as a logseries-version of EG-001 that is currently *latent* because `kprimeLogseriesC` is fixed. The agent correctly noted the bias cancels in MH ratios at fixed `c` but is a hazard if `c` becomes sampled. This is exactly the kind of "the audit reaches further than the current code paths" finding that justifies math-prover work — confirming the profile's emphasis on first-principles derivation pays off.

- **Agent pushed back on the orchestrator's brief with primary evidence.** N2's brief instructed it to audit `log(1−p) → log1p(−p)` as a likely cancellation site. The agent ran the experiment (Experiment 6: zero difference at p = 1 − 10⁻ⁱ for i=1..16) and found the two formulations are *bit-equivalent in IEEE 754 doubles* across the reachable range — once `p` is stored as a double, `1.0 − p` rounds once and both formulations consume that rounded value. The real precision loss is upstream in computing `p_const`. The agent captured the 16-site patch but marked it cosmetic and did not recommend application. **This is exactly the behaviour the role spec wants** — "if you have primary-source evidence that contradicts a specific claim … adapt". The numerical-auditor profile's "quantify, don't qualify" instruction enabled this. Worth elevating in the profile: "if the brief hypothesises a numerical issue, verify it numerically before working on the fix — the brief may be wrong."

- **`fast_neg_exp` audit reframed the search.** N1's brief asked about the matrix-exponential primitive itself. The primitive turned out to be stable; the actual bug (FAST-EXP-001) was in the *algebra around* it — `(1/k) - (1/k)·exp(arg)` and F81's `1 − d` are cancellation-prone at small `rt`, regardless of whether `exp` is computed exactly. The agent followed the conditioning all the way through. The profile's instruction "conditioning analysis first, stress test second" paid off.

### From Wave 3

- **Quick-mode tests must be self-calibrating about their own statistical power.** D2 ran a chi² uniformity test on `GibbsSubtreeSwap` and `WeightedSubtreeSwap` at 5-tip × 2000 samples and got p=0.979 / p=0.749 — apparent PASS. The agent correctly refused to declare SWAP-001/SWAP-002 refuted: "at n=5 with 2000 samples, partner-set asymmetries may be too small to surface." This restraint is exactly what the role spec asks for ("do not silently widen tolerances to make a test pass"). Worth elevating in the mcmc-diagnostician profile: "if a quick-mode test is structurally underpowered to detect the bug it targets, say so explicitly; never report PASS without a power statement."

- **C++-side diagnostic counters can be the heavy test.** N3 found that `src/mcmc.cpp` already cross-checks every partial-CL move against full-eval (`diagMaxDiff`, per-move-type mismatch counters at `src/mcmc.cpp:4608-4727`). The N3 harness is essentially a thin reader of `pcl_diag.txt`. This made the harness ~0.4s in quick mode. Lesson: before designing a heavy test from scratch, check whether the production code already has the diagnostic plumbing and just isn't surfacing it. Worth adding to the mcmc-diagnostician and numerical-auditor profiles.

- **advisor() reconcile worked correctly on a design decision.** D1 called advisor() to reconcile whether topology rank could be included in SBC. Outcome: rank-uniformity is for continuous parameters; bucketing RF distance would smuggle in an arbitrary threshold; topology was correctly excluded. The role spec already mentions advisor() implicitly (via the inline instructions); the mcmc-diagnostician profile could make it explicit: "for design decisions that materially affect what your test is testing, advisor() before committing."

- **Multiple agents reported `R/RcppExports.R` / `src/RcppExports.cpp` regenerated** by `pkgload::load_all()` in their worktrees. These are auto-generated files; they appear modified in worktree status but the agent didn't touch them. The orchestrator's integration step must NOT stage these. Worth noting in the harness-side documentation: agents touching the package via `load_all()` will see these regenerated. Profile fix: explicitly tell agents to expect this and not flag it as a worktree-state concern.

- **Profile-feedback observation aggregated**: the three profiles' "trivial fix → patch file → `git restore`" policy has worked cleanly in 5/5 cases where it triggered. No agent left the source tree dirty. Keep.

---

## Campaign closing summary (orchestrator, 2026-05-26)

**Status:** all 13 lanes returned. Wave 1 commit `34a2607`; Wave 2 commit `9f759d1`;
Wave 3 commit `a37c3a6`. Profile revisions to `~/.claude/agents/{math-prover,
mcmc-diagnostician, numerical-auditor}.md` applied based on accumulated Wave 1–3
observations.

**What is now watertight (in the gold-standard sense — written proof, not just
"the test passes"):**

- JC(k) lumpability collapse (L1) — the PR #2 hot-path kernels match Kemeny–Snell
  strong lumpability against `pruning_jc_collapsed` line-by-line. F81-Het collapse
  is deferred per commits; design stub on `wip/f81-het-collapse-design`.
- All six continuous-proposal Hastings ratios (L2) — Bactrian M-118 / M-120,
  logit-MH p, beta-simplex, Dirichlet-simplex (random + local), bounded-int walk.
- All seven tree-move Hastings ratios (L3) — NNI, plain SPR, plain TBR, plain
  subtree-swap, pSPR, GibbsSPR, WeightedSPR — modulo the two subtree-swap variants
  whose detailed balance is still empirically open (SWAP-001 / SWAP-002).
- Mk' relabelling correction (L4) — falling-factorial verified against numerical
  brute-force enumeration; commutes with both ascertainment denominators (L5).
- Ascertainment site probabilities for JC / F81 / MkN under variable and informative
  coding (L5) — closes the L4 commutation caveat; gives the closed-form fix for
  LIKE-001 in `const_site_prob_for_k`.
- ACRV mean-1 invariant (L7) — algebraic identity for the quantile-midpoint
  discrete-lognormal post-rescale.
- k'-prior normalisations for Geometric and Beta-Geometric (L6); EG and Logseries
  confirmed mis-normalised in the same arithmetic family (EG-001 + new LS-001).
- TreeESS root-invariance (D2 sub-harness a) — bit-exact under random re-rooting.

**What remains empirically open** (heavy-test artefacts in place, full-scale runs
queued for Hamilton or follow-up):

- SWAP-001 / SWAP-002 — empirical test at meaningful scale (1M iter / n ≥ 6).
- EG-001 SBC arm — expected FAIL under D1 full-scale run; will quantify the bias.
- R5-4 (`project_pi0_audit`) — needs sim3 rerun against corrected `HasBipartSplits`.
- ESS-CAP-INFO-1 — needs follow-up drill-down by an mcmc-diagnostician.

**Patches awaiting human review** (none auto-applied — `dev/red-team/patches/`):

| Patch | Severity | Bug | LOC |
|---|---|---|---|
| `L5-ascertainment.patch` | HIGH | LIKE-001 root cause (JC singleton under informative coding) — applied to `const_site_prob_for_k`; partial-CL sites at `src/node_cl_cache.h:697,703,716` need analogous one-line addition | 7 + (3 analogous) |
| `N1-fast-exp.patch` | HIGH | FAST-EXP-001 (`1 − exp(arg)` cancellation at small rt) — substitutes `-std::expm1(arg)` at 24 sites | 24 |
| `D3-ess-rhat-stress.patch` | MED | CONV-002 (`.ComputeRhat` cbind-recycle on unequal-length per-run matrices) | 13 |
| `N2-pruning-underflow.patch` | INFO | Cosmetic only — `log(1−p) → log1p(−p)` is bit-equivalent in IEEE 754; do not apply | 16 |

**Heavy tests ready for Hamilton** (`dev/red-team/heavy-tests/` and
`dev/red-team/numerical/`, each with quick-mode demonstrated locally):

- `sbc.R` + `sbc-hamilton.sh` — 6-arm SBC (MkNT × {geometric, logseries}; Mk' × {geometric, beta_geometric, empirical_geometric, logseries}); ~50 CPU-hours.
- `subtree-swap-db.R` — full-scale chi² on `GibbsSubtreeSwap` and `WeightedSubtreeSwap` at 1M iter or n=6/7 to close SWAP-001/002.
- `tree-ess-rooting.R` — already passes locally; Hamilton run is for higher-N posteriors if desired.
- `ess-rhat-stress.R` — full grid for AR(p), bimodal, sticky, near-non-stationary, constant-chain regression.
- `cache-coherence-driver.R` — full 10⁴-move drift characterisation under variable and informative coding; doubles as the LIKE-001 regression test once L5 patch applies.
- `fast-exp-driver.{R,py}` — already-collected results in `numerical/fast-exp-results/`; Hamilton not required.
- `pruning-driver.R` — already-collected results in `numerical/pruning-results/`.

**Out of scope for this campaign** (deferred to follow-up sessions):

- Applying patches to source. None applied; all in `dev/red-team/patches/` awaiting
  human review per the campaign's "isolation + fix-as-artefact" policy.
- Submitting heavy tests to Hamilton. Submit scripts ready (`sbc-hamilton.sh`); the
  user should sequence these with the existing `/hand-off` and `hamilton-hpc` skills.
- Building an F81-Het collapse implementation. Math foundation is in `f81-collapse.md`
  §6, which states the additional condition (constant π on the lumped class).

**Agent profile revisions** (applied 2026-05-26):

- `math-prover.md`: path anchoring against `git rev-parse --show-toplevel`;
  `git show` access to lagging-base artefacts; brief-errata recovery rule;
  out-of-lane verdict; downstream-lane caveat tagging; RcppExports note.
- `mcmc-diagnostician.md`: same path / git-show / RcppExports notes; "check
  existing diagnostic plumbing before parallel-implementing" rule;
  "PASS-without-power is worse than INCONCLUSIVE" rule; advisor() on design
  decisions.
- `numerical-auditor.md`: same path / git-show / RcppExports notes;
  "verify the brief's numerical hypothesis empirically before fixing it" rule;
  "follow conditioning through the enclosing algebra" rule; "cosmetic patches
  must be labelled so" rule.

These revisions are durable — they will inform the next campaign without
re-paying the discovery cost from this one.
