# Red-team campaign — 2026-05-26 (live)

**Goal.** Replace nine rounds of focused-but-narrow audits with formal proofs and
heavy tests, so MkPrime's mathematical output is analytically defensible end-to-end.

**Plan.** [we-need-to-do-frolicking-sutherland.md](../../../../.claude/plans/we-need-to-do-frolicking-sutherland.md).

---

## Status snapshot (2026-05-28, post-mixed-SBC PASS)

**A3 mixed-SBC (job 17302921) — PASS.** New harness
`dev/red-team/heavy-tests/sbc-mixed.R` exercises the
`neoScale = r/(1+r)·n/n_neo` / `transScale = 1/(1+r)·n/n_trans` algebra
end-to-end on a 4-neo + 8-trans dataset (rejection-sampled to keep
partition counts stable across forward sim and inference). 200/200 sims
completed; all four monitored params PASS at Bonferroni/4
(p > 0.00025): `tree_length` 0.357, **`rate_neo` 0.116**, `rate_loss`
0.653, `rate_log_sd` 0.816. Combined with the v10 trans-only PASS,
forward-prior calibration is now established on *both* sides of the
partition-rate algebra. Verdict: `dev/red-team/sbc-results-mixed/`.

## Earlier snapshot (2026-05-28, post-v10 PASS)

**v10 (job 17302830) — ALL 6 ARMS PASS.** SBC pass bar (tree_length +
rate_log_sd raw p > 0.01) met by every arm; min p = 0.070, max p = 0.94.
This validates the partition-rate fix (a188401) and joint_tl_rn move
(e08d328) on trans-only datasets — no regression in tree_length or
rate_log_sd posterior calibration. `kPrime_pooled` and `p` are
permanently excluded as structurally untestable in this prior (see
SBC-KPRIME-STRUCTURAL). The campaign's primary calibration goal is met.

Remaining open items (now lower-priority): A2 neo_joint fate
(superseded — dropped after correlation measurement, see
`dev/notes/2026-05-27-neo-joint-fate.md`); SWAP-001/002 after
SBC-HARNESS-001 fix. LIKE-001 partial-CL singleton ascertainment now
closed (interim guard; full pseudo-CL singleton implementation deferred).

## Standing constraints (load-bearing for future work)

**Partition-rate normalisation (RB-style) merged** as `a188401` (F1 cache
fix in `9e68cad`). Symmetric scaling `neoScale = r/(1+r) · n/n_neo`,
`transScale = 1/(1+r) · n/n_trans` is applied at every site.
`tree_length` and `rate_neo` posteriors on mixed (neo+trans) datasets
are **not comparable** to pre-patch runs. Trans-only and Casali
workloads are bit-identical.

**SBC suite restricted to tree_length + rate_log_sd.** `kPrime_pooled`
and `p` are structurally untestable within the inference prior
`kPrime ~ kObs + Geo(p)` (data-dependent floor). v9 falsified the
alleged kSim=2 → p-Gibbs collapse hypothesis; v10 reverts to kSim=2 and
excludes both rank tests. See **SBC-KPRIME-STRUCTURAL** (`findings.md`).

**Submit script must invoke `${SRC}/dev/red-team/heavy-tests/sbc.R`.**
v3–v7 silently ran a stale `${RT}/heavy-tests/sbc.R` scratch copy
(SBC-HARNESS-006). v8 onward use the source-controlled path.

---

## Next actions (ordered)

1. **Relax `subtree-swap-db.R` PASS rule** (SBC-HARNESS-001) so the n=6
   NNI control passes on chi² alone above ~50 buckets; resubmit n=6 to
   surface SWAP-001 / SWAP-002 at meaningful scale.
2. **A2: Decide neo_joint fate** — harvest cor(log rate_loss, log rate_neo),
   cor(log T, log rate_neo), cor(log T, log rate_loss) from mixed-arm
   warmup to see whether neo_joint (case 18) is still useful post
   partition-rate-fix.

---

## Patch ledger

| Patch | Severity | Bug | Status | Commit |
|---|---|---|---|---|
| `L5-ascertainment.patch` | HIGH | LIKE-001 — JC/MkN singleton ascertainment under informative coding (full-eval path) | **APPLIED** | `59276ad` |
| LIKE-001 partial-CL guard | HIGH | Gibbs SPR / Gibbs subtree-swap pseudo-CL path dropped singleton term — fall back to `_full` under `codingType == 2` | **APPLIED** | pending |
| `N1-fast-exp.patch` (24 sites) | HIGH | FAST-EXP-001 — `1 − exp(arg)` cancellation at small `rt` | **APPLIED** | `59276ad` |
| `D3-ess-rhat-stress.patch` | MED | CONV-002 — `.ComputeRhat` `cbind`-recycle on unequal-length runs | **APPLIED** | `59276ad` |
| `N2-pruning-underflow.patch` | INFO | `log(1−p)` rewrite — bit-equivalent in IEEE 754 | **DO NOT APPLY (cosmetic)** | — |
| Partition-rate RB fix | HIGH | Asymmetric neoScale/transScale formula; rate_neo affecting neo only | **APPLIED** (incl. F1 partLogLik cache fix) | `a188401` (merge), `9e68cad` (F1), `c739d0c` (initial) |

Patch files retained in `dev/red-team/patches/` as regression-test reference.

---

## Hamilton state (latest)

```
/nobackup/pjjg18/mkp-study/red-team/
  mkp-source/                       # needs resync to post-a188401
  lib/                              # stale (pre-partition-rate-fix)
  heavy-tests/sbc.R                 # STALE SCRATCH COPY — root cause of SBC-HARNESS-006
                                    # ACTION: replace with symlink to ${SRC}/dev/red-team/heavy-tests/sbc.R
  results/sbc/                      # stale; all v1-v8 results invalid on mixed datasets
```

| Job | Scope | State | Collected to | Notes |
|---|---|---|---|---|
| 17301890 (array 0-5) | first clean submit-path run | COMPLETED | `sbc-results-hamilton-v8/` | **v8 PASS** tree_length+rate_log_sd all arms; kPrime/p FAIL (structural). |
| 17302830 (array 0-5) | v10 — kSim=2 all arms; kPrime/p excluded; pre-built before sbatch | **COMPLETED 2026-05-28: ALL 6 ARMS PASS** | `sbc-v10/` | Pass bar (raw p > 0.01) met in all 6 arms; min p = 0.070, max p = 0.94. Validates partition-rate fix (a188401) + joint_tl_rn (e08d328). |
| 17302921 (mixed-SBC) | A3 — mixed 4-neo + 8-trans dataset | **COMPLETED 2026-05-28: PASS** | `sbc-results-mixed/` | 200/200 sims; tree_length 0.357, rate_neo 0.116, rate_loss 0.653, rate_log_sd 0.816 (all > 0.00025 Bonferroni/4). |

Older / superseded jobs (v1–v7, v9) — see git log; results are stale on
mixed datasets (partition-rate fix changes semantics). Local result dirs
purged in `5207c77`; Hamilton purge still pending.

---

## Open findings (one-liner; full detail in `findings.md`)

- **SBC-BUILD-RACE-001** (MED, OPEN with workaround) — 6 concurrent `devtools::load_all` calls race on `${SRC}/src/`, corrupting MkPrime.so. Workaround: pre-build once before sbatch (documented in `submit-sbc.sh`).
- **SBC-HARNESS-001** (MED, OPEN) — n=6 swap-db PASS rule too strict (chi² passes but max/min fails NNI control).
- **SBC-HARNESS-002** (LOW, OPEN) — array tasks race-overwrite top-level `verdict.txt`.
- **SWAP-001 / SWAP-002** (HIGH, OPEN; n=5 inconclusive) — need n=6 after SBC-HARNESS-001 fix.
- **LIKE-001** (HIGH, **FIXED**) — singleton ascertainment under `coding="informative"`. Full-eval path patched in `59276ad` (`L5-ascertainment.patch`); partial-CL sites in `cache_total_loglik` patched with `singleton_site_prob_*` additions; `gibbs_spr_impl` / `gibbs_subtree_swap_impl` guarded to fall back to `_full` variants under `codingType == 2` (math-prover option 3 interim — option 2 `evaluate_singleton_prob` deferred pending profiling). Regression tests in `tests/testthat/test-node-cl-cache.R::"LIKE-001 ..."`.
- **EG-001** (CONFIRMED math) — L6 proof stands.
- **LS-001** (MED-latent) — logseries truncation normaliser; latent until `c` is sampled.
- **R5-4** (OUT-OF-LANE) — needs sim3 rerun against corrected `HasBipartSplits`.
- **ESS-CAP-INFO-1** (INFO) — follow-up drill-down by mcmc-diagnostician.
- **F81-Het collapse** (HOLDS-PARTIAL design at `proofs/f81-het-collapse.md`) — do **not** implement before profiling Het runs show kernel-bound.
- **joint_tl_rn A2 (OPEN)** — neo_joint fate depends on correlations from mixed-arm warmup.

---

## Lane → artefact index

| Lane | Role | Artefact | Verdict |
|---|---|---|---|
| L1 F81-Het collapse | math-prover | `proofs/f81-collapse.md` | watertight (collapsed kernels); §6 stub for F81-Het |
| L2 Hastings continuous | math-prover | `proofs/hastings-continuous.md` | watertight (6 families) |
| L3 Hastings tree moves | math-prover | `proofs/hastings-tree-moves.md` | watertight (7 moves); SWAP-001/002 open |
| L4 Mk' relabelling | math-prover | `proofs/relabelling-correction.md` | watertight; commutes with ascertainment (closed in L5) |
| L5 Ascertainment | math-prover | `proofs/ascertainment.md` | watertight (mixed); closed-form fix for LIKE-001 |
| L6 k'-priors | math-prover | `proofs/kprime-priors.md` | Geo / BetaGeo watertight; EG-001 + LS-001 confirmed |
| L7 ACRV discretisation | math-prover | `proofs/acrv.md` | watertight |
| D1 SBC MkNT+Mk' | mcmc-diagnostician | `heavy-tests/sbc.{R,md,sh}` | v10 PASS all 6 arms (tree_length + rate_log_sd); kPrime/p excluded (structural); harness defects 003/004/005/006 fixed |
| D2 TreeESS + SWAP empirical | mcmc-diagnostician | `heavy-tests/{tree-ess-rooting,subtree-swap-db}.R` | TreeESS bit-exact; SWAP empirical inconclusive at n=5 |
| D3 ESS/R-hat stress | mcmc-diagnostician | `heavy-tests/ess-rhat-stress.{R,md}` | harness ready; CONV-002 confirmed live + patched |
| N1 fast_exp + eigendecomp | numerical-auditor | `numerical/fast-exp-conditioning.md` | `fast_neg_exp` stable; **FAST-EXP-001** in surrounding algebra, patched |
| N2 Felsenstein underflow | numerical-auditor | `numerical/pruning-underflow.md` | brief refuted; patch cosmetic |
| N3 Partial-CL cache | numerical-auditor | `numerical/cache-coherence.md` | LIKE-001 reproduced quantitatively; L5 owns the fix |
| post-patch audit | numerical-auditor | `numerical/post-patch-audit.md` | L5/N1/D3 patches all SAFE |
| F81-Het collapse design | math-prover | `proofs/f81-het-collapse.md` | HOLDS-PARTIAL, profile-contingent |

---

## Historical preamble (one paragraph)

Three dispatch waves on 2026-05-26: Wave 1 (commit `34a2607`) ran the five
math-prover foundations; Wave 2 (`9f759d1`) added ascertainment + k'-priors +
two numerical lanes; Wave 3 (`a37c3a6`) added the three diagnostician harnesses
plus the partial-CL cache audit. Production patches L5/N1/D3 + SBC harness
fixes landed as `59276ad`, `8e4e370`, `f9ea652`, `bcd2ec6`. Hamilton runs
17289622, 17289638, 17294905, 17294906, 17295308, 17296284 progressively
narrowed the SBC harness defects; v4 (17296284) revealed that the kPrime
forward/inference mismatch is joint-level, not just kPrime_pooled-level.
Agent profile files (`~/.claude/agents/{math-prover,mcmc-diagnostician,numerical-auditor}.md`)
were revised 2026-05-26 with accumulated wave observations — full per-wave
narrative removed once durable lessons landed in those profiles and per-bug
detail landed in `findings.md`.


