# Red-team focus areas — mkp

Rotation list. The /red-team skill picks the next area each invocation: the
area whose most recent Discussion is oldest (the skill carries the rule and the
query; do not restate either here).

**Discussions #118-#123 are ordering markers, not round records.** Each says so
and names the real record. Delete one as soon as a genuine round record
supersedes it.

Rows marked **Static / Measurement** split their questions by what a cloud
session can answer; the measurement half needs Martin's desktop (Hamilton,
RevBayes, a built package).

| # | Area | Files | Key questions |
|---|------|-------|---------------|
| 1 | k′ prior families & normalisation | `R/MkPrimeModel.R` (`LogPrior`, `.LogPriorEmpiricalGeometric`, `LogPemp`, `MkPrimeEmpiricalPrior`, all four `kPrimePrior` branches); `R/data.R`; `data-raw/empirical_n_obs.R`; `data/empiricalNObs.rda`; `src/mcmc.cpp` (`cpp_log_prior`); `src/corrections.cpp`; `tests/testthat/test-priors.R`, `test-empirical-geometric-prior.R`, `test-logseries-prior.R`, `test-corrections.R` | Covers **all four** priors — `geometric`, `empirical_geometric`, `beta_geometric`, `logseries` — not just EG, and their shared `LogPrior` spine. **The governing principle is that a prior is pre-data**: `kObs_i` is an observation and must not enter it. Does any surviving code path still condition on `kObs` — `priorVariant="conditional"` (the retained opt-in, called *Model B* in the older records; the pre-data default is *Model A*), the truncation normaliser, the relabelling correction? Is each prior normalised over its own support, and does the empirical body→tail join stay consistent? Do R and C++ agree to ~1e-9 on every arm, including `beta_geometric`, whose two hyperparameter columns have already caused an off-by-one elsewhere (STREAM-004)? Numerical stability in log space at the tails. Does `empiricalNObs` cover the range real datasets hit? Edge cases: `n_obs = 0`, `n_obs` above the table maximum, `NA` in `kObs`. Open issues #48, #49, #51, #52, #53, #55, #56 are the live questions. |
| 2 | k′ marginalisation & the p-samplers | `src/mcmc.cpp` (`compute_per_kprime_log_lik`, marginal_k branches, case 30 `mh_logit_p`, case 35 `gibbs_p_marginal`, the move-gating switch); `src/mcmc_likelihood.cpp`; `src/node_cl_cache.h`; `R/MkPrimeModel.R` (`likelihoodMode`); `R/RunMkPrime.R` (EG + marginal paths, `.BuildMoves`); `tests/testthat/test-marginal-k-*.R`; `dev/notes/2026-05-28-marginal-k-plan.md`; `dev/red-team/MARGINAL-K-*.md` | The `marginal_k` evaluator is the newest and highest-risk surface on `main`, and **`fixTopology = TRUE` SBC has already failed to catch two topology defects here** (FREEZE-003 Bug B: the evaluator read `state->parent/child` instead of its passed arguments) — so every question below must be asked under a **free** topology. **Two moves remain gated off** — `gibbs_spr` and `gibbs_subtree_swap` (the `droppedByMargK` block in `.BuildMoves`; re-enable design: `dev/red-team/MARGINAL-K-GIBBS-MARGINAL-AWARE-design.md`); the weighted/block family was re-enabled via scratch-eval in `c68f3dc`. Both remaining gates are necessary for a deeper reason than cache coherence: they select candidates at fixed kPrime and write `state->logLik` directly, so no cache fix makes them target-correct (verified 2026-09-18). Is the scratch-eval re-enable sound — `compute_full_loglik_at` does not force cold, and the `useCache` gate carries no topology or branch fingerprint. Is the `u_max(p)` truncation and its normaliser correct, and is the tail mass it discards bounded and stated? Is case 35 (`gibbs_p_marginal`, opt-in) invariant — derive it, do not assume; the claimed ~3.2× p-ESS win says nothing about correctness. Does case 30 still hold up (Jacobian, detailed balance, gating)? Do the v1 unsupported combinations (`empirical_geometric`/`beta_geometric`/`logseries`, `qHeterogeneity`, `usePartitioned`) actually abort at the boundary rather than silently mis-evaluating? |
| 3 | Model finalisation, stepping stone & marginal likelihood | `R/SteppingStone.R`; `R/MkPrimeModel.R` (`.FinalizeModel`, `treeLengthRate` / `expSteps` resolution); `src/mcmc.cpp` (`cpp_log_prior` tree-length term, `gibbs_kprime_sweep` cost); `tests/testthat/test-stepping-stone*.R`, `test-kprime-sweep-truncation.R` | Do prior normalising constants that cancel in MCMC ratios survive in stepping-stone ratios (#51, #65)? Does each stone run the configured move schedule, including partitioned moves? Is the sweep cost bounded in prior-dominated stones (#66)? Is `treeLengthRate` scaled per character (#62)? Does any nullable model field resolve differently on the run and resume paths? |
| 4 | Research harnesses & recorded-result provenance | `data-raw/hamilton/**`; `data-raw/*.R`; `dev/pilots/**`; `dev/m9-pilot/**`; `inst/hamilton/**`; `dev/red-team/heavy-tests/**`; `RED_TEAM_summariser.md` | **Static (cloud-auditable):** Can each recorded headline result be regenerated from committed config, and does its commit carry the script (#100)? Do joins pair by key, not position? Do SBC forward and inference models match (#57)? Can concurrent array tasks or arms clobber each other's files? Do the harness's stopping and time budgets hold? **Measurement (needs Hamilton):** does a rerun reproduce the recorded numbers? |
| 5 | Move scheduler, tuning & tempering | `R/MkPrimeMCMC.R`; `R/TuningBudget.R` (bandit half); `R/RunMkPrime.R` (`.RunMkPrimeSingleRun` phase logic, `.BuildTemperatureLadder`, `.ProposeChainSwap`, `.AdaptTemperatures`, `.AdaptMoveWeights`, `.DecayLowAcceptMoves`, `.AdaptTuning`, `.PerturbMoveWeights` and neighbours); `tests/testthat/test-{m092-adaptive-scheduler,move-weight-budget,move-category,tempering,three-phase,t018-adaptive-moves,proposal-tuning,adapt-thin,auto-thin,tuning-budget,m126-warmup-rho,cache-aware-scheduling}.R` | Are move weights renormalised after every adaptation, and does the bandit (#219-#222) leave the sampled chain's target unchanged? Is adaptation frozen before sampling? Is swap acceptance for heated chains correct, and are ladder adaptations frozen before sampling? Does the three-phase loop hand state between phases without dropping moves or tuning? |
| 6 | Streaming, checkpoint, resume & GUI consumers | `R/streaming.R`; `R/RunMkPrime.R` (`ResumeMkPrime`, interrupt handler, `.RunParallelRuns`, `.InitRun` / `.SaveCheckpoint` serialisers, `.ParamNames`, `.BrColStart`); `R/BayesianModule.R`; `inst/MkPrime/app.R`; `R/EasyMkPrime.R`; `R/verbosity.R` | Does the stop-button handler leave valid state? Does resume from checkpoint reproduce the continuation, including the column contract shared by the three chain serialisers? Do trace appends avoid double headers on resume? Does `.RelaunchFromCheckpoint` / log polling read what the writer writes? Parallel runs (`nCore > 1`) do not checkpoint worker state. |
| 7 | Tree-move proposals & Rcpp surface | `R/proposals.R`; `src/tree_moves.cpp`; `src/proposals.cpp`; `src/fitch.h`; `src/mcmc.cpp` (tree-move cases: `gibbs_spr`, `gibbs_subtree_swap`, `weighted_*`, pSPR, TBR); `src/mcmc_state.h`; `tests/testthat/test-{tree-moves,nni-preorder,pspr,tbr,tbr-detailed-balance,topology-detailed-balance,subtree-swap,swap-partial-cl,gibbs-spr*,gibbs-subtree-swap-invariance,weighted-*,topo-hash,determinism-gibbs-subtree-swap}.R`; `dev/red-team/proofs/*hastings*.md` | Do NNI/SPR/TBR Hastings ratios hold on the rooted state space (root placement, degree-2 root in TBR), not just over unrooted topologies? Do branch-length proposals respect bounds? Does Rcpp ↔ R state round-trip? |
| 8 | Likelihood kernel & partial CLs | `R/likelihood.R`; `R/MkPrimeData.R` (`.PhyDatToIntMatrix`, tip/row pairing); `R/acrv.R`; `src/mcmc_likelihood.cpp`; `src/likelihood.cpp`; `src/ascertainment.cpp`; `src/rate_matrix.cpp`; `src/acrv.cpp`; `src/f81.h`; `src/fast_exp.h`; `src/node_cl_cache.h`; `src/gibbs_partial_cl.h` (non-partition paths); `tests/testthat/test-{likelihood,full-loglik,ascertainment,fused-ascertainment,node-cl-cache,m161-selective-cache,jc-collapse*,rate-matrix,acrv,het,fast-exp,m170-csp-symmetry}.R`; `dev/red-team/proofs/{ascertainment,f81-collapse,f81-het-collapse,acrv}.md`; `dev/red-team/numerical/` | Is each Q normalised to mean rate 1, as RevBayes does? Is the invariance event right for characters with `?`/`-` and polymorphic tips? Are tips paired with data rows by label, not position (#224)? Is `coding = "informative"` the parsimony-uninformative set for k ≥ 3? Is partial-CL cache invalidation complete? Underflow on deep trees. |
| 9 | Convergence diagnostics & ESS | `R/Convergence.R`; `R/ConvergenceTier.R`; `R/ess.R`; `R/treeESS.R`; `src/tree_ess.cpp`; `R/burnin.R`; `R/TuningBudget.R` (ESS/s pricing); `R/MkPosterior.R`; `R/PlotDuringMCMC.R`; `R/RunMkPrime.R` (`.CheckConvergence*`, `.ComputeTreeEssInLoop`, `.WarnUnreachableCriteria`, `.EstimateEta`, `.MinEssRate`) | Are stopping criteria reachable in every orchestration path? Does tree ESS credit only real topology movement (#195)? Is ESS/s priced consistently across resume and parallel runs? Does multi-chain R-hat handle heated chains? |
| 10 | Test infrastructure | `tests/testthat/helper-*.R`; `tests/testthat/setup.R`; `tests/testthat/_reference/`; `skip_slow_tests()` gating; `.github/workflows/*.yml`; `.lintr`; plus a rotating sample of test files no other row names | Which tests assert the property their name claims (mutation-check the high-stakes ones)? Which tiers does CI actually run (#136)? Is every golden fixture a regenerable oracle rather than a re-baselined snapshot (#137)? Which default-on moves or defaults have no failing test? |
| 11 | **RB-oracle equivalence** | `dev/rb-equivalence/**`; package code only as far as needed to establish the model MkPrime runs | **Static (cloud-auditable):** is the harness a valid oracle (same model, same data, same tips) independent of any measurement? **Measurement (needs Hamilton + RevBayes):** do MkPrime posteriors agree statistically with RevBayes on the 5 frozen matrices × 2 models (by_nt_9v, by_nt_kv)? Cross-sampler R-hat < 1.025 and pooled ESS > 128 on every scalar (tree_length, rate_log_sd, rate_loss, rate_neo) and on CID-to-pooled-median? Wall-seconds to target trend stable across commits? Tree ESS comparable (reported, not gating, since RB lacks the diagnostic)? |
| 12 | Red-team process meta-review | `dev/red-team/focus-areas.md`, `README.md`, `discussion-categories.md`, `findings-archive.md`, `migration-map.tsv`; `dev/plans/`, `dev/notes/`, root `RED_TEAM_*.md` (prior-art stores); open issues labelled `red-team` | Reviews the rotation, not the package. Run the scope-coverage diff: which files does no row own? Are any areas too broad, too narrow or overlapping? Do rows cite closed issues, deleted files or dead line numbers? Is tier routing consistent with the dry and dormancy rules in the `/red-team` skill (never restate them here)? Category headroom before proposing an area (25 cap). Output is concrete restructuring actions tied to yield evidence. |
| 13 | Partition API & hierarchical hyperprior | `R/partition-api.R`; `R/partition.R`; `R/RunMkPrime.R` (callers of `.InitStatePartitioned` / `.ParamNamesPartitioned`, `unlink=` plumbing); `R/MkPrimeModel.R` (`hyper_tau`, `class_rate_log_sd_z`, class-weight prior); `src/mcmc.cpp` (cases 31 `scale_class_rate_log_sd`, 32 `dirichlet_simplex_class_w`, 34 `scale_hyper_tau` and its restore paths, `cpp_log_prior_partitioned`, `neoScale` / `transScale` sites); `src/mcmc_likelihood.cpp` (`cpp_log_likelihood_partitioned`); `src/gibbs_partial_cl.h` (partitioned paths); `src/mcmc_state.h` (partition fields); `tests/testthat/test-partition*.R`, `helper-partition-*.R`, `_reference/`; `dev/rb-equivalence/notes/partition-rate-and-acrv-audit.md`; `dev/red-team/reviews/fix-partition-rate-normalisation/` | Does every evaluator (full, partial-CL, node-CL cache, Gibbs k′, Gibbs SPR/subtree-swap, weighted moves, marginal_k) apply per-class rates, σ_c and the `neoScale`/`transScale` normalisation identically? Test each against the full evaluation under a multi-class partition with a free topology. Is cache invalidation complete after a `rate_neo` change now that it rescales trans partitions (the review's F1 blocker)? Is the non-centred `hyper_tau` / `class_rate_log_sd_z` Jacobian right, and is case 34's restore symmetric with its proposal? Is `dirichlet_simplex_class_w` reversible, and does its own α reach it (#76)? Does `unlink=` coercion silently drop a user request? Is the bitcompat reference a valid oracle (#137)? Do stepping stone and resume carry the partitioned move set and state? |
