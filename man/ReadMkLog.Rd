% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streaming.R
\name{ReadMkLog}
\alias{ReadMkLog}
\title{Read a MkPrime streaming log file}
\usage{
ReadMkLog(logFile)
}
\arguments{
\item{logFile}{Character.  Path to one or more log files.  If a vector of
paths is supplied (e.g. the \verb{$logFile} field of an \code{MkPosterior} from a
multi-run streaming analysis), the samples are combined row-wise.}
}
\value{
A named numeric matrix with one row per thinned sample.  Column
names match those of \code{MkPosterior$samples}.  The \code{Sample} column (MCMC
iteration numbers) is stored as the row-names attribute and is not
included as a data column.
}
\description{
Loads the tab-separated parameter log written by \code{\link[=RunMkPrime]{RunMkPrime()}} when
\code{logFile} is set in \code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}.  The returned matrix has the same
column layout as \code{MkPosterior$samples} and can be assigned directly:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{result$samples <- ReadMkLog(result$logFile)
summary(result)
}\if{html}{\out{</div>}}
}
