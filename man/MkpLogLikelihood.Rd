% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/likelihood.R
\name{MkpLogLikelihood}
\alias{MkpLogLikelihood}
\title{Compute log-likelihood of morphological data on a tree}
\usage{
MkpLogLikelihood(
  tree,
  mkd,
  kPrime = NULL,
  rate_loss = 1,
  rate_log_sd = 0,
  nCat = 6L,
  coding = "variable",
  rate_neo = 1,
  relabel = TRUE
)
}
\arguments{
\item{tree}{A \code{phylo} object (from ape). Must be unrooted or rooted.}

\item{mkd}{A \code{MkPrimeData} object.}

\item{kPrime}{Integer vector of length \code{mkd$nChar}. The assumed true number
of states for each character. For "known" characters, this is the fixed k.
For "neomorphic", this must be 2. For "transformational", this is the
current MCMC proposal.}

\item{rate_loss}{Rate asymmetry for neomorphic characters
(1.0 = symmetric). Default 1.0.}

\item{rate_log_sd}{Standard deviation of the log-normal ACRV distribution.
0 means no rate variation. Default 0.}

\item{nCat}{Number of ACRV rate categories. Default 6.}

\item{coding}{Ascertainment bias correction type. \code{"none"} for no
correction, \code{"variable"} for conditioning on variable characters,
\code{"informative"} for conditioning on parsimony-informative characters
(excludes every pattern in which fewer than two states each occur
twice or more). Default \code{"variable"}.}

\item{rate_neo}{Rate scalar for the neomorphic partition relative to
transformational (which is fixed at 1.0). Default 1.0 (equal rates).}

\item{relabel}{Logical. Apply Mk' relabelling correction for
transformational characters? Set to \code{FALSE} for standard Mk
likelihood (e.g., for validation against phangorn). Default \code{TRUE}.}
}
\value{
Scalar log-likelihood.
}
\description{
Evaluates the Mk' likelihood \insertCite{Lewis2001}{MkPrime} via
Felsenstein pruning \insertCite{Felsenstein1981}{MkPrime}, with optional
ACRV \insertCite{Harrison2014}{MkPrime} and ascertainment bias correction.
}
\references{
\insertAllCited{}
}
