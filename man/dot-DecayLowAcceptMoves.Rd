% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{.DecayLowAcceptMoves}
\alias{.DecayLowAcceptMoves}
\title{Decay weights of low-acceptance moves during warmup.}
\usage{
.DecayLowAcceptMoves(
  currentWeights,
  batchAccept,
  batchPropose,
  initialWeights,
  moveNames,
  pinnedWeights = NULL,
  accept_floor = 0.02,
  decay = 0.7,
  weight_floor = 0.1,
  n_min = 30L
)
}
\arguments{
\item{currentWeights}{Numeric vector (current move probabilities, sums to 1).}

\item{batchAccept}{Integer vector of \strong{per-batch} accept counts (cold chain).}

\item{batchPropose}{Integer vector of \strong{per-batch} propose counts (cold chain).}

\item{initialWeights}{Numeric vector of move weights at warmup start
(used to compute the per-move floor).}

\item{moveNames}{Character vector of move names.}

\item{pinnedWeights}{Named numeric vector or NULL.}

\item{accept_floor}{Acceptance rate threshold below which decay fires
(default 0.02 = 2 \%).}

\item{decay}{Multiplicative decay factor applied to flagged moves
(default 0.7).}

\item{weight_floor}{Floor as a fraction of each move's initial weight
(default 0.1).  Prevents any move from falling below
\code{weight_floor * initialWeights[m]}.}

\item{n_min}{Minimum proposals in the batch before decay is considered
(default 30).}
}
\value{
Numeric vector of (possibly decayed and re-normalised) move weights.
}
\description{
Complement to \code{.AdaptMoveWeights()}. Applies a multiplicative decay to
any free move whose \strong{batch-level} acceptance rate falls below
\code{accept_floor}, provided at least \code{n_min} proposals were made in the batch.
Pinned moves are never decayed. Weights are normalised to sum to the
unpinned budget after decay. A per-move floor prevents any move from being
completely eliminated.
}
\details{
This adaptation is designed to run during warmup only; the caller is
responsible for not invoking it after the warmup-to-sampling transition.

Diagnostic output (per-move before/after) is emitted to stderr at
verbosity 2, or when \code{MKPRIME_ADAPT_DIAG=1} is set.
}
\keyword{internal}
