% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MkPrimeModel.R
\name{MkPrimeModel}
\alias{MkPrimeModel}
\title{Specify an MkPrime model}
\usage{
MkPrimeModel(
  coding = "variable",
  nCat = 6L,
  relabel = TRUE,
  treeLengthShape = 2,
  treeLengthRate = NULL,
  expSteps = NULL,
  expStepsInflation = 1.05,
  rateLossMeanlog = 0,
  rateLossSdlog = 2,
  rateLogSdShape = 1,
  rateLogSdRate = 1,
  kPrimePrior = "empirical_geometric",
  empiricalNObs = NULL,
  kprimeHyperA = 1,
  kprimeHyperB = 1,
  kprimeAlpha = 1,
  kprimeBeta = 1,
  kprimeLogseriesC = 0.7,
  rateNeoMeanlog = 0,
  rateNeoSdlog = 1,
  qHeterogeneity = FALSE,
  nBetaCat = 4L,
  betaScaleShape = 1,
  betaScaleRate = 1,
  ecologyAware = FALSE,
  magnitudeMode = "global",
  rho0Alpha = 360,
  rho0Beta = 120,
  thetaAlpha = 2,
  thetaBeta = 2,
  sigmaPhi = 1.5,
  gibbsZEvery = 50L
)
}
\arguments{
\item{coding}{Ascertainment bias correction: \code{"variable"} (default),
\code{"informative"}, or \code{"none"}.}

\item{nCat}{Number of ACRV rate categories (default 6).}

\item{relabel}{Apply Mk' relabelling correction for transformational
characters? Default \code{TRUE}.}

\item{treeLengthShape, treeLengthRate}{Shape and rate for the Gamma prior
on tree length. Defaults: shape = 2, rate = 2 / \code{expSteps}.}

\item{expSteps}{Expected tree length (sum of edge lengths in expected
substitutions per character). Used to set the tree length prior scale
via \code{rate = 2 / expSteps}. Default \code{NULL}: the parsimony (Fitch) score
of the starting tree on the supplied data is computed in
\code{\link[=.FinalizeModel]{.FinalizeModel()}}, divided by the number of
characters in the data (\code{nChar}), and inflated by \code{expStepsInflation}
(default 1.05). This encodes the parsimony score as a lower bound on
total expected state changes (\verb{TL × nChar}), with the expectation that
true tree length sits a few percent above the per-character parsimony
minimum. A numeric value supplied here is treated as a user override
and is used verbatim (no division or inflation applied).}

\item{expStepsInflation}{Multiplicative factor applied to
\code{parsimony_score / nChar} when \code{expSteps = NULL}. Default 1.05.
Ignored when \code{expSteps} is set explicitly.}

\item{rateLossMeanlog, rateLossSdlog}{Parameters for the LogNormal prior
on \code{rate_loss} (neomorphic asymmetry). Defaults: meanlog = 0, sdlog = 2.}

\item{rateLogSdShape, rateLogSdRate}{Shape and rate for the Gamma prior
on \code{rate_log_sd} (ACRV dispersion). Defaults: shape = 1, rate = 1.}

\item{kPrimePrior}{Prior distribution for the true number of character states
(\verb{k'}) for transformational characters. One of \code{"empirical_geometric"}
(default; convolution of an empirical pmf on the number of observed
states with a \code{Geometric(p)} prior on the number of unobserved states,
so \verb{k' = N_obs + N_unobs}),
\code{"beta_geometric"} (per-character Beta-Geometric with shared
hyperparameters \code{alpha}, \code{beta}),
\code{"geometric"} (hierarchical geometric with Beta hyperprior on \code{p}),
or \code{"logseries"} (logarithmic series with fixed parameter \code{c}; matches
the RevBayes default).
The \code{"empirical_geometric"} option counters the tendency of inference
to collapse to zero unobserved states by anchoring the observed-state
component to a real-data empirical distribution; the \code{"beta_geometric"}
option avoids the same over-shrinkage in a different way, by allowing
per-character \code{p}.}

\item{empiricalNObs}{An object of class \code{"MkPrimeEmpiricalPrior"} (see
\code{\link[=MkPrimeEmpiricalPrior]{MkPrimeEmpiricalPrior()}}) giving the empirical pmf on \code{N_obs} used by
the \code{"empirical_geometric"} prior. Defaults to the package dataset
\link{empiricalNObs} tabulated from the \code{neotrans} corpus. Ignored for other
prior options.}

\item{kprimeHyperA, kprimeHyperB}{Parameters for the Beta hyperprior on \code{p}
when \code{kPrimePrior = "geometric"}. Defaults: a = 1, b = 1 (uniform).}

\item{kprimeAlpha, kprimeBeta}{Starting values for the shared
hyperparameters of the Beta-Geometric prior
(\code{kPrimePrior = "beta_geometric"}). Both must be positive.
Defaults: alpha = 1, beta = 1. These are estimated during MCMC with
Exponential(1) hyperpriors.}

\item{kprimeLogseriesC}{The \code{c} parameter of the log-series prior on \verb{k'}
when \code{kPrimePrior = "logseries"}. Must be in (0, 1). Default 0.7, matching
the RevBayes \code{dnMkPrime} default.}

\item{rateNeoMeanlog, rateNeoSdlog}{Parameters for the LogNormal prior
on the neomorphic partition rate scalar. Defaults: meanlog = 0, sdlog = 1
(95\% prior interval ~ \link{0.14, 7.4}).  Earlier defaults used sdlog = 2,
but that admitted rate_neo blow-ups along the (tree_length, rate_neo)
ridge near JC saturation; sdlog = 1 prevents the pathology while still
covering realistic per-partition rate scalings.}

\item{qHeterogeneity}{Logical. Enable Q-matrix heterogeneity across
characters via discretised Dirichlet-marginal equilibrium frequencies?
Default \code{FALSE}. When enabled, each character's likelihood is averaged
over a set of F81 rate matrices whose equilibrium frequencies are drawn
from \code{Beta(beta_scale, (k - 1) * beta_scale)}, where \code{k} is the number
of states. This is the marginal distribution of one component of a
symmetric \code{Dirichlet(beta_scale, ..., beta_scale)}. See the
\strong{Q-matrix heterogeneity} section below for details.}

\item{nBetaCat}{Integer. Number of equal-probability Beta bins for the
heterogeneity discretisation. Default \code{4L}. Higher values increase
accuracy at the cost of compute time (scales linearly with \code{nBetaCat}).
Ignored when \code{qHeterogeneity = FALSE}.}

\item{betaScaleShape, betaScaleRate}{Shape and rate for the Gamma prior
on \code{beta_scale} (the symmetric Dirichlet concentration parameter).
Defaults: shape = 1, rate = 1. Ignored when \code{qHeterogeneity = FALSE}.}

\item{ecologyAware}{Logical. Enable the ecology-aware NT model? Default
\code{FALSE}. When \code{TRUE}, the supplied \link{MkPrimeData} must carry an \code{ecology}
component (see \code{\link[=MkPrimeData]{MkPrimeData()}}). Per-character substitution rates then
depend on the ecology assigned to each edge through a marginal-mixture
over edge ecology probabilities. See \code{vignette("ecology-details", package = "MkPrime")} for the mathematical specification.}

\item{magnitudeMode}{How the rate-modifier magnitude is shared across
ecologies: \code{"global"} (default; single \code{phi}) or \code{"per_ecology"} (one
\code{phi_e} per ecology state). Ignored when \code{ecologyAware = FALSE}.}

\item{rho0Alpha, rho0Beta}{Shape parameters for the Beta hyperprior on
\code{pi_0}, the prior probability that a (character, ecology) pair has
no ecology effect. Defaults: 360, 120 (mode 0.75, effective sample
size 480). The spike-and-slab is structurally a sparsity prior;
earlier defaults of Beta(75, 25) (ESS = 100) were overwhelmed by
typical z-cell counts (480 in a 120/360 NT split at kEco = 2):
because per-cell likelihood contrast is sub-nat on datasets with
O(10) tips per ecology, the Gibbs z sweep samples from the prior
rather than the data, and pi0 MH then chases the z-counts (R5-1).
ESS = 480 matches the z-cell count in the sim3 setting, giving the
prior and data equal weight. Users with substantially different
character counts may want to override (Beta(alpha, alpha/3) preserves
mode 0.75 while scaling ESS with nChar).
Ignored when \code{ecologyAware = FALSE}.}

\item{thetaAlpha, thetaBeta}{Shape parameters for the Beta hyperprior on
\code{theta_e}, the slab balance for non-reference ecology \code{e}. Given a
non-none \code{z}, \code{P(z = encouraged | non-none) = theta_e}. Defaults: 2, 2
(\code{Beta(2, 2)}, symmetric bell centred at 0.5). The uniform \code{Beta(1, 1)}
provides zero counterweight to z-cell counts: when per-cell likelihood
contrast is sub-nat (O(10) tips per ecology), theta drifts freely to
the 0.5 saddle where z=1 and z=2 become exchangeable, symmetrising
the chain and accelerating pi0 collapse (R5-3). \code{Beta(2, 2)} puts mild
mass at 0.5 while still admitting the biologically natural boundary
cases (\code{theta} near 0 or 1) at finite (though low) density; this
balances the counterweight need against boundary accessibility.
Ignored when \code{ecologyAware = FALSE}.}

\item{sigmaPhi}{Standard deviation of the LogNormal prior on \code{phi} (or
on each \code{phi_e}). Default 1.5 (95\% prior interval ~ \link{0.09, 11.2},
centred at no-effect \code{phi = 1}). With \code{sigmaPhi = 1.0} the prior
density at phi = 1 (no effect) was 9x higher than at phi = 4
(moderate ecology effect), making the prior the dominant force when
per-cell likelihood contrast is sub-nat (R5-2). At \code{sigmaPhi = 1.5},
the ratio falls to ~5x, giving the data a realistic chance to move
phi to ecologically plausible values.  Earlier defaults used 0.5,
which penalised \verb{|log phi| > ~1} heavily.
Ignored when \code{ecologyAware = FALSE}.}

\item{gibbsZEvery}{Integer. Number of MCMC generations between Gibbs
sweeps over the per-(character, ecology) influence categories \code{z}.
Default 50. Ignored when \code{ecologyAware = FALSE}.}
}
\value{
An S3 object of class \code{MkPrimeModel}.
}
\description{
Specify an MkPrime model
}
\section{Ecology-aware NT model}{


When \code{ecologyAware = TRUE}, the model adds a per-edge, per-character rate
modifier whose value depends on the inferred ecology of each edge. Edge
ecology is reconstructed as a marginal under a standard Mk(K) process on
the same tree, and the per-edge mixture weight \verb{w_\{e\}(s)} for ecology
state \code{s} is taken from this marginal at the parent node.

For each pair \verb{(c, e)} of (character, ecology state) a latent influence
category \verb{z_\{c,e\}} is sampled with three values:
\describe{
\item{\code{none}}{character \code{c}'s rate is unchanged in ecology \code{e}}
\item{\code{encouraged}}{rate scaled by \code{phi} (asymmetric for neomorphic)}
\item{\code{discouraged}}{rate scaled by \code{1 / phi} (asymmetric for neomorphic)}
}
The prior on \code{z} is sparse: \code{P(z = none) = pi_0}, with \code{pi_0 ~ Beta(360, 120)}
by default (mode 0.75, ESS 480) so most characters are \emph{a priori} unaffected
by ecology.
}

\section{Q-matrix heterogeneity}{


Standard Mk and Mk' assume all characters share the same (equal-frequency)
rate matrix. In reality, some morphological characters may have strongly
unequal state frequencies. Q-matrix heterogeneity (\code{qHeterogeneity = TRUE})
relaxes this by integrating each character's likelihood over a mixture of
F81 rate matrices \insertCite{Felsenstein1981}{MkPrime} with varying
equilibrium frequencies.

The mixture is controlled by a single scalar parameter, \code{beta_scale}
(= \eqn{\alpha}), which acts as the concentration of a symmetric Dirichlet:
\itemize{
\item Large \eqn{\alpha}: all characters have nearly equal state
frequencies, recovering the standard Mk/JC model.
\item Small \eqn{\alpha}: characters can have strongly unequal
frequencies (one state dominant, others rare).
}

The key insight is that the marginal distribution of one component of
\eqn{\mathrm{Dirichlet}(\alpha, \ldots, \alpha)} with \eqn{k} components
is \eqn{\mathrm{Beta}(\alpha, (k-1)\alpha)}. This allows a unified
discretisation for characters of any state count: the scheme adapts
automatically to \eqn{k}.

For binary characters (\eqn{k = 2}), this simplifies to the symmetric
\eqn{\mathrm{Beta}(\alpha, \alpha)}.

This feature is intended for \strong{model comparison} (e.g. via
\code{\link[=mkp_stepping_stone]{mkp_stepping_stone()}}). It multiplies computation time by roughly
\eqn{4-5\times}{4-5x} for binary-dominated datasets.
See \code{vignette("het-details", package = "MkPrime")} for the full
mathematical derivation.
}

