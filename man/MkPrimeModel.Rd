% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MkPrimeModel.R
\name{MkPrimeModel}
\alias{MkPrimeModel}
\title{Specify an MkPrime model}
\usage{
MkPrimeModel(
  coding = "variable",
  nCat = 6L,
  relabel = TRUE,
  treeLengthShape = 2,
  treeLengthRate = NULL,
  expSteps = NULL,
  rateLossMeanlog = 0,
  rateLossSdlog = 2,
  rateLogSdShape = 1,
  rateLogSdRate = 1,
  kPrimePrior = "empirical_geometric",
  empiricalNObs = NULL,
  kprimeHyperA = 1,
  kprimeHyperB = 1,
  kprimeAlpha = 1,
  kprimeBeta = 1,
  kprimeLogseriesC = 0.7,
  rateNeoMeanlog = 0,
  rateNeoSdlog = 2,
  qHeterogeneity = FALSE,
  nBetaCat = 4L,
  betaScaleShape = 1,
  betaScaleRate = 1,
  classRateConcentration = 1,
  priorOnClassRateLogSd = c("hyperprior_pooled", "gamma_independent"),
  likelihoodMode = c("sampled_k", "marginal_k"),
  priorVariant = NULL,
  kprimeTruncK = 200L
)
}
\arguments{
\item{coding}{Ascertainment bias correction: \code{"variable"} (default),
\code{"informative"}, or \code{"none"}.}

\item{nCat}{Number of ACRV rate categories (default 6).}

\item{relabel}{Apply Mk' relabelling correction for transformational
characters? Default \code{TRUE}.}

\item{treeLengthShape, treeLengthRate}{Shape and rate for the Gamma prior
on tree length. Defaults: shape = 2, rate = 2 / \code{expSteps}.}

\item{expSteps}{Expected number of character state changes. Used to set
the tree length prior scale. Default \code{NULL} (computed from parsimony score
of the starting tree).}

\item{rateLossMeanlog, rateLossSdlog}{Parameters for the LogNormal prior
on \code{rate_loss} (neomorphic asymmetry). Defaults: meanlog = 0, sdlog = 2.}

\item{rateLogSdShape, rateLogSdRate}{Shape and rate for the Gamma prior
on \code{rate_log_sd} (ACRV dispersion). Defaults: shape = 1, rate = 1.
Used as-is in the legacy single-σ path and as the legacy
\code{priorOnClassRateLogSd = "gamma_independent"} per-class prior.
Not consulted by the default pooled hyperprior (see
\code{priorOnClassRateLogSd}).}

\item{kPrimePrior}{Prior distribution for the true number of character states
(\verb{k'}) for transformational characters. One of \code{"empirical_geometric"}
(default; convolution of an empirical pmf on the number of observed
states with a \code{Geometric(p)} prior on the number of unobserved states,
so \verb{k' = N_obs + N_unobs}),
\code{"beta_geometric"} (per-character Beta-Geometric with shared
hyperparameters \code{alpha}, \code{beta}),
\code{"geometric"} (hierarchical geometric with Beta hyperprior on \code{p}),
or \code{"logseries"} (logarithmic series with fixed parameter \code{c}; matches
the RevBayes default).
The \code{"empirical_geometric"} option counters the tendency of inference
to collapse to zero unobserved states by anchoring the observed-state
component to a real-data empirical distribution; the \code{"beta_geometric"}
option avoids the same over-shrinkage in a different way, by allowing
per-character \code{p}.}

\item{empiricalNObs}{An object of class \code{"MkPrimeEmpiricalPrior"} (see
\code{\link[=MkPrimeEmpiricalPrior]{MkPrimeEmpiricalPrior()}}) giving the empirical pmf on \code{N_obs} used by
the \code{"empirical_geometric"} prior. Defaults to the package dataset
\link{empiricalNObs} tabulated from the \code{neotrans} corpus. Ignored for other
prior options.}

\item{kprimeHyperA, kprimeHyperB}{Parameters for the Beta hyperprior on \code{p}
when \code{kPrimePrior = "geometric"}. Defaults: a = 1, b = 1 (uniform).}

\item{kprimeAlpha, kprimeBeta}{Starting values for the shared
hyperparameters of the Beta-Geometric prior
(\code{kPrimePrior = "beta_geometric"}). Both must be positive.
Defaults: alpha = 1, beta = 1. These are estimated during MCMC with
Exponential(1) hyperpriors.}

\item{kprimeLogseriesC}{The \code{c} parameter of the log-series prior on \verb{k'}
when \code{kPrimePrior = "logseries"}. Must be in (0, 1). Default 0.7, matching
the RevBayes \code{dnMkPrime} default.}

\item{rateNeoMeanlog, rateNeoSdlog}{Parameters for the LogNormal prior
on the neomorphic partition rate scalar. Defaults: meanlog = 0, sdlog = 2.}

\item{qHeterogeneity}{Logical. Enable Q-matrix heterogeneity across
characters via discretised Dirichlet-marginal equilibrium frequencies?
Default \code{FALSE}. When enabled, each character's likelihood is averaged
over a set of F81 rate matrices whose equilibrium frequencies are drawn
from \code{Beta(beta_scale, (k - 1) * beta_scale)}, where \code{k} is the number
of states. This is the marginal distribution of one component of a
symmetric \code{Dirichlet(beta_scale, ..., beta_scale)}. See the
\strong{Q-matrix heterogeneity} section below for details.}

\item{nBetaCat}{Integer. Number of equal-probability Beta bins for the
heterogeneity discretisation. Default \code{4L}. Higher values increase
accuracy at the cost of compute time (scales linearly with \code{nBetaCat}).
Ignored when \code{qHeterogeneity = FALSE}.}

\item{betaScaleShape, betaScaleRate}{Shape and rate for the Gamma prior
on \code{beta_scale} (the symmetric Dirichlet concentration parameter).
Defaults: shape = 1, rate = 1. Ignored when \code{qHeterogeneity = FALSE}.}

\item{classRateConcentration}{Numeric specifying the concentration of the
symmetric Dirichlet prior on per-class relative rate weights; values below
one favour uneven class rates, values above one pull them together.}

\item{priorOnClassRateLogSd}{Prior structure on per-class ACRV
dispersion \code{σ_c = class_rate_log_sd[c]} when \code{unlink = "shape"} is
active with two or more user classes. One of:
\itemize{
\item \code{"hyperprior_pooled"} (default): non-centred half-normal hierarchy
\verb{σ_c = τ · z_c}, with \code{z_c ~ HalfNormal(1)} i.i.d. and
\code{τ ~ HalfNormal(1)}. Pools information about within-class rate
dispersion across classes; designed for cells where some classes
have few characters and so carry little likelihood signal on σ_c.
\item \code{"gamma_independent"}: each \code{σ_c} independently
\code{Gamma(rateLogSdShape, rateLogSdRate)}. Legacy behaviour, kept for
backward compatibility and prior-sensitivity comparisons.
}

Ignored when \code{unlink} does not include \code{"shape"} or when only one
user class is present (the structure is degenerate at K = 1; the
single σ uses the \code{rateLogSdShape} / \code{rateLogSdRate} Gamma prior).}

\item{likelihoodMode}{How per-character \verb{k'_i} enters the likelihood.
One of:
\itemize{
\item \code{"sampled_k"} (default): \verb{k'_i} is a sampled MCMC state variable;
a per-character integer-walk + Gibbs sweep (case 25) move family
updates it. The trace carries \code{kPrime_i...} columns and \code{LogPrior}
evaluates the per-character \verb{P(k'_i | hyperparams)} term.
\item \code{"marginal_k"} (v1 = geometric arm only): \verb{k'_i} is analytically
marginalised out of the likelihood at every evaluation. The
posterior is over \verb{(tree, mu, sigma, p)} only; the slow discrete
coordinate is removed. Required when chain mixing on \verb{k'_i} is
known to be uninformative (per finding EG-003 in
\code{dev/red-team/findings.md}). v1 supports
\code{kPrimePrior = "geometric"} only — other arms are §11 follow-ups
in \code{dev/notes/2026-05-28-marginal-k-plan.md}. Het + marginal-k and
partition-API + marginal-k are deferred (§13 of the plan).
}}

\item{priorVariant}{Whether the \verb{k'} prior conditions on the observed state
count \code{kObs_i}. One of \code{"unconditional"} (Model A) or \code{"conditional"}
(Model B), or \code{NULL} (default) to select per \code{kPrimePrior} (see below).
A prior is pre-data, so \code{kObs_i} — an observation — should not enter it:
\itemize{
\item \code{"unconditional"} (Model A): the prior lives on the full support
\verb{k' >= 2}, unconditional on \code{kObs_i}. For \code{empirical_geometric} the
convolution is used with no per-character \code{Z_i(p)} truncation; for
\code{geometric} the marginal weight for state count \code{k} is \code{p (1-p)^(k - 2)}.
The \verb{k' >= kObs_i} floor (you cannot have fewer states than observed) is
enforced by the likelihood, not the prior. This is the correct pre-data
prior and matches the forward simulator used by the SBC harness.
\item \code{"conditional"} (Model B): the prior conditions on \code{kObs_i} —
\verb{k'_i ~ kObs_i + Geometric(p)} for \code{geometric}, and for
\code{empirical_geometric} the convolution is renormalised by
\verb{Z_i(p) = sum_\{k >= kObs_i\} P(k | p)}. Model B is data-dependent and
fails SBC on \code{p} (see \code{project_sbc_kprime_structural}); retained only for
backward comparison.
}

Default (\code{NULL}): \code{"unconditional"} for both \code{"empirical_geometric"} and
\code{"geometric"} (the correct pre-data prior; the geometric SBC harness uses
\verb{k' ~ 2 + Geo(p)}, so the default aligns inference with the validated SBC
forward model). Consulted for \code{empirical_geometric} and for \code{geometric}
under \code{likelihoodMode = "marginal_k"}; ignored for \code{beta_geometric} and
\code{logseries}.}

\item{kprimeTruncK}{Integer specifying the cap \code{K} at which the geometric
arm's \verb{k'} prior is truncated and renormalised over \verb{[2, K]}, bounded
above by the compile-time candidate cap of 256.}
}
\value{
An S3 object of class \code{MkPrimeModel}.
}
\description{
Specify an MkPrime model
}
\section{Q-matrix heterogeneity}{


Standard Mk and Mk' assume all characters share the same (equal-frequency)
rate matrix. In reality, some morphological characters may have strongly
unequal state frequencies. Q-matrix heterogeneity (\code{qHeterogeneity = TRUE})
relaxes this by integrating each character's likelihood over a mixture of
F81 rate matrices \insertCite{Felsenstein1981}{MkPrime} with varying
equilibrium frequencies.

The mixture is controlled by a single scalar parameter, \code{beta_scale}
(= \eqn{\alpha}), which acts as the concentration of a symmetric Dirichlet:
\itemize{
\item Large \eqn{\alpha}: all characters have nearly equal state
frequencies, recovering the standard Mk/JC model.
\item Small \eqn{\alpha}: characters can have strongly unequal
frequencies (one state dominant, others rare).
}

The key insight is that the marginal distribution of one component of
\eqn{\mathrm{Dirichlet}(\alpha, \ldots, \alpha)} with \eqn{k} components
is \eqn{\mathrm{Beta}(\alpha, (k-1)\alpha)}. This allows a unified
discretisation for characters of any state count: the scheme adapts
automatically to \eqn{k}.

For binary characters (\eqn{k = 2}), this simplifies to the symmetric
\eqn{\mathrm{Beta}(\alpha, \alpha)}.

This feature is intended for \strong{model comparison} (e.g. via
\code{\link[=mkp_stepping_stone]{mkp_stepping_stone()}}). It multiplies computation time by roughly
\eqn{4-5\times}{4-5x} for binary-dominated datasets.
See \code{vignette("het-details", package = "MkPrime")} for the full
mathematical derivation.
}

