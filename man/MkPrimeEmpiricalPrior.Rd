% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{MkPrimeEmpiricalPrior}
\alias{MkPrimeEmpiricalPrior}
\title{Construct an empirical prior on observed state counts}
\usage{
MkPrimeEmpiricalPrior(
  body,
  tail_decay = 0,
  tail_start_k = NULL,
  nSource = NA_integer_
)
}
\arguments{
\item{body}{Numeric vector of probabilities \code{P(N_obs = 2)},
\code{P(N_obs = 3)}, ..., \code{P(N_obs = nMax)}.  Need not sum to 1; the
remaining mass is assumed to lie in the geometric tail.}

\item{tail_decay}{Geometric decay rate \code{q} for the tail, with
\verb{0 < q < 1}.  Default \code{0} (no tail; pmf truncated at \code{nMax}).  When
\code{0}, \code{body} must sum to 1.}

\item{tail_start_k}{Integer specifying the smallest \code{k} governed by the
tail; must be \code{length(body) + 2L}, as the tail is anchored on the last
\code{body} entry.}

\item{nSource}{Optional integer.  Number of characters from which the
body was derived (for provenance).}
}
\value{
An object of class \code{"MkPrimeEmpiricalPrior"}.
}
\description{
Build a \code{"MkPrimeEmpiricalPrior"} object for use as the \code{empiricalNObs}
argument of \code{\link[=MkPrimeModel]{MkPrimeModel()}}.
}
\details{
The full pmf is
\deqn{P(N_{obs} = k) = body[k - 1] \quad (k = 2, \ldots, nMax)}
\deqn{P(N_{obs} = k) = c \cdot q^{k - tail\_start\_k} \quad (k \ge tail\_start\_k)}
with \code{c = tail_start_p} chosen automatically so the pmf normalises to 1.
}
\examples{
# Uniform on k = 2..5, no tail
MkPrimeEmpiricalPrior(body = rep(0.25, 4))

# Body plus geometric tail
MkPrimeEmpiricalPrior(body = c(0.6, 0.3), tail_decay = 0.4)
}
