% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotDuringMCMC.R
\name{MkpWatchLog}
\alias{MkpWatchLog}
\title{Watch a MkPrime log file and draw live trace plots}
\usage{
MkpWatchLog(
  logFiles,
  interval = 2,
  params = NULL,
  maxSamples = 2000L,
  warmup = NULL
)
}
\arguments{
\item{logFiles}{Character vector of log file paths. For a single run,
supply the value of \code{mcmc$logFile} directly. For multiple runs, supply
the resolved per-run paths (e.g. \code{"run_1.log"}, \code{"run_2.log"}), or use
\code{\link[=MkLogPaths]{MkLogPaths()}} to expand a base name.}

\item{interval}{Seconds between poll–redraw cycles. Default \code{2}.}

\item{params}{Character vector of parameter names to plot. Default
\code{NULL} auto-selects key scalar parameters (\code{log_posterior},
\code{tree_length}, \code{rate_loss}, \code{rate_log_sd}, \code{p}, plus up to three
\verb{kPrime_*} columns). Branch lengths and \code{log_likelihood} are excluded.}

\item{maxSamples}{Maximum rows to read per file (tail of file). Set lower
for faster redraws on large logs. Default \code{2000}.}

\item{warmup}{Integer. If supplied, draws a vertical dashed line at this
sample index to mark the warmup boundary. Note that log files only
contain post-warmup samples by default, so this is rarely needed.
Default \code{NULL}.}
}
\value{
The last-read sample data, invisibly. For a single run, a numeric
matrix (as returned by \code{\link[=ReadMkLog]{ReadMkLog()}}). For multiple runs, a named list
of such matrices.
}
\description{
Polls one or more Tracer-compatible log files written by \code{\link[=RunMkPrime]{RunMkPrime()}}
(when \code{logFile} is set in \code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}) and redraws trace plots in the
active graphics device at each interval. Intended to be run interactively
while a \code{RunMkPrime()} call executes in a background job or another R
session.
}
\details{
Press \strong{Ctrl+C} (or Escape in RStudio) to stop watching. The last-read
sample data is returned invisibly.

The function waits up to 30 seconds for \code{logFiles[1]} to appear on disk
(useful when called immediately after launching a background MCMC job).
If the file still does not exist after 30 seconds, the function aborts.
}
\seealso{
\code{\link[=ReadMkLog]{ReadMkLog()}} to read a log file after the run has finished.
\code{\link[=MkpTracePlot]{MkpTracePlot()}} for the in-memory callback-based alternative.
}
