% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{.AdaptMoveWeights}
\alias{.AdaptMoveWeights}
\title{Adapt move weights based on per-move acceptance rates and wall-time.}
\usage{
.AdaptMoveWeights(
  currentWeights,
  acceptCount,
  proposeCount,
  moveTimeNs,
  moveNames,
  moveDim = rep(1L, length(currentWeights)),
  pinnedWeights,
  warmupProgress,
  tStart = 2,
  tEnd = 0.5,
  wMin = 0.01,
  minProposals = 20L
)
}
\arguments{
\item{currentWeights}{Numeric vector (current move probabilities,
sums to 1).}

\item{acceptCount}{Named integer vector (cold chain, cumulative).}

\item{proposeCount}{Named integer vector (cold chain, cumulative).}

\item{moveTimeNs}{Named numeric vector (cold chain, cumulative ns).}

\item{moveNames}{Character vector of move names.}

\item{moveDim}{Integer vector of per-move dimensionality (number of
parameters updated per call). Default: all 1s.}

\item{pinnedWeights}{Named numeric vector or NULL.}

\item{warmupProgress}{Fraction of warmup completed (0 to 1).}

\item{tStart}{Starting softmax temperature (default 2.0).}

\item{tEnd}{Ending softmax temperature (default 0.5).}

\item{wMin}{Floor per free move as fraction of 1 (default 0.01).}

\item{minProposals}{Minimum proposals before adapting (default 20).}
}
\value{
Updated weight vector (sums to 1).
}
\description{
Uses softmax reweighting with temperature annealing. Pinned moves
are excluded from adaptation. Moves with fewer than \code{minProposals}
proposals keep their current weight.
}
\details{
The score formula accounts for multi-dimensional moves via the
\code{moveDim} parameter: \code{score = accept_rate * dim / cost}. For
single-parameter moves (\code{dim = 1}), this reduces to the original
\code{accept_rate / cost}. Block moves (e.g. block Gibbs branch sweep)
set \code{dim = nEdge} so the scheduler values them proportionally to
the number of parameters they update per call.
}
\keyword{internal}
