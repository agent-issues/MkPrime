% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotDuringMCMC.R
\name{MkpPngProgress}
\alias{MkpPngProgress}
\title{Create a PNG-writing progress callback}
\usage{
MkpPngProgress(dir, width = 800, height = 600)
}
\arguments{
\item{dir}{Directory to write progress files to. Created if it does
not exist.}

\item{width, height}{PNG dimensions in pixels.}
}
\value{
A function with signature \verb{function(info)} suitable for use
as \code{progressFn}.
}
\description{
Returns a progress callback function suitable for \code{progressFn} in
\code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}. Each invocation writes a trace-plot PNG and a JSON
status file to the specified directory. Files are written atomically
(write to temp, then rename) so that external consumers (e.g., a
Shiny app) never read a half-written file.
}
\details{
Two files are written on each call:
\describe{
\item{\code{mkp_progress.png}}{Trace plots (same as \code{\link[=MkpTracePlot]{MkpTracePlot()}}).}
\item{\code{mkp_progress.json}}{JSON object with fields \code{iter}, \code{nIter},
\code{warmup}, \code{inWarmup}, \code{elapsed}, and \code{recentAcceptance}.}
}
}
