% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{.RunMkPrimeSingleRun}
\alias{.RunMkPrimeSingleRun}
\title{Run the complete MCMC batch loop for one independent run}
\usage{
.RunMkPrimeSingleRun(
  mkd,
  model,
  mcmc,
  initialState,
  moves,
  tipLabels,
  runIdx,
  paramNames,
  nEdge,
  brColStart,
  logFilePath,
  cancelFile,
  checkpointFile,
  startIter = 1L,
  isStreaming = FALSE,
  convWindowSize = 0L,
  treeFile = NULL,
  shared = NULL
)
}
\arguments{
\item{initialState}{Run list from \code{\link[=.InitRun]{.InitRun()}} or a checkpoint, with
\code{chains} in checkpoint-compatible format (no \code{chainStates}).}

\item{runIdx}{Integer index of this run (1-based), used for display only.}

\item{startIter}{First iteration to run (1L for fresh, >1 when resuming).}
}
\description{
Self-contained: accepts only R-serializable inputs (no XPtrs), reconstructs
C++ state internally, runs the full \code{repeat} loop, and returns a serializable
updated run state.  Used by the sequential \verb{for (run)} loop in
\code{\link[=RunMkPrime]{RunMkPrime()}} / \code{\link[=ResumeMkPrime]{ResumeMkPrime()}} and (Phase 10b) by \code{future} workers.
}
\keyword{internal}
