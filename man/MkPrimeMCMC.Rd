% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MkPrimeMCMC.R
\name{MkPrimeMCMC}
\alias{MkPrimeMCMC}
\title{Configure MCMC settings}
\usage{
MkPrimeMCMC(
  nIter = Inf,
  thin = "auto",
  treeThin = NULL,
  warmup = NULL,
  minWarmup = 2000L,
  maxWarmup = NULL,
  autoTune = TRUE,
  tuningBudget = 10000L,
  tuningRounds = 5L,
  nRuns = 2L,
  nChains = 1L,
  heat = 0.2,
  maxTime = NULL,
  minEss = NULL,
  maxRhat = NULL,
  minTreeEss = NULL,
  checkEvery = 1000L,
  cancelFile = NULL,
  checkpointFile = NULL,
  treeFile = NULL,
  logFile = NULL,
  bufferSize = 500L,
  plotEvery = NULL,
  progressFn = NULL,
  gibbsSpr = TRUE,
  gibbsSubtreeSwap = TRUE,
  tbr = TRUE,
  pSpr = TRUE,
  joint2d = TRUE,
  weightedBranchScale = FALSE,
  weightedSpr = FALSE,
  weightedSubtreeSwap = FALSE,
  blockGibbsBranch = FALSE,
  dirichletBranch = TRUE,
  dirichletK = NULL,
  localDirichlet = TRUE,
  localDirichletK = NULL,
  nBranchBins = 10L,
  moveWeights = NULL,
  cacheBonus = 5,
  tuning = list(),
  nCore = getOption("mc.cores", 1L),
  pollInterval = 10L,
  cancelGrace = 30L,
  gibbsWarmupFactor = 1/3
)
}
\arguments{
\item{nIter}{Total iterations (including warmup). Default \code{Inf}, which
relies on stopping criteria (\code{minEss}, \code{maxRhat}, \code{maxTime}) to
terminate the run. Pass a finite integer to cap the number of iterations
regardless of convergence.}

\item{thin}{Thinning interval (save every \code{thin}-th iteration).
Default \code{"auto"}: initially set to the number of active move types
(one "full cycle" per stored sample), then adapted at the first
convergence check to \code{max(nMoves, round(maxACT * log(2)))} based on
the observed autocorrelation time of the worst-mixing scalar
parameter. This targets ~50\\% correlation between consecutive
stored samples. Pass a positive integer to fix the thinning interval
and disable adaptation.}

\item{treeThin}{Thinning interval for tree samples (iterations).
Default \code{NULL}, which stores a tree for every scalar sample
(equivalent to \code{treeThin = thin}). Pass a positive integer that is
a \strong{multiple of \code{thin}} to store trees less frequently than scalar
parameters. For example, \verb{thin = 10, treeThin = 100} stores one tree
for every 10 scalar samples. The multiple-of-thin constraint is
validated at run time (after \code{thin = "auto"} is resolved).}

\item{warmup}{\strong{Deprecated.} If supplied, treated as \code{maxWarmup}.
Use \code{minWarmup} / \code{maxWarmup} instead. Retained for backward
compatibility; a deprecation message is emitted when non-NULL.}

\item{minWarmup}{Minimum warmup iterations before the stabilisation
detector can end warmup. Default 2000. Set lower for quick
debugging runs.}

\item{maxWarmup}{Maximum warmup iterations. Warmup ends when the
chain stabilises OR this ceiling is reached (with a warning).
Default 50 000 when \code{nIter = Inf}; \code{nIter / 2} when finite.}

\item{autoTune}{Logical; enable the tuning phase after warmup
(default \code{TRUE}). When \code{TRUE}, a short tuning phase optimises
move weights by maximising min-ESS/s using a perturbation bandit.
When \code{FALSE}, move weights are frozen at the end of warmup using
the acceptance-rate heuristic (legacy behaviour). See section
\strong{Three-phase MCMC} below.}

\item{tuningBudget}{Maximum iterations in the tuning phase.
Default 10 000. Only used when \code{autoTune = TRUE}.}

\item{tuningRounds}{Number of perturbation-evaluation rounds in
the tuning phase. Default 5. Each round evaluates the current
weights plus \code{nPerturbations} candidates. Only used when
\code{autoTune = TRUE}.}

\item{nRuns}{Number of independent runs. Default 2. Each run has its
own set of \code{nChains} chains. Convergence diagnostics (R-hat) require
\code{nRuns >= 2}.}

\item{nChains}{Number of chains in the temperature ladder (parallel
tempering). Default 1 (no tempering). Set to 4 for typical analyses.
Chain 1 is the cold chain (beta = 1).}

\item{heat}{Temperature of the hottest chain (0 < heat < 1). Default
0.2. The temperature ladder uses geometric spacing:
\code{beta_i = heat^((i-1)/(nChains-1))} for i = 1, ..., nChains.
Ignored when \code{nChains = 1}.}

\item{maxTime}{Maximum wall-clock time in seconds. \code{NULL} (default)
means no time limit.}

\item{minEss}{Minimum effective sample size for early stopping.
\code{NULL} (default) disables ESS-based stopping.}

\item{maxRhat}{Maximum R-hat (rank-normalized; Vehtari et al. 2021)
for early stopping. \code{NULL} (default) disables R-hat-based stopping.
The modern recommendation is 1.01 for reliable inference; 1.05 is
a pragmatic threshold for phylogenetics where mixing is slower.
Requires \code{nRuns >= 2}.
Replaces the classical PSRF (Gelman-Rubin) statistic.}

\item{minTreeEss}{Minimum tree-topology ESS (median pseudo-ESS) for
early stopping.  \code{NULL} (default) disables tree-ESS-based stopping.
When set, tree ESS is computed adaptively during convergence checks:
skipped when scalar ESS is far from \code{minEss}, coarse (500-tree
subsample) when approaching, and fine (1000-tree subsample) when
tree ESS is the binding constraint.  Requires \strong{TreeDist}.
Also used in the tuning-phase bandit: topology moves receive credit
for improving tree ESS, preventing underallocation.}

\item{checkEvery}{Check convergence every this many iterations
(default 1000). Only used when stopping criteria are set.}

\item{cancelFile}{Path to a cancel-signal file. \code{NULL} (default) disables
cancel-file checking. When set, \code{\link[=RunMkPrime]{RunMkPrime()}} checks every 200
iterations whether this file exists. If it does, the run flushes any
buffered samples, saves a checkpoint (if \code{checkpointFile} is set), and
exits with \code{stop_reason = "cancelled"}. Create the file to request a
clean stop: \code{file.create(cancelFile)}. See also \code{\link[=MkCancelPath]{MkCancelPath()}}.}

\item{checkpointFile}{Path to write checkpoint RDS files. \code{NULL}
(default) auto-derives from \code{logFile} when set
(e.g. \code{"run.log"} -> \code{"run.ckp"}). Set to \code{FALSE} to
disable checkpointing. Checkpoints are saved at each
convergence check interval and on cancel.}

\item{treeFile}{Path to write sampled trees in Newick format.
\code{NULL} (default) auto-derives from \code{logFile} when set
(e.g. \code{"run.log"} → \code{"run_trees.nwk"}). Set to \code{FALSE} to
disable tree file logging entirely. Trees are always stored
in the returned \code{MkPosterior} object regardless.}

\item{logFile}{Path to write a tab-separated scalar parameter log
(Tracer-compatible). \code{NULL} (default) keeps all samples in memory only.
If the path has no file extension, \code{.log} is appended automatically.
When set, samples are flushed to disk in batches of \code{bufferSize}, so the
file can be opened in Tracer during the run.
For multiple runs (\code{nRuns > 1}), separate files are created automatically
by appending \verb{_1}, \verb{_2}, \dots before the file extension
(e.g. \code{"run.log"} → \code{"run_1.log"}, \code{"run_2.log"}).
See \code{\link[=ReadMkLog]{ReadMkLog()}} to load the log back into R after the run.}

\item{bufferSize}{Integer. Number of thinned samples to accumulate per run
before flushing to \code{logFile}. Ignored when \code{logFile = NULL}. Default 500.
Smaller values give more frequent flushes (lower Tracer latency, slightly
more I/O); larger values amortise write overhead at the cost of slightly
higher peak memory.}

\item{plotEvery}{Integer; invoke the progress callback every this many
iterations. In interactive sessions, defaults to \code{checkEvery} so that
live trace plots update alongside convergence checks. Set to \code{NULL}
explicitly to disable. Typical values: 500--2000.}

\item{progressFn}{Progress callback function, the string \code{"default"},
or \code{NULL}. In interactive sessions, defaults to \code{\link[=MkpTracePlot]{MkpTracePlot()}} for
live base-R trace plots. In non-interactive sessions (e.g. \code{Rscript}),
defaults to \code{NULL} (no plotting). Setting \code{plotEvery} without
\code{progressFn} also implies \code{MkpTracePlot}. A custom function must
accept a single argument: a named list with fields \code{iter}, \code{nIter},
\code{warmup}, \code{inWarmup}, \code{nRuns}, \code{nChains}, \code{runSamples},
\code{currentState}, \code{recentAcceptance}, and \code{elapsed}. See
\code{\link[=MkpTracePlot]{MkpTracePlot()}} for details.}

\item{gibbsSpr}{Logical; include the Gibbs SPR move (default \code{TRUE}).
Cost: O(N) likelihood evaluations per proposal, where N is the number
of candidate reattachment edges.}

\item{gibbsSubtreeSwap}{Logical; include the Gibbs subtree-swap move
(default \code{TRUE}). Cost: O(N) likelihood evaluations.}

\item{tbr}{Logical; include the TBR (Tree Bisection and Reconnection)
topology move (default \code{TRUE}). TBR is a superset of SPR: it additionally
re-roots the pruned subtree at a random internal edge before regrafting,
enabling larger jumps in tree space. Cost: O(nEdge) per proposal (same
as SPR). The adaptive scheduler will downweight TBR on small trees where
SPR is sufficient.}

\item{pSpr}{Logical; include the parsimony-guided SPR move (default
\code{TRUE}). Like standard SPR but weights candidate regraft positions by
Fitch parsimony score, so topologically better positions are proposed
more often. Much cheaper than Gibbs SPR (no likelihood evaluation per
candidate), but better guided than uniform SPR.
See Yang & Rodríguez (2013); Ronquist et al. (2020).}

\item{joint2d}{Logical; include 2D joint Bactrian proposals for
correlated parameter pairs (default \code{TRUE}). Proposes correlated
updates to tree_length × rate_log_sd (and tree_length × rate_loss
when neomorphic characters are present) using a bivariate Bactrian
kernel. The correlation is learned adaptively during warmup from
posterior sample correlations. No effect when parameters are
uncorrelated (degenerates to independent proposals).}

\item{weightedBranchScale}{Logical; include the weighted branch-length
scale move (default \code{FALSE}). Cost: O(B) likelihood evaluations, where
B = \code{nBranchBins}.}

\item{weightedSpr}{Logical; include the weighted SPR move (default
\code{FALSE}). Cost: O(N * B) likelihood evaluations. Enable for improved
mixing on difficult tree spaces at the cost of slower iterations.}

\item{weightedSubtreeSwap}{Logical; include the weighted subtree-swap
move (default \code{FALSE}). Cost: O(N * B) likelihood evaluations.}

\item{blockGibbsBranch}{Logical; include the block Gibbs branch-length
sweep move (default \code{FALSE}). Each call sweeps over all edge pairs
in random-permutation order, sampling each from an approximate
conditional via \code{nBranchBins} bin evaluations and independent MH
accept/reject. Cost: O(nEdge * B) likelihood evaluations per call.
Useful when single-edge moves (BetaSimplex, weighted branch scale)
mix slowly through the branch-length space. The adaptive scheduler
accounts for the multi-dimensional nature of this move via its \code{dim}
field.}

\item{dirichletBranch}{Logical; include the block Dirichlet simplex
branch-length move (default \code{TRUE}). Each proposal selects K random
edges, draws new fractions from a Dirichlet centered on current values,
and rescales the remaining edges to maintain the simplex. Cost: one
likelihood evaluation per proposal (same as BetaSimplex).}

\item{dirichletK}{Integer or \code{NULL}; number of edges to update per
Dirichlet branch proposal. Default \code{NULL} uses \code{min(nEdge, 5)}.
Smaller values give higher acceptance but smaller moves; larger
values give bolder moves at the cost of lower acceptance.}

\item{localDirichlet}{Logical; include a localized Dirichlet branch
proposal that selects K \emph{connected} edges via BFS from a random
starting edge (default \code{TRUE}). The connected selection produces
compact dirty sets for efficient partial CL evaluation, and targets
correlated local branch lengths.}

\item{localDirichletK}{Integer or \code{NULL}; number of connected edges
for the localized Dirichlet proposal. Default \code{NULL} uses
\code{min(nEdge, 6)}.}

\item{nBranchBins}{Integer; number of branch-fraction bins for weighted
and block Gibbs moves (default \code{10L}). Used by \code{weightedBranchScale},
\code{weightedSpr}, \code{weightedSubtreeSwap}, and \code{blockGibbsBranch}.
Ignored when all bin-based moves are disabled. Higher values increase
accuracy of the Gibbs approximation but cost more likelihood
evaluations.}

\item{moveWeights}{Named numeric vector of user-pinned move weights,
or \code{NULL} (default). When non-NULL, each named entry fixes the
probability of proposing that move type. Names must match valid move
names (e.g., \code{"nni"}, \code{"spr"}, \code{"gibbs_spr"}, \code{"tree_length"}, etc.).
Values must be positive and sum to at most 1. Remaining probability
is distributed among un-pinned moves by the adaptive scheduler during
warmup. Example: \code{moveWeights = c(nni = 0.3, spr = 0.2)} fixes NNI
at 30\% and SPR at 20\%, with the remaining 50\% allocated adaptively
among other moves. To disable adaptive scheduling entirely, pin all
moves (sum to 1). See section \strong{Adaptive move scheduling} below.}

\item{cacheBonus}{Numeric; multiplier applied to partial-CL-eligible
move weights (NNI, beta_simplex, Dirichlet, local_dirichlet) when the
node CL cache is valid. Default 5. A value of 1 disables the boost.
Higher values preferentially select fast partial-CL moves after
another partial-CL move succeeds, exploiting the ~15\eqn{\times}
speedup. Statistically valid: each component kernel individually
satisfies detailed balance; only selection frequency changes.
Ignored when Q-heterogeneity is enabled (partial CL not supported).}

\item{tuning}{Named list of initial tuning parameters for each move
type. See Details.}

\item{nCore}{Integer. Number of parallel worker processes for independent
runs. Default \code{getOption("mc.cores", 1L)}, matching the convention used
by \pkg{TreeDist}. With \code{nCore = 1} (the default), runs execute serially.
With \code{nCore > 1} and \code{nRuns > 1}, runs are dispatched as background R
processes via \code{\link[callr:r_bg]{callr::r_bg()}} and the parent process polls for
convergence. If \code{nRuns == 1}, \code{nCore} is ignored (within-run
parallelism is a separate facility). When \code{nRuns > nCore}, runs are
dispatched from a rolling pool of \code{nCore} workers: as each finishes,
the next pending run launches in its place. Cross-run convergence
(\code{maxRhat}) cannot trigger early in this regime — it activates only
once every run has produced samples — so prefer \code{nRuns <= nCore}
when convergence-based early stopping matters.

\strong{Parallel runs are fully resumable.} Each callr worker writes its
own per-run \code{.ckp} file every \code{checkEvery} iterations (paths derived
from \code{checkpointFile}: \code{run.ckp} -> \code{run_1.ckp}, \code{run_2.ckp}, ...).
On SIGKILL, SLURM walltime overrun, or Ctrl-C, work is lost only
back to the most recent per-run checkpoint -- bounded by \code{checkEvery}
iterations per run.  \code{\link[=ResumeMkPrime]{ResumeMkPrime()}} (or just re-running the same
\code{\link[=RunMkPrime]{RunMkPrime()}} call with \code{overwrite = FALSE}) synthesises the master
checkpoint from the per-run \code{.ckp} files when the parent process did
not exit cleanly.  Checkpoint writes use a tmp+rename idiom so a
SIGKILL during a save leaves the previous \code{.ckp} intact.}

\item{pollInterval}{Integer. Seconds between convergence polls in parallel
mode. Ignored when \code{nCore = 1}. Default \code{10L}.}

\item{cancelGrace}{Integer (seconds) or \code{Inf}. After a stopping criterion
fires (cancel file, \code{maxTime}, convergence), each parallel worker is sent
a cancel file and given \code{cancelGrace} seconds to finish its current batch
and exit cleanly. Workers still alive after the grace period are
hard-killed. Default \code{30L}.

Trade-off:
\itemize{
\item \strong{Shorter grace} → quicker abort, but workers mid-batch are hard-killed
and their in-progress samples are lost (may trigger PAR-006-style
all-killed scenarios if batches are longer than the grace period).
\item \strong{Longer grace} → cancel is slower, but workers finish their batch
reliably before stopping.
\item \code{Inf} → wait indefinitely; workers are never hard-killed. Use when
batch completion is more important than a timely abort.
}

Ignored when \code{nCore = 1}.}

\item{gibbsWarmupFactor}{Numeric in (0, 1]; weight of the Gibbs kPrime
sweep relative to other moves during warmup (default \code{1/3}). A lower
value reduces warmup overhead from the sweep; the weight is restored at
the warmup-to-tuning transition.}
}
\value{
An S3 object of class \code{MkPrimeMCMC}.
}
\description{
Configure MCMC settings
}
\details{
Default tuning values:
\itemize{
\item \code{scale_tree_length}: 0.5
\item \code{beta_simplex}: 10
\item \code{scale_rate_loss}: 0.5
\item \code{scale_rate_log_sd}: 0.5
\item \code{scale_p}: 0.5
\item \code{int_walk_window}: 1
}
\subsection{Parallel tempering}{

When \code{nChains > 1}, chains run at different temperatures. The cold
chain (beta = 1) targets the true posterior. Heated chains (beta < 1)
flatten the likelihood surface, aiding exploration. Chain swap proposals
(see M-030) exchange states between adjacent temperatures.
}

\subsection{Gibbs and weighted moves}{

The Gibbs moves (\code{gibbsSpr}, \code{gibbsSubtreeSwap}) evaluate all candidate
topologies and sample proportional to their posterior weight. They cost
O(N) likelihood evaluations per proposal (where N is the number of
candidates, roughly the number of edges) but often achieve much better
mixing than standard NNI/SPR. On larger trees (> ~20 tips), the
per-invocation cost grows quadratically and may outweigh the mixing gain.
Initial Gibbs weights are therefore capped to limit their time share;
the adaptive scheduler refines further during warmup.

The weighted moves (\code{weightedBranchScale}, \code{weightedSpr},
\code{weightedSubtreeSwap}) additionally marginalise over a discrete grid
of branch-fraction placements. This produces proposals that are
approximately independent of the current state but costs
O(B) or O(N * B) likelihood evaluations. These are off by default
and recommended only when standard + Gibbs moves show poor mixing.

The block Gibbs branch-length sweep (\code{blockGibbsBranch}) updates all
edge pairs in a single MCMC move via random-permutation-scan
MH-within-Gibbs. Each edge pair is sampled from an approximate
conditional (bin-based, same as \code{weightedBranchScale}) and
accepted/rejected independently. Cost: O(nEdge * B). Off by default.
The adaptive scheduler accounts for its multi-dimensional nature via
a \code{dim} field (= nEdge) in the score formula.
}

\subsection{Three-phase MCMC: Warmup / Tuning / Sample}{

The MCMC engine runs in three phases, shown in the progress display:

\strong{Warmup.} Tuning parameters (scale, window), move weights
(acceptance-rate/cost heuristic), and temperatures (parallel
tempering) all adapt. The phase ends automatically when the cold
chain's log-posterior stabilises (Geweke z-score test), or when
\code{maxWarmup} is reached. No samples are saved.

\strong{Tuning.} The chain is approximately stationary. Move weights
are optimised to maximise min-ESS/s (minimum effective sample
size per second across parameters) using a perturbation bandit.
Each round evaluates the current and perturbed weight vectors
over short windows, adopting the best. Tuning samples are
discarded. Step-size tuning is frozen. The phase runs for
\code{tuningRounds} rounds or up to \code{tuningBudget} iterations.
Set \code{autoTune = FALSE} to skip this phase.

\strong{Sample.} Move weights are frozen and posterior samples are
collected. Convergence is monitored at \code{checkEvery} intervals.
The run terminates when \code{minEss}/\code{maxRhat} criteria are met,
\code{maxTime} is reached, or \code{nIter} iterations complete.

Use \code{moveWeights} to pin specific move frequencies and exclude
them from adaptation in all phases.
}
}
