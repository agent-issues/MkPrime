% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MkPrimeMCMC.R
\name{MkPrimeMCMC}
\alias{MkPrimeMCMC}
\title{Configure MCMC settings}
\usage{
MkPrimeMCMC(
  nIter = 10000L,
  thin = 10L,
  warmup = NULL,
  nRuns = 2L,
  nChains = 1L,
  heat = 0.2,
  max_time = NULL,
  min_ess = NULL,
  max_psrf = NULL,
  check_every = 1000L,
  checkpoint_file = NULL,
  tree_file = NULL,
  tuning = list()
)
}
\arguments{
\item{nIter}{Total iterations (including warmup). Default 10,000.}

\item{thin}{Thinning interval (save every \code{thin}-th iteration).
Default 10.}

\item{warmup}{Number of warmup (adaptation) iterations. Default
\code{nIter / 2}.}

\item{nRuns}{Number of independent runs. Default 2. Each run has its
own set of \code{nChains} chains. Convergence diagnostics (PSRF) require
\code{nRuns >= 2}.}

\item{nChains}{Number of chains in the temperature ladder (parallel
tempering). Default 1 (no tempering). Set to 4 for typical analyses.
Chain 1 is the cold chain (beta = 1).}

\item{heat}{Temperature of the hottest chain (0 < heat < 1). Default
0.2. The temperature ladder uses geometric spacing:
\code{beta_i = heat^((i-1)/(nChains-1))} for i = 1, ..., nChains.
Ignored when \code{nChains = 1}.}

\item{max_time}{Maximum wall-clock time in seconds. \code{NULL} (default)
means no time limit.}

\item{min_ess}{Minimum effective sample size for early stopping.
\code{NULL} (default) disables ESS-based stopping.}

\item{max_psrf}{Maximum PSRF for early stopping. \code{NULL} (default)
disables PSRF-based stopping. Requires \code{nRuns >= 2}.}

\item{check_every}{Check convergence every this many iterations
(default 1000). Only used when stopping criteria are set.}

\item{checkpoint_file}{Path to write checkpoint RDS files. \code{NULL}
(default) disables checkpointing. Checkpoints are saved at each
convergence check interval.}

\item{tree_file}{Path to write sampled trees in Newick format.
\code{NULL} (default) disables file logging. Trees are always stored
in the returned \code{MkPosterior} object regardless.}

\item{tuning}{Named list of initial tuning parameters for each move
type. See Details.}
}
\value{
An S3 object of class \code{MkPrimeMCMC}.
}
\description{
Configure MCMC settings
}
\details{
Default tuning values:
\itemize{
\item \code{scale_tree_length}: 0.5
\item \code{beta_simplex}: 10
\item \code{scale_rate_loss}: 0.5
\item \code{scale_rate_log_sd}: 0.5
\item \code{scale_p}: 0.5
\item \code{int_walk_window}: 1
}
\subsection{Parallel tempering}{

When \code{nChains > 1}, chains run at different temperatures. The cold
chain (beta = 1) targets the true posterior. Heated chains (beta < 1)
flatten the likelihood surface, aiding exploration. Chain swap proposals
(see M-030) exchange states between adjacent temperatures.
}
}
