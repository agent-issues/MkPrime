% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{empiricalNObs}
\alias{empiricalNObs}
\title{Empirical distribution of observed state counts (\code{N_obs})}
\format{
An object of class \code{"MkPrimeEmpiricalPrior"}: a list with
\describe{
\item{body}{Named numeric vector.  \code{body[i]} is \code{P(N_obs = i + 1)} for
\verb{i = 1, ..., nMax - 1}; names are the corresponding \code{k} values
as strings.}
\item{tail_decay}{Geometric decay rate \code{q} governing the tail beyond the
body, so \code{P(N_obs = k + 1) = q * P(N_obs = k)} for \code{k >= tail_start_k}.}
\item{tail_start_k}{Integer: smallest \code{k} governed by the tail.}
\item{tail_start_p}{Probability mass at \code{tail_start_k}.}
\item{nSource}{Number of source characters used to build the body.}
}
}
\source{
\code{data-raw/empirical_n_obs.R}, run against
\verb{https://github.com/ms609/neotrans}.
}
\usage{
empiricalNObs
}
\description{
Tabulated from the transformational character matrices of the \code{neotrans}
project corpus (7,017 informative characters across 87 published
morphological matrices).  The pmf is supported on \code{k >= 2}: an explicit
body covers \verb{k = 2, ..., nMax}, and a fitted geometric tail extends to
\code{k > nMax}.
}
\details{
Used as the empirical component of the \code{"empirical_geometric"} \verb{k'} prior
(see \code{\link[=MkPrimeModel]{MkPrimeModel()}}), where the prior on the total number of states is
the convolution of this empirical pmf with a \code{Geometric(p)} prior on the
number of unobserved states.

Users may supply their own object of class \code{"MkPrimeEmpiricalPrior"} via
\code{\link[=MkPrimeEmpiricalPrior]{MkPrimeEmpiricalPrior()}} and pass it to \code{\link[=MkPrimeModel]{MkPrimeModel()}} through the
\code{empiricalNObs} argument.
}
\seealso{
\code{\link[=MkPrimeEmpiricalPrior]{MkPrimeEmpiricalPrior()}}, \code{\link[=MkPrimeModel]{MkPrimeModel()}}
}
\keyword{datasets}
