% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BayesianModule.R
\name{MkBayesianServer}
\alias{MkBayesianServer}
\title{Shiny server module for Bayesian Mk' analysis}
\usage{
MkBayesianServer(id, dataset, startTree = NULL)
}
\arguments{
\item{id}{Shiny module namespace ID — must match the \code{id} passed to
\code{\link[=MkBayesianUi]{MkBayesianUi()}}.}

\item{dataset}{A \emph{reactive} that returns a \code{phyDat} object, or \code{NULL}
when no dataset is loaded.  Supplied by the host app.}

\item{startTree}{A \emph{reactive} that returns a starting \code{phylo}, or
\code{NULL} to generate a random unrooted tree.  Optional.}
}
\value{
A named list of reactives:
\describe{
\item{\code{jobFile}}{Path to \code{job.rds} for the current or most recent
run, or \code{NULL} if no run has been started.}
\item{\code{trees}}{A \code{multiPhylo} of post-burnin trees when the run is
complete, otherwise \code{NULL}.}
\item{\code{status}}{Character scalar: \code{"idle"}, \code{"running"},
\code{"done"}, \code{"cancelled"}, or \code{"error"}.}
}
}
\description{
Launches MCMC as a detached \code{Rscript} process (survives Shiny session
close / browser reload), writes a \code{job.rds} alongside the log files so
the "Reconnect" button can resume monitoring after a session restart.
Progress is polled every five seconds by reading the streaming TSV log
files produced by \code{\link[=RunMkPrime]{RunMkPrime()}}.
}
\section{Requirements}{

The \code{processx} package must be installed.  Add it via
\code{install.packages("processx")}.
}

\seealso{
\code{\link[=MkBayesianUi]{MkBayesianUi()}}, \code{\link[=RunMkPrime]{RunMkPrime()}}, \code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}},
\code{\link[=ReadMkLog]{ReadMkLog()}}, \code{\link[=MkCancelPath]{MkCancelPath()}}
}
