% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{RunMkPrime}
\alias{RunMkPrime}
\title{Run Bayesian MCMC under the MkPrime model}
\usage{
RunMkPrime(
  data,
  tree = NULL,
  neomorphic = integer(0),
  knownStates = integer(0),
  model = NULL,
  mcmc = NULL,
  fixTopology = FALSE,
  overwrite = FALSE,
  partition = NULL,
  unlink = character(0),
  verbosity = MkPrimeVerbosity(),
  ...
)
}
\arguments{
\item{data}{A \code{phyDat} object or \code{MkPrimeData} object.}

\item{tree}{A \code{phylo} object (starting topology), or \code{NULL} (default)
to start from a greedy parsimony stepwise-addition tree (via
\code{TreeSearch::AdditionTree}) when the \code{TreeSearch} package is
installed, falling back to a neighbour-joining tree otherwise.}

\item{neomorphic, knownStates}{Passed to \code{\link[=MkPrimeData]{MkPrimeData()}} if \code{data} is
a \code{phyDat} object.}

\item{model}{An \code{MkPrimeModel} object, or \code{NULL} for defaults.}

\item{mcmc}{An \code{MkPrimeMCMC} object, or \code{NULL} for defaults.
When \code{NULL}, any additional arguments in \code{...} are forwarded to
\code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}, so MCMC options can be passed inline without
constructing a separate object.}

\item{fixTopology}{Logical. If \code{TRUE}, tree topology is fixed (Phase 3
behaviour). Default \code{FALSE} enables NNI and SPR topology proposals.}

\item{overwrite}{Logical. If \code{FALSE} (the default) and
\code{mcmc$checkpointFile} points to an existing file, the run is
automatically resumed from that checkpoint.
Set to \code{TRUE} to discard the existing checkpoint and start fresh.}

\item{partition}{Optional integer vector of length \code{mkd$nChar} (after
invariant-character drop) assigning each character to a user class.
Values must form a contiguous range \code{1:nClasses}. \code{NULL} (the default)
routes through the unchanged legacy code path; see the §7a bit-identity
contract in \code{NOTES/partition-api-plan.md}.}

\item{unlink}{Character vector of model-component tokens to unlink
across classes. Layer 1 honours \code{"shape"} (per-class \code{rate_log_sd}) and
\code{"ratemultiplier"} (char-weighted mean-1 Dirichlet on class rates).
Layer 2 will add \code{"brlens"} (per-class branch lengths under a shared
topology). Tokens match case-insensitively with partial-prefix
resolution (warns on prefix; errors on ambiguous prefix or unknown
token with an \code{agrep}-driven "did you mean" suggestion). Silently
coerced to \code{character(0)} with an info-level alert when \code{partition}
is \code{NULL} or \code{nClasses == 1} so the AutoPart dispatcher can pass
\code{unlink} uniformly across treatments.}

\item{verbosity}{Integer from \code{0} (silent) to \code{2} (tuning diagnostics)
controlling console output for the duration of the call; see
\code{\link[=MkPrimeVerbosity]{MkPrimeVerbosity()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}. Allows
passing MCMC configuration inline (e.g. \code{nIter}, \code{logFile}, \code{nChains})
without constructing a separate object. Cannot be combined with an
explicit \code{mcmc} argument.}
}
\value{
An \code{MkPosterior} object.
}
\description{
Metropolis-Hastings MCMC sampling tree topology, branch lengths, model
parameters, and per-character k' (for transformational characters).
Supports parallel tempering with a geometric temperature ladder,
multiple independent runs, convergence monitoring, and early stopping.
}
\section{Inline MCMC options}{


For quick runs, pass MCMC options directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- RunMkPrime(data, nIter = 10000, nRuns = 1L)
}\if{html}{\out{</div>}}

For complex configurations, construct the object explicitly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mcmc <- MkPrimeMCMC(nRuns = 2, nChains = 4, minEss = 200)
result <- RunMkPrime(data, mcmc = mcmc)
}\if{html}{\out{</div>}}
}

\section{Parallel independent runs}{


Set \code{nCore} (in \code{\link[=MkPrimeMCMC]{MkPrimeMCMC()}}) to dispatch multiple independent runs
as parallel background R processes via \code{\link[callr:r_bg]{callr::r_bg()}}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Workstation: 4 cores, 4 independent runs
result <- RunMkPrime(data, tree, nRuns = 4L, nCore = 4L)

# Or set via global option (TreeDist-style):
options(mc.cores = 4L)
result <- RunMkPrime(data, tree, nRuns = 4L)
}\if{html}{\out{</div>}}
\subsection{HPC usage}{

Submit one R job per node from your scheduler (SLURM/PBS/LSF) and set
\code{nCore} to the number of cores allocated per node. R-side dispatch to a
remote scheduler is not supported.

For long runs, set \code{checkpointFile} so the job can be resumed if it
times out, and set \code{logFile} to a path on the local node's filesystem.
}
}

