% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{.RunSerialRuns}
\alias{.RunSerialRuns}
\title{Run multiple serial MCMC runs with cross-run R-hat convergence}
\usage{
.RunSerialRuns(
  mkd,
  model,
  mcmc,
  runs,
  moves,
  tipLabels,
  paramNames,
  nEdge,
  brColStart,
  logFilePaths,
  convWindowSize,
  treeFile,
  startIters = NULL,
  shared = NULL,
  startPhase = 1L
)
}
\value{
Named list: \code{runs}, \code{stopReason}, \code{actualIter}.
}
\description{
Called by \code{\link[=.RunWithRecovery]{.RunWithRecovery()}} when \code{nCore == 1}, \code{nRuns >= 2}, and
\code{maxRhat} is set.  Phase 1 runs each run sequentially until per-run ESS
convergence (or nIter / maxTime / cancel).  Phase 2 checks cross-run R-hat
from log files; if not met and iteration headroom remains, resumes each run
for one epoch (\code{checkEvery} iterations) and re-checks.
}
\keyword{internal}
