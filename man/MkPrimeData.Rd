% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MkPrimeData.R
\name{MkPrimeData}
\alias{MkPrimeData}
\title{Prepare morphological data for Mk' analysis}
\usage{
MkPrimeData(data, neomorphic = integer(0), knownStates = integer(0))
}
\arguments{
\item{data}{A \code{phyDat} object (from ape or TreeTools).}

\item{neomorphic}{Integer vector of character indices to treat as neomorphic
(asymmetric binary, 0 = absent, 1 = present). Default: none.}

\item{knownStates}{Named integer vector specifying the known true number of
states for specific characters. Names are character indices (as strings),
values are the true k. Characters listed here use standard Mk(k) with no
k' inference.}
}
\value{
An object of class \code{"MkPrimeData"}, a list with components:
\describe{
\item{matrix}{Character matrix (nTip x nChar), character states as integers
(0-indexed). \code{NA} for missing/ambiguous data.}
\item{nTip}{Number of taxa.}
\item{nChar}{Number of characters.}
\item{taxon_names}{Character vector of taxon names.}
\item{type}{Character vector of length nChar: \code{"neomorphic"},
\code{"transformational"}, or \code{"known"}.}
\item{kObs}{Integer vector: observed state count per character.}
\item{known_k}{Integer vector: known k per character (\code{NA} for
non-"known" characters).}
\item{levels}{Character state levels from the original phyDat.}
}
}
\description{
Accepts a \code{phyDat} object and classifies characters into three types:
neomorphic (asymmetric binary), transformational (Mk' with inferred k'),
or known state space (standard Mk with user-specified k).
}
\details{
Characters not listed in \code{neomorphic} or \code{knownStates} are classified as
\strong{transformational} and will have their true state count k' inferred under
the Mk' model.

For each character, the number of observed states (kObs) is computed by
counting distinct non-ambiguous states across all taxa.
}
\examples{
\dontrun{
library("TreeTools")
data(Lobo.phy)  # example morphological dataset
mkd <- MkPrimeData(Lobo.phy)
summary(mkd)
}

}
