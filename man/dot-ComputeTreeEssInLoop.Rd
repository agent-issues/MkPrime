% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunMkPrime.R
\name{.ComputeTreeEssInLoop}
\alias{.ComputeTreeEssInLoop}
\title{Compute tree ESS from in-memory MCMC runs}
\usage{
.ComputeTreeEssInLoop(runs, maxPerRun, isStreaming)
}
\arguments{
\item{runs}{List of run state objects (each with \verb{$tree_samples} and
\verb{$tree_saved_idx}).}

\item{maxPerRun}{Maximum trees per run to use (controls coarse vs fine).}

\item{isStreaming}{Logical; when \code{TRUE}, trees are in a flat list
(streaming mode stores all trees, no \code{saved_idx} subsetting needed).}
}
\value{
Scalar minimum median pseudo-ESS, or \code{NA_real_} on failure.
}
\description{
Extracts tree samples from each run, subsamples to \code{maxPerRun},
and returns the \strong{minimum} median pseudo-ESS across runs (conservative).
Used during convergence checks when \code{minTreeEss} is set.
}
\keyword{internal}
